{"q": "What is a database index and what does it do for query performance?", "must": ["index"]}
{"q": "Which index types do the sources describe (B-tree, hash, GIN/GiST/BRIN, compound/composite, partial, unique, TTL, text, HNSW/IVFFlat)?", "must": ["B-tree", "partial"]}
{"q": "How does the query planner or optimizer decide to use an index instead of a full table or collection scan?", "must": ["planner"]}
{"q": "What is a covered query or index-only scan, and when does an index cover a query?", "must": ["cover"]}
{"q": "What is selectivity or cardinality and how does it affect whether an index is used or worth creating?", "must": ["selectiv"]}
{"q": "How do I inspect a query plan (explain(), EXPLAIN, EXPLAIN QUERY PLAN, executionStats) to see whether an index was used and how many keys or documents were examined?", "must": ["explain"]}
{"q": "What goes wrong with indexes: unused or redundant indexes, index bloat, write overhead, and full scans (COLLSCAN / Seq Scan / full table scan)?", "must": ["unused"]}
{"q": "How does a compound (multi-column) index differ from a single-field index, and does the order of columns or fields matter (ESR rule, leftmost prefix)?", "must": ["compound"]}
{"q": "How do I define an index in the ORM or schema layer (Prisma @@index, Drizzle index()/uniqueIndex(), Convex .index(), Nile/Postgres CREATE INDEX, Turso/SQLite CREATE INDEX)?", "must": ["@@index"]}
{"q": "Which sources cover indexing most, and how do MongoDB's terms (IXSCAN, COLLSCAN, createIndex) map to the SQL/Postgres/SQLite terms (Index Scan, Seq Scan, CREATE INDEX)?", "must": ["COLLSCAN"]}
