# Dev/test dependencies for the hub (runtime deps: mcp-server/requirements.txt).
# hub-manager TUI runtime:
textual>=8,<9
# test/lint toolchain:
pytest>=9,<10
ruff>=0.16.5
mcp>=2.1.1
pyyaml>=6.0.3,<7
