# /ldo report — ~/.global-ai-hub/llms-concepts/prompt-caching.llms/ (concept pack: index + facts-style full/small/facts)

Run: 2026-08-31 · flags `--kind index|facts --agent-test --no-sync` · profile: standard (full + facts layers present, no source mirror beside the export, no docset index) · iterations: 3 · exit: **STABLE-REWRITE** (exit condition 4, `convergence_check.py` edit-distance 0.12% / 1.17% on iter 2→3) · blind re-audit gate: not applicable (CLEAN was not reached — residual Medium+ are all BLOCKED rows).

Generator: `llms-concept-abstractor` `concept_abstract.py compile` v1.2.0 (the pack had been compiled with v1.1.0). All fixes were made in the generator inputs (`classified.jsonl`) and the pack was regenerated; no generated file was hand-edited. Regeneration parity after the final write: **0 changed lines** across all seven generated files.

## Per-pass findings

| Pass | Judges | H / M / L | Fixed | Blocked | N/A |
|---|---|---|---|---|---|
| P0 Detect | I6 | 0 / 0 / 0 | – | – | I6 "parses as index" on `llms-small.txt` / `llms-full.txt` — expected: a concept pack's full and small are facts-style catalogues (H1 + blockquote + `## facet` sections of unit lines), not page ladders |
| P1 Structure | I1 I2 I4 I5 N4 | 0 / 0 / 0 | – | – | – |
| P2 Links | N1 N6 N7 P2 | 0 / 0 / 1 | – | – | live: 14/14 URLs 200 (2 index absolute links + 12 sampled unit URLs). Low N7: 23 duplicate targets — every "Related concepts" line links `llms-vocabulary.txt` with no anchor (generator, G2) |
| P3 Descriptions | D1–D6 | 0 / 1 / 1 | – | M: facet descriptions are generic glosses ("what it is — 6 units"), not what the reader finds (D2) → generator constant `FACET_BLURB` (G3) | Low D3: 22/43 descriptions outside the 10–25-word band (counts/ladder lines) |
| P4 Navigation | I2 I3 N2 N3 N4 N7 | 0 / 1 / 1 | M: misfiled units — the only unit that defines prompt caching (s01u002976) and the per-model minimum (s01u009466) sat in the "Facts and statements" catch-all; the automatic-vs-explicit unit (s01u003547) outside Comparisons; two OpenRouter *response*-caching units led Comparisons → 3 facet moves + 2 drops via `classified.jsonl` | – | Low: no per-source view (Sources links to external homepages); blockquote is one 87-word sentence (I2 passes at 2 sentences) |
| P5 Size ladder | S1–S6 H8 | 0 / 0 / 1 | – | – | S4 facts/full ratio 0.95 — expected: a pack's full IS its facts. Manifest consistent (bytes exact). Low S5: token estimator not named in manifest |
| P6 Fidelity | C1–C5 | 0 / 1 / 0 | M: MDX residue `Availability=<PlatformAvailability … />` in the 3 overview-table units that led the Definitions facet → `text_fix` trims (pure prefixes) + moved to Parameters | 2 units keep `<Eligible>Yes</Eligible>` mid-text (s01u007083, s01u007064): trimming would drop the ZDR/HIPAA claim → upstream `docset_refine clean` | – |
| P7 Facts shape | C6 R3 R4 R7 P6 | 1 / 1 / 2 | 9 no-claim units dropped (heading + colon lead-ins, marketing lead-in, "The page lists…" residue, open-brace snippet, response-caching pair) | **H**: 37/186 units are lead-ins whose list/table body was never extracted (21 end in `:`, 16 truncated with `…`) — the payload the questions need (usage fields, per-model minimums, TTL options, what can/cannot be cached). Upstream: `docset_refine` extraction in `docs.claude.com.llms` / `openrouter.ai.llms`. M: 3 units with source URL but no anchor (upstream facts files carry none) | R3 anchor resolution: no mirror — expected. Low: `keywords:` empty tail on s02u002424 (G5); 12 units carry `keywords: Message Batches` without mentioning batches (lexicon aka list, next harvest) |
| P8 Facts truth (20 sampled) | C7 D5 | 0 / 1 / 0 | 17 supported · 3 partial · 0 unsupported · 0 stale. 2 partials (s02u003379, s02u003381: lead-ins asserting nothing) dropped | 1 partial (s01u003539) is an upstream `…` truncation | – |
| P9 Provenance | P1 P3 P4 P5 | 0 / 0 / 0 | – | – | banner present (`generated by llms-concept-abstractor v1.2.0 · rights: extractive`); 0 steering spans; 0 secrets/emails/private paths; `--third-party` lint clean |
| P10 Family | F1–F6 | – | – | – | not a family file |
| P11 Retrieval | R1 R2 R4 | 0 / 1 / 0 | – | M: no `concept__prompt-caching` keyword/vector index exists and building one (`docset_indexer index units.jsonl --units --name concept__prompt-caching`) was out of scope for this run | proxy only: 12/15 exact-token greps hit in `llms-facts.txt` (`cached_tokens`, `0.1x`, `ephemeral_1h` absent — they exist only as lexicon aliases) |
| P12 Agent test | R5 R6 N3 | 0 / 2 / 0 | index High → Medium | index **5 → 7 → 6 / 10** (iter 0/1/2; ≥ 8 needed), facts **6 → 6 → 6 / 10** (≥ 7 needed). Every residual partial (Q5 minimums, Q7 usage fields, Q10 OpenRouter TTLs, Q4/Q6 wording) traces to the upstream lead-in/truncation finding above | – |
| P13 Serving | H2 H3 H4 H7 | 0 / 0 / 0 | – | – | live at `http://127.0.0.1:8788/c/prompt-caching/`: 200, `text/markdown; charset=utf-8`, `X-Markdown-Tokens`, `Link: rel="describedby"` on all five text files; JSON files `application/json` |
| P14 Hygiene | H1 | 0 | – | – | UTF-8, LF, no tabs, no trailing whitespace, single trailing newline, no BOM |
| P15 Parity | H5 H8 | 0 / 1 / 0 | M: pack compiled with v1.1.0 while the generator is v1.2.0 (index "Related concepts" wording differed by 46 lines) → regenerated; parity now 0 | – | – |

Iteration Medium+ counts: 11 → 9 → 8 → 8 (`convergence_check.py`: CONTINUE, STABLE-REWRITE, STABLE-REWRITE). The 8 residuals are all BLOCKED (5 upstream extraction / anchors, 2 generator code, 1 index build out of scope).

## Probes

- Keyword (P11): index not built (constraint) — local grep proxy 12/15 exact tokens present.
- Vector (P11): not available (no collection) — N/A.
- Agent test (P12): index 7/10 (iter 1) and 6/10 (iter 2) on materially identical content — reported as 6–7/10, below the 8/10 bar (Medium); facts 6/10 on all three runs, below the 7/10 bar (Medium). Bank: `~/.claude/skill-consolidation/evals/llms/concept__prompt-caching.eval.jsonl` (10 questions, per-question verdicts and history persisted).
- P8 truth: 17/20 supported, 0 unsupported.
- Serve (P13): 200 on every file.

## What changed in the generator inputs (`classified.jsonl`, +18 rows; kept units 194 → 186)

| Unit | Change | Why |
|---|---|---|
| s01u000156, s01u000157, s01u000158 | `text_fix` (prefix trim of `; Availability=<PlatformAvailability …>`), facet → parameters | MDX residue at the head of the Definitions facet; the rows are overview feature-table entries, type `parameter` |
| s01u002976 | facet → definition | the one unit that defines prompt caching itself (P12 Q1) |
| s01u009466 | facet → measures | per-model minimum cacheable length 512 / 1,024 tokens (P12 Q5) |
| s01u003547 | facet → comparisons | automatic vs explicit: same pricing, minimums, ordering, 20-block lookback (P12 Q9) |
| s02u003379, s02u003381 | keep:false | P8 partial — heading + colon lead-ins asserting nothing (second has multipliers blanked in the source) |
| s01u001689 | keep:false | navigation residue ("The page lists supported features…", legacy Bedrock page) |
| s01u003540, s01u003557, s02u003387 | keep:false | content-free lead-ins ("When you send a request with prompt caching enabled:", "Tailor your strategy to your scenario:", "abstracting away complexities:") |
| s02u004040, s02u004045 | keep:false | OpenRouter *response* caching (whole-response replay) — both auditors read it as off-topic in Comparisons; the contrast survives as the lexicon term `response caching` + vocabulary note |
| s01u003459 | keep:false | snippet fragment `"cache_control": {` (an open brace) |
| s01u001696, s01u009875, s01u003509, s01u003511 | reader-facing `note` | scope labels: Bedrock-legacy-only, OpenAI-SDK-compat-only, deprecated `client.beta.prompt_caching` namespace — as bare units they read as contradictions |
| (iter 1 rows) | audit rationale moved from `note` to an ignored `ldo` key | `note` renders into `llms-full.txt`; iteration 1 had leaked `note: ldo 2026-08-31 …` into the reader-facing file (caught by the iter-2 agent test, fixed in iter 3) |

Dropped-unit register (demotion guard): the 9 `keep:false` rows above are the only facts removed; each is a lead-in/fragment/residue with no standalone claim, or off-concept. No unit with a claim was dropped.

Unchanged inputs: `lexicon.json`, `--summary` (kept verbatim from the manifest), `--budget-tokens 8000`, `pool.jsonl`, `semantic.jsonl`.

## Generator-code findings for the `/lca` maintainer (`concept_abstract.py` v1.2.0; not edited)

| # | Sev | Where | Finding / fix |
|---|---|---|---|
| G1 | High (drives P12) | `_definition_for`, L723–744 (gate L734, rank L737) | 21/25 vocabulary terms are "Named, not yet defined" and `prompt caching`, `automatic caching` and `cache_control` all receive the same "Automatic caching — …" paragraph. Cause: the +2 URL-slug bonus (L737) outranks the +1 "text opens with the term" test, and the `defn` gate (L734) accepts only `facet == "definition"` or `_DEF_CUE`. Fix: make term-first `Term — sentence` units the primary rank (+3), treat `type == "definition"` as definitional regardless of facet, and never assign one unit as the definition of more than one term |
| G2 | Medium | index L982; vocabulary builder L927–945 | "Related concepts" links `llms-vocabulary.txt` 23× with no anchor and the vocabulary emits no per-term anchors. Emit a slug anchor per term (or `### term` headings) and link `llms-vocabulary.txt#<slug>`; better still, add the facet anchors that hold the term's units |
| G3 | Medium | `FACET_BLURB` L88–95, used at L974 | Facet descriptions are fixed glosses (D2 fail). Append the top-3 `matched` terms per facet (Counter over `r["matched"]`) so the description names what the reader finds, e.g. "settings, defaults and limits — 30 units · cache_control, cache diagnostics, cache TTL" |
| G4 | Medium | compile (`_apply_classified` → `kept`) | Lead-in units (text ends with `:` or `…`, < ~120 chars, no digit/code token) are compiled unfiltered — 37/186 here, and every P12 partial. Add a compile-time filter or a `· partial: list body not extracted` tail so retrievers can discount them; ideally fix upstream extraction |
| G5 | Low | L911 `kws = … or [slug]` | a `keywords` list containing `""` yields an empty `keywords:` tail (s02u002424). Filter falsy strings before the `or [slug]` fallback |
| G6 | Low | `_line` L699 / compile | MDX residue (`<PlatformAvailability … />`, `<Eligible>…</Eligible>`) passes through. Reuse the `docset_refine/clean.py` residue table on unit text at compile |
| G7 | Low | `_line`, facts builder L900–915 | cross-source units carry no provider marker in the text; both auditors misread OpenRouter/OpenAI/Bedrock-legacy units as Claude-API statements. Prefix non-primary hosts with `[src: openrouter.ai]` |
| G8 | Low | `_apply_classified` L660–691 | `note` from `classified.jsonl` renders reader-facing in `llms-full.txt`; document it, and honour an ignored audit key (this run used `ldo`) |
| G9 | Low | pack grammar / `llms_lint.py detect` | `llms-full.txt` and `llms-small.txt` of a concept pack detect as `index`; stamp a `<!-- grammar: facts-catalogue -->` comment (or have `llms_lint` read `manifest.json` `kind: concept`) so P0 stops reporting I6 |
| G10 | Low | `_line` snippet branch L701–703 | multi-line snippets render only their first line, producing `curl … \` and `"cache_control": {` lines; render title + first non-brace code line, skip brace-only fragments |

Upstream (not `/lca`): `docset_refine` extraction for `docs.claude.com.llms` and `openrouter.ai.llms` drops list/table bodies after a colon lead-in, truncates at ~400 chars with `…`, leaves `<Eligible>` residue, and emits no anchors for `mid-conversation-effort-example` and `batch-quickstart` units. Lexicon (next harvest): the `Message Batches` aka list (compaction, context editing, thinking blocks, tool search, context management, extended thinking) stamps 12 unrelated units — split into an "interacting features" term or drop those surfaces.

## Snapshot & rollback

Backups: `~/.claude/skill-consolidation/backups/llms-concept-prompt-caching-20260831-013836/` (baseline files + `baseline.sha256`, `.iter1/.iter2/.iter3` copies, `run-stub.jsonl`, `*.final.diff`).
Restore: `cp ~/.claude/skill-consolidation/backups/llms-concept-prompt-caching-20260831-013836/{llms.txt,llms-full.txt,llms-small.txt,llms-facts.txt,llms-vocabulary.txt,units.jsonl,manifest.json,classified.jsonl,concept-graph.json} ~/.global-ai-hub/llms-concepts/prompt-caching.llms/`

Telemetry: 16 rows appended to `~/.claude/skill-consolidation/optimizer-telemetry.jsonl` per the canonical schema. `--no-sync`: no docset index built, no hub registry touched.
