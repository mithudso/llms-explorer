{"q": "What is prompt caching and what does it save?", "must": ["prompt caching"]}
{"q": "What is cache_control and where do you put it in a request?", "must": ["cache_control", "ephemeral"]}
{"q": "How does the cache prefix / breakpoint matching work — what order are tools, system and messages hashed in?", "must": ["prefix"]}
{"q": "What are the cache TTL options (5-minute vs 1-hour) and how does refresh-on-use work?", "must": ["1-hour", "5-minute"]}
{"q": "What is the minimum cacheable prompt length per model?", "must": ["1024"]}
{"q": "How much do cache writes and cache reads cost relative to base input tokens?", "must": ["1.25", "0.1"]}
{"q": "Which usage fields report cache hits and misses (cache_read_input_tokens, cache_creation_input_tokens, cached_tokens)?", "must": ["cache_read_input_tokens", "cache_creation_input_tokens"]}
{"q": "What invalidates the prompt cache (which changes break the prefix)?", "must": ["invalidat"]}
{"q": "How does automatic caching differ from explicit cache breakpoints?", "must": ["automatic"]}
{"q": "How does OpenRouter handle prompt caching across providers (sticky routing, cache_control passthrough, cache TTL options)?", "must": ["OpenRouter"]}
