# Hub MCP server + docset indexer deps (install into ~/.global-ai-hub/.venv)
mcp>=2.2.0
chromadb>=1.5
tiktoken

# scripts/hub-daemon.py — the file-corpus event bus daemon (not the MCP
# server itself, but shares this venv and this is the closest "runtime deps"
# file to it)
fastapi>=0.141.1
uvicorn>=0.53.0
