# Index types — facts

> Source-anchored units about Index types: 565 across 7 sources. Each line ends in the URL and anchor it came from; `keywords:` names the lexicon terms that matched.

<!-- generated by llms-concept-abstractor v1.1.1 · generated 2026-08-31 · 565 units · 7 sources · rights: extractive -->

## Definitions

- [passage] GiST indexes are a versatile type of index that can handle complex data types, such as geometric shapes, full-text search, and network addresses. [They are implemented using a custom data structure optimized for searching large amounts of data1](https://www.postgresql.org/docs/current/gist.html). Here are some key points: — https://thenile.dev/docs/postgres/indexes#gist-index · keywords: B-tree, GiST index, index
- [passage] SP-GiST (Spatial Generalized Search Tree) indexes are a versatile index type offered by PostgreSQL. They are designed for complex, non-rectangular data types and work especially well with geometrical and network-based data. Here are some key points: — https://thenile.dev/docs/postgres/indexes#sp-gist · keywords: B-tree, GiST index, index
- [passage] An expression index stores the result of an expression rather than a raw column value. Use expression indexes when queries frequently filter or sort by a computed value. — https://docs.turso.tech/sql-reference/statements/create-index#expression-indexes · keywords: expression index, index, index build
- [passage] The `where` argument allows you to define [partial indexes](https://www.postgresql.org/docs/current/indexes-partial.html) (also known as filtered indexes). A partial index only includes rows that match a specified condition, which reduces the index size and improves both write performance and query performance for the indexed subset of data. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#configuring-partial-indexes-with-where-configuring-partial-indexes-with-where · keywords: index, index overhead, partial index, where
- [passage] The SP-GiST index is a good choice for many different non-balanced data structures. If the query matches the partitioning rule, it can be very fast. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#space-partitioned-gist-sp-gist-space-partitioned-gist-sp-gist · keywords: GiST index, index
- [passage] Bounds intersection refers to the point where multiple bounds overlap. For example, given the bounds `[ [ 3, Infinity ] ]` and `[ [ -Infinity, 6 ] ]`, the intersection of the bounds results in `[ [ 3, 6 ] ]`. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-multikey/multikey-index-bounds/#bounds-intersection-for-a-multikey-index · keywords: index, multikey index, [ [ 3, Infinity ] ], [ [ -Infinity, 6 ] ], [ [ 3, 6 ] ]

## Structure and components

- [passage] Compound indexes may contain **a single** [hashed index field.](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-hashed/#std-label-index-type-hashed) — https://www.mongodb.com/docs/manual/core/index-compound/#hashed-index-fields · keywords: compound index, hashed index, index, index definition
- [passage] 1. **Plan Your Hierarchy**: Design your tree structure carefully before implementation 2. **Index Usage**: Create GiST indexes for better query performance: ```sql theme={null} CREATE INDEX path_idx ON categories USING GIST (path); ``` 3. **Validation**: Implement checks to maintain data integrity 4. **Path Length**: Keep paths reasonably short for better performance — https://thenile.dev/docs/extensions/ltree#best-practices · keywords: GiST index, index, index build, unused index
- [passage] 1. **What is a GIN Index?** * A GIN index stores a set of `(key, posting list)` pairs. * The posting list contains row IDs where the key occurs. * Multiple posting lists can share the same row ID since an item can have multiple keys. * [Each key value is stored only once, making GIN indexes compact when the same key appears multiple times](https://www.postgresql.org/docs/current/gin-intro.html). 2. **Use Cases for GIN Indexes:** * GIN indexes are ideal for data values with multiple components, like arrays. * [They efficiently handle queries that search for specific component values within composite items](https://www.postgresql.org/docs/current/gin-intro.html). 3. **Example 1: Basic Query Using GIN Index** — https://thenile.dev/docs/postgres/indexes#gin-index · keywords: B-tree, GIN index, index, (key, posting list)
- [passage] A `UNIQUE` index enforces that no two rows contain the same combination of values in the indexed columns. NULL values are considered distinct from each other, so a `UNIQUE` index permits multiple rows with NULL in the indexed columns. — https://docs.turso.tech/sql-reference/statements/create-index#unique-indexes · keywords: index, index build, unique index, UNIQUE
- [passage] Compound indexes can contain different types of sparse indexes. The combination of index types determines how the compound index matches documents. — https://www.mongodb.com/docs/manual/core/index-compound/#sparse-compound-indexes · keywords: compound index, index, sparse index
- [passage] This table summarizes the behavior of a compound index that contains different types of sparse indexes: — https://www.mongodb.com/docs/manual/core/index-compound/#sparse-compound-indexes · keywords: compound index, index, sparse index
- [passage] Only use wildcard indexes when the fields you want to index are unknown or may change. Wildcard indexes don't perform as well as targeted indexes on specific fields. If your collection contains arbitrary field names that prevent targeted indexes, consider remodeling your schema to have consistent field names. To learn more about targeted indexes, see [Create Indexes to Support Your Queries.](https://www.mongodb.com/docs/manual/data-modeling/schema-design-process/create-indexes/#std-label-create-indexes-to-support-queries) — https://www.mongodb.com/docs/manual/core/index-wildcard/#use-cases · keywords: index, index build, wildcard index · also: https://www.mongodb.com/docs/manual/core/indexes/index-types/index-text/create-wildcard-text-index/
- [passage] 1. Create the `contacts` collection:db.contacts.insertMany( [ { name: "Evander Otylia", phone: "202-555-0193", address: [ 55.5, 42.3 ] }, { name: "Georgine Lestaw", phone: "714-555-0107", address: [ -74, 44.74 ] } ] ) The `address` field contains[legacy coordinate pairs.](https://www.mongodb.com/docs/manual/geospatial-queries/#std-label-geospatial-legacy) 2. To query for location data with the `$near` operator, you must create a[geospatial index](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-geospatial/#std-label-geospatial-index) on the field that contains the location data.Create a 2d index on the `address` field:db.contacts.createIndex( { address: "2d" } ) — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2d/query/proximity-flat-surface/#chunk-1 · keywords: geospatial index, index, index build, contacts, address, $near
- [passage] With the compound index on `department` and `description`, the query only examines **one** index key. There is only one document in the collection where `department` is `kitchen` and the `description` contains the string `green`. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-text/limit-number-of-items-scanned-for-text-search/#view-number-of-documents-examined · keywords: compound index, index, index definition, department, description, kitchen
- [passage] - Create a wildcard index that only covers specific fields. For example, if you have multiple embedded documents with multiple subfields, you can create an index to cover queries on both embedded documents and their subfields. - Create a wildcard index that omits specific fields. For example, if you have a collection that contains a field that is never queried, you can omit that field from the index. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/create-wildcard-index-multiple-fields/#chunk-0 · keywords: index, index build, wildcard index
- [passage] | Parameter | Type | Description | |---|---|---| | `keys` | document | A document that contains the field and value pairs where the field is the index key and the value describes the type of index for that field. For an ascending index on a field, specify a value of `1` . For descending index, specify a value of`-1` . An asterisk (`*` ) is not a valid index name. MongoDB supports several different index types, including: See [index types](https://www.mongodb.com/docs/manual/core/indexes/index-types/#std-label-index-types) for more information. [Wildcard indexes](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/#std-label-wildcard-index-core) support workloads where users query against custom fields or a large variety of fields in a collection. You can create a wildcard index on a specific field and its subpaths or on all of the fields in a document. For details, see [Wildcard Indexes.](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/#std-label-wildcard-index-core) | | `options` | document | Optional. A document that contains a set of options that controls the creation of the index. See [Options](https://www.mongodb.com#std-la — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#chunk-1 · keywords: index, index definition, wildcard index, keys,  . For descending index, specify a value of,  . An asterisk (
- [passage] As part of the `JsonbPathOps` the `@>` operator is handled by the index, speeding up queries such as `value @> '{"foo": 2}'`. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#generalized-inverted-index-gin-generalized-inverted-index-gin · keywords: GIN index, index, JsonbPathOps, @>, value @> '{"foo": 2}'
- [definition] Database Manual - (manual) (Part 1 of 9) — > This part of the MongoDB manual covers getting started, administration, and core concepts like clustered collections, change streams, and geospatial and other index types. — https://www.mongodb.com/docs/manual/manual-1-llms.txt#database-manual-manual-part-1-of-9 · keywords: clustered index, index
- [passage] > This part of the MongoDB manual covers getting started, administration, and core concepts like clustered collections, change streams, and geospatial and other index types. — https://www.mongodb.com/docs/manual/manual-1-llms.txt#database-manual-manual-part-1-of-9 · keywords: clustered index, index
- [passage] The GIN index stores composite values, such as arrays or `JsonB` data. This is useful for speeding up querying whether one object is part of another object. It is commonly used for full-text searches. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#generalized-inverted-index-gin-generalized-inverted-index-gin · keywords: GIN index, index, JsonB
- [passage] When you run `prisma db pull` on a database that contains partial indexes, Prisma ORM will: — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#introspection-introspection · keywords: index, partial index, prisma db pull
- [passage] `on conflict do update` can have a `where` clause in two different places - as part of the conflict target (i.e. for partial indexes) or as part of the `update` clause: — https://orm.drizzle.team/docs/cockroach/insert#where-clauses · keywords: index, partial index, on conflict do update, where, update · also: https://orm.drizzle.team/docs/pg/insert, https://orm.drizzle.team/docs/sqlite/insert
- [passage] **1. Structure of B-Tree Indexes:** — https://thenile.dev/docs/postgres/indexes#b-tree-index · keywords: B-tree, index
- [passage] If your application repeatedly runs a query that contains multiple fields, you can create a compound index to improve performance for that query. For example, a grocery store manager often needs to look up inventory items by name and quantity to determine which items are low stock. You can create a compound index on both the `item` and `quantity` fields to improve query performance. — https://www.mongodb.com/docs/manual/core/index-compound/#chunk-1 · keywords: compound index, index, item, quantity
- [passage] A single compound index can contain up to 32 fields. — https://www.mongodb.com/docs/manual/core/index-compound/#field-limit · keywords: compound index, index
- [passage] The unique index allows the insertion of a document without the `email` field if the collection does not already contain a document missing the `email` field: — https://www.mongodb.com/docs/manual/core/index-unique/#missing-document-field-in-a-unique-single-field-index · keywords: index, unique index, email
- [passage] When you create a geospatial index on a field that contains [legacy coordinate pairs](https://www.mongodb.com/docs/manual/reference/glossary/#std-term-legacy-coordinate-pairs), MongoDB computes [geohash](https://www.mongodb.com/docs/manual/reference/glossary/#std-term-geohash) values for the coordinate pairs within the specified [location range](https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2d/create/define-location-range/#std-label-2d-index-define-location-range), then indexes the geohash values. — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2d/internals/#geohash-values · keywords: geospatial index, index
- [passage] Because `test_scores` contains an array value, MongoDB stores this index as a multikey index. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-multikey/create-multikey-index-basic/#chunk-1 · keywords: index, multikey index, test_scores
- [passage] This index supports single-field queries on any field in the collection. If a document contains an embedded document or array, the wildcard index traverses the document or array and stores the value for all fields in the document or array. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/create-wildcard-index-all-fields/#results · keywords: index, wildcard index
- [passage] The following operation creates a wildcard index that contains all scalar values (meaning strings and numbers) of the `attributes.size` and `attributes.color` fields: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/create-wildcard-index-multiple-fields/#chunk-3 · keywords: index, wildcard index, attributes.size, attributes.color
- [passage] MongoDB can enforce a uniqueness constraint on a ranged shard key index. Using a unique index on the shard key enforces uniqueness on the entire key combination and not individual components of the shard key. — https://www.mongodb.com/docs/manual/core/sharding-shard-key-indexes/#unique-indexes · keywords: index, unique index

## How it works

- [passage] | Parameter | Type | Description | |---|---|---| | `unique` | boolean | Optional. Creates a unique index so that the collection will not accept insertion or update of documents where the index key value matches an existing value in the index. Specify `true` to create a unique index. The default value is`false` . The option is *unavailable* for[hashed indexes.](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-hashed/#std-label-index-hashed-index) | | `name` | string | Optional. The name of the index. If unspecified, MongoDB generates an index name by concatenating the names of the indexed fields and the sort order. | | `partialFilterExpression` | document | Optional. If specified, the index only references documents that match the filter expression. See [Partial Indexes](https://www.mongodb.com/docs/manual/core/index-partial/) for more information. A filter expression can include: equality expressions (i.e. `field: value` or using the[`$eq`](https://www.mongodb.com/docs/manual/reference/operator/query/eq/#mongodb-query-op.-eq) operator) [`$exists: true`](https://www.mongodb.com/docs/manual/reference/operator/query/exists/#mongodb-query-op.-exists) expression [` — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#chunk-5 · keywords: hashed index, index, index definition, partial index, unique index, unique
- [passage] The fields `"ratings.scores.q1"` and `"ratings.scores.q2"` share the field path `"ratings.scores"`. In order to compound index bounds, a query must use `$elemMatch` on the common field path. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-multikey/multikey-index-bounds/#create-a-compound-multikey-index · keywords: compound index, index, index definition, multikey index, "ratings.scores.q1", "ratings.scores.q2"
- [passage] Because the constraint applies to separate documents, for a unique [multikey](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-multikey/#std-label-index-type-multikey) index, a document may have array elements that result in repeating index key values as long as the index key values for that document do not duplicate those of another document. In this case, the repeated index entry is inserted into the index only once. — https://www.mongodb.com/docs/manual/core/index-unique/#unique-constraint-across-separate-documents · keywords: index, index definition, multikey index, unique constraint
- [passage] 1. Use hstore when dealing with dynamic attributes that don't require strict schema validation. 2. Consider using JSON/JSONB instead if you need to store nested structures or arrays. 3. Create indexes on frequently queried keys using GiST or GIN indexes: — https://thenile.dev/docs/extensions/hstore#best-practices · keywords: GIN index, GiST index, index, index build
- [passage] 1. **Infrastructure**: SP-GiST indexes support various kinds of searches, similar to GiST indexes. [They permit the implementation of a wide range of different non-balanced disk-based data structures, such as quadtrees, k-d trees, and radix trees (tries) 1](https://www.postgresql.org/docs/current/spgist.html). 2. **Use Cases**: * **Geometric Searches**: SP-GiST is ideal for spatial data, such as points, lines, and polygons. * **IP Network Searches**: When dealing with IP addresses or network ranges. * [**Text Search with Complex Pattern Matching**: For scenarios where you need to search for patterns within text data](https://www.postgresql.org/docs/current/spgist.html) [2](https://roadmap.sh/postgresql-dba/sql-optimization-techniques/indexes-usecases/sp-gist). 3. **Performance Considerations**: * SP-GiST indexes are most useful for data that has a natural clustering element and is not an equally balanced tree. * [They work well with data types that don't fit neatly into rectangular shapes, like GIS (geospatial), multimedia, phone routing, and IP routing data 3](https://www.postgresqltutorial.com/postgresql-indexes/postgresql-index-types/). — https://thenile.dev/docs/postgres/indexes#sp-gist · keywords: B-tree, GiST index, index
- [passage] Remember that BRIN indexes are most effective when dealing with large tables and specific column types. — https://thenile.dev/docs/postgres/indexes#output · keywords: B-tree, BRIN index, index
- [passage] A composite index is useful when queries filter or sort by multiple columns. The order of columns matters -- an index on `(a, b)` can accelerate queries filtering on `a` alone, but not queries filtering only on `b`. — https://docs.turso.tech/sql-reference/statements/create-index#column-indexes · keywords: compound index, index, index build, (a, b),  alone, but not queries filtering only on 
- [passage] The [btree\_gin](https://www.postgresql.org/docs/current/btree-gin.html) extension in PostgreSQL enables GIN indexes to support B-tree indexable data types. It is useful when you want to use a GIN index for multi-column queries that include standard B-tree searchable data types like `int`, `text`, `timestamp`, and `uuid`. Your Nile database arrives with `btree_gin` extension already enabled, so there's no need to run `create extension`. — https://thenile.dev/docs/extensions/btree_gin · keywords: B-tree, GIN index, index, int, text, timestamp
- [passage] A GIN index with `btree_gin` is useful when performing multi-column searches that include B-tree indexable columns. Here's how to create one: — https://thenile.dev/docs/extensions/btree_gin#creating-a-gin-index-with-btree-gin · keywords: B-tree, GIN index, index, btree_gin
- [passage] - To utilize an index on an embedded document, your query must specify the entire embedded document. This can lead to unexpected behaviors if your schema model changes and you add or remove fields from your indexed document. - When you query embedded documents, the order that you specify fields in the query matters. The embedded documents in your query and returned document must match exactly. To see examples of queries on embedded documents, see [Query on Embedded/Nested Documents.](https://www.mongodb.com/docs/manual/tutorial/query-embedded-documents/#std-label-read-operations-subdocuments) - Before you create an index on an embedded document, consider if you should instead index specific fields in that document, or use a [wildcard index](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/#std-label-wildcard-index-core) to index all of the document's subfields. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-single/create-embedded-object-index/#about-this-task · keywords: index, index build, wildcard index
- [passage] - To use the `wildcardProjection` option, your index key must be`$**` . - Wildcard indexes don't support mixing inclusion and exclusion statements in the `wildcardProjection` document except when explicitly including the`_id` field. For example: - The following `wildcardProjection` document is**invalid** because it specifies both an inclusion and an exclusion of a field:{ "wildcardProjection" : { "attributes" : 0, "users" : 1 } } - The following `wildcardProjection` document is**valid** because even though it specifies both inclusion and exclusion, it includes the`_id` field:{ "wildcardProjection" : { "attributes" : 0, "_id" : 1 } } — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/create-wildcard-index-multiple-fields/#chunk-1 · keywords: index, index definition, wildcard index, wildcardProjection, $**, _id
- [passage] Wildcard indexes do not support mixing inclusion and exclusion statements in the `wildcardProjection` document *except* when explicitly including the `_id` field. For more information on `wildcardProjection`, see the [parameter documentation](https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/#std-label-createIndexes-method-wildcard-option). — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#chunk-32 · keywords: index, index build, wildcard index, wildcardProjection, _id · also: https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/
- [passage] * B-trees are versatile and widely applicable: * **Equality and Range Queries**: They excel in handling equality and range queries. Common operators include `=`, `<`, `>`, `BETWEEN`, and `IN`. * **NULL Conditions**: B-trees can handle `IS NULL` or `IS NOT NULL` conditions. * **Pattern Matching**: When anchored to the beginning of a string, they efficiently support pattern matching using `LIKE` or `~`. — https://thenile.dev/docs/postgres/indexes#b-tree-index · keywords: B-tree, index, , , , and , . * **NULL Conditions**: B-trees can handle ,  or 
- [passage] Previous versions of Prisma ORM used to create a *unique index* on these two columns. In Prisma v6, this unique index is changing to a *primary key* in order to [simplify for the default replica identity behaviour](https://github.com/prisma/orm/issues/25196). — https://www.prisma.io/docs/guides/upgrade-prisma-orm/v6#schema-change-for-implicit-m-n-relations-on-postgresql-schema-change-for-implicit-m-n-relations-on-postgresql · keywords: index, unique index
- [passage] In MySQL, the default collation setting for string comparison is case-insensitive, which means that when performing operations like searching or comparing strings in SQL queries, the case of the characters does not affect the results. However, because collation settings can vary and may be configured to be case-sensitive, we will explicitly ensure that the `email` is unique regardless of case by creating a unique index on the lowercased `email` column. — https://orm.drizzle.team/docs/guides/unique-case-insensitive-email#mysql · keywords: index, unique index, email
- [passage] - Indexes on individual foreign key columns (`userId`, `groupId` in `usersToGroups`) help when you are querying from one side to find the other (e.g., "find groups for a user"). - The composite index on `(userId, groupId)` in `usersToGroups` is particularly important for quickly finding all relationships defined in the junction table. This is used when Drizzle ORM resolves the `many-to-many` relation to fetch related entities. </Callout> — https://orm.drizzle.team/docs/mysql/relations#many-to-many-relationships · keywords: compound index, index, userId, groupId, usersToGroups, (userId, groupId) · also: https://orm.drizzle.team/docs/pg/relations, https://orm.drizzle.team/docs/singlestore/relations-v2, https://orm.drizzle.team/docs/sqlite/relations
- [passage] * Indexes that reference the column (including expression indexes) * Triggers that reference the column * CHECK constraints that reference the column * Foreign key constraint definitions — https://docs.turso.tech/sql-reference/statements/alter-table#rename-column · keywords: expression index, index
- [passage] - The values in the field you query with the `$geoWithin` operator must be in GeoJSON format. - When you specify longitude and latitude coordinates, list the **longitude** first, and then**latitude** . - Valid longitude values are between `-180` and`180` , both inclusive. - Valid latitude values are between `-90` and`90` , both inclusive. - When you specify Polygon `coordinates` , the first and last coordinates in the array must be the same. This closes the bounds of the polygon. - `$geoWithin` does not require a geospatial index. However, a geospatial index improves query performance. Only the[2dsphere](https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2dsphere/#std-label-2dsphere-index) geospatial index supports`$geoWithin` . For more information see[Create a 2dsphere Index.](https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2dsphere/create/#std-label-create-2dsphere-index) — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2dsphere/query/geojson-bound-by-polygon/#chunk-1 · keywords: geospatial index, index, $geoWithin, -180, 180, -90
- [passage] - When you specify longitude and latitude coordinates, list the **longitude** first, and then**latitude** . - Valid longitude values are between `-180` and`180` , both inclusive. - Valid latitude values are between `-90` and`90` , both inclusive. - A location intersects with an object if it shares at least one point with the specified object. This includes objects that have a shared edge. - `$geoIntersects` does not require a geospatial index. However, a geospatial index improves query performance. Only the[2dsphere](https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2dsphere/#std-label-2dsphere-index) geospatial index supports`$geoIntersects` . For more information see[Create a 2dsphere Index.](https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2dsphere/create/#std-label-create-2dsphere-index) — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2dsphere/query/intersections-of-geojson-objects/#chunk-1 · keywords: geospatial index, index, -180, 180, -90, 90
- [passage] Compound bounds combine bounds for multiple keys of a [compound index](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-compound/#std-label-index-type-compound). Using bounds from multiple keys reduces the time it takes to process a query because MongoDB does not need to compute results for each bound individually. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-multikey/multikey-index-bounds/#chunk-3 · keywords: compound index, index
- [passage] When you create a wildcard index, you can specify fields to include or exclude in the index. This lets you: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/create-wildcard-index-multiple-fields/#chunk-0 · keywords: index, wildcard index
- [passage] If a field is an embedded document or array (like `attributes.size`), the wildcard index recurses into the field and indexes all embedded scalar field values. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/create-wildcard-index-multiple-fields/#results · keywords: index, wildcard index, attributes.size
- [passage] Unfortunately, it is expensive to create a lot of individual indexes to cover all of the possible queries. A wildcard index is a good alternative to creating a large number of individual indexes because one wildcard index can efficiently cover many potential queries. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/index-wildcard-compound/#search-using-the-attribute-pattern · keywords: index, wildcard index
- [passage] Wildcard indexes have specific behavior when indexing embedded object and array fields: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/reference/embedded-object-behavior/#chunk-0 · keywords: index, wildcard index
- [passage] - If the field is an object, the wildcard index descends into the object and indexes its contents. The wildcard index continues descending into any additional embedded documents it encounters. - If the field is an array, the wildcard index traverses the array and indexes each element: - If the element is an object, the wildcard index descends into the object to index its contents. - If the element is an array (that is, an array which is embedded directly within the parent array), the wildcard index does not traverse the embedded array, but indexes the *entire* array as a single value. - For all other fields, the index stores the **primitive value** . A primitive value is a non-object, non-array value. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/reference/embedded-object-behavior/#chunk-0 · keywords: index, wildcard index
- [passage] When a wildcard index encounters an embedded object, it descends into the object and indexes its contents. For example, consider this document: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/reference/embedded-object-behavior/#wildcard-indexes-on-embedded-objects · keywords: index, wildcard index
- [passage] When a wildcard index encounters an array, it traverses the array to index its elements. If the array element is itself an array (an embedded array), the index records the *entire* embedded array as a value instead of traversing its contents. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/reference/embedded-object-behavior/#wildcard-indexes-on-arrays · keywords: index, wildcard index
- [passage] The index records for `ship.coordinates` and `ship.captains` do not include the array position for each element. Wildcard indexes ignore array element positions when recording the element into the index. However, wildcard indexes can still support queries that include explicit array indices. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/reference/embedded-object-behavior/#chunk-3 · keywords: index, wildcard index, ship.coordinates, ship.captains
- [passage] If a field is a nested document or array, the wildcard index recurses into it and indexes all scalar fields in the document or array. — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#include-specific-fields-in-wildcard-index-coverage · keywords: index, wildcard index
- [passage] If a field is a nested document or array, the wildcard index recurses into the document or array and indexes all scalar fields in the document or array. — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#chunk-33 · keywords: index, wildcard index
- [passage] * All fields that make up the unique constraint **must** be mandatory fields. The following model is **not** valid because `id` could be `null`: — https://www.prisma.io/docs/orm/v7/reference/prisma-schema-reference#general-general-2 · keywords: @@index, unique constraint, id, null
- [passage] The [citext](https://www.postgresql.org/docs/current/citext.html) extension in PostgreSQL provides a case-insensitive text type. It behaves just like the standard `TEXT` data type but treats values as case-insensitive when comparing or indexing, making it useful for case-insensitive searches and unique constraints. Your Nile database arrives with `citext` extension already enabled, so there's no need to run `create extension`. — https://thenile.dev/docs/extensions/citext · keywords: index, unique constraint, TEXT, citext, create extension
- [passage] * Allows case-insensitive text comparisons without using `LOWER()`. * Simplifies case-insensitive unique constraints and indexes. * Reduces errors when working with user-provided text data like emails or usernames. — https://thenile.dev/docs/extensions/citext#benefits-of-using-citext · keywords: index, unique constraint, LOWER()

## Parameters and configuration

- [passage] nual/core/indexes/index-types/#std-label-index-types) | | `sparse` | boolean | Optional. If `true` , the index only references documents with the specified field. These indexes use less space but behave differently in some situations (particularly sorts). The default value is`false` . See[Sparse Indexes](https://www.mongodb.com/docs/manual/core/index-sparse/) for more information. The following index types are sparse by default and ignore this option: For a compound index that includes `2dsphere` index keys and keys for other types, only the`2dsphere` index fields determine whether the index references a document. [Partial indexes](https://www.mongodb.com/docs/manual/core/index-partial/#std-label-index-type-partial) have a superset of the sparse index functionality. Unless your application has a specific requirement, use partial indexes instead of sparse indexes. | | `expireAfterSeconds` | integer | Optional. Specifies a value, in seconds, as a time to live ([TTL](https://www.mongodb.com/docs/manual/reference/glossary/#std-term-TTL) ) to control how long MongoDB retains documents in this collection. This option only applies to[TTL](https://www.mongodb.com/docs/manual/reference/gl — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#chunk-7 · keywords: TTL index, compound index, geospatial index, index, index definition, partial index
- [passage] <Info> Note: `USING gin` specifies that this is a GIN index. The extension allows `column1` (text), `column2` (integer), and `column3` (timestamp) to be indexed efficiently using GIN. </Info> — https://thenile.dev/docs/extensions/btree_gin#creating-a-gin-index-with-btree-gin · keywords: B-tree, GIN index, index, USING gin, column1, column2
- [passage] <Info> Note: `USING gist` specifies that this is a GiST index. The extension allows `column1` (text), `column2` (integer), and `column3` (timestamp) to be indexed efficiently using GiST. </Info> — https://thenile.dev/docs/extensions/btree_gist#creating-a-gist-index-with-btree-gist · keywords: B-tree, GiST index, index, USING gist, column1, column2

## How-to and procedures

- [passage] You can also create basic indexes with the same key pattern with and without the sparse option: — https://www.mongodb.com/docs/manual/core/index-unique/#basic-and-sparse-index-creation · keywords: index, index build, index definition, sparse index
- [passage] You can create multiple indexes on the same key(s) with different collations. To create indexes with the same key pattern but different collations, you must supply unique index names. — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#tip · keywords: index, index build, index definition, unique index
- [passage] You can also enforce a unique constraint on the combination of index key values for a [compound index.](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-compound/#std-label-index-type-compound) — https://www.mongodb.com/docs/manual/core/index-unique/#chunk-1 · keywords: compound index, index, index definition, unique constraint
- [passage] To create a `hidden` index, use the [`db.collection.createIndex()`](https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#mongodb-method-db.collection.createIndex) method with the [hidden](https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#std-label-method-createIndex-hidden) option set to `true`. — https://www.mongodb.com/docs/manual/core/index-hidden/#create-a-hidden-index · keywords: hidden index, index, index build, hidden, db.collection.createIndex(), true
- [passage] Create a compound index on the `ratings.scores.q1` and the `ratings.scores.q2` fields: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-multikey/multikey-index-bounds/#create-a-compound-multikey-index · keywords: compound index, index, multikey index, ratings.scores.q1, ratings.scores.q2
- [passage] use inventory db.products_catalog.createIndexes( [ { "product_attributes.$**" : 1 } ] ) — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/#create-a-wildcard-index-on-a-single-field-path · keywords: index, index build, wildcard index
- [passage] use inventory db.products_catalog.createIndexes( [ { "$**" : 1 } ] ) — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/#create-a-wildcard-index-on-all-field-paths · keywords: index, index build, wildcard index
- [actionable] Create a wildcard index on all fields excluding `_id` by specifying the `$**` field in the index definition. — https://www.mongodb.com/docs/manual/manual-1-llms.txt · keywords: index, index definition, wildcard index, _id, $** · also: https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/create-wildcard-index-all-fields/
- [passage] CREATE INDEX "idx_content_search" ON "test" USING gin ("content_search"); ``` </CodeTab> </CodeTabs> — https://orm.drizzle.team/docs/cockroach/generated-columns#database-side · keywords: GIN index, index, index build · also: https://orm.drizzle.team/docs/pg/generated-columns
- [passage] CREATE INDEX [name_idx] ON [user] ([name]); CREATE UNIQUE INDEX [email_idx] ON [user] ([email]); ``` </Section> — https://orm.drizzle.team/docs/mssql/indexes-constraints#indexes · keywords: index, index build, unique index · also: https://orm.drizzle.team/docs/mysql/indexes-constraints, https://orm.drizzle.team/docs/singlestore/indexes-constraints, https://orm.drizzle.team/docs/sqlite/indexes-constraints
- [passage] To create a unique index, use the [`db.collection.createIndex()`](https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#mongodb-method-db.collection.createIndex) method with the `unique` option set to `true`. — https://www.mongodb.com/docs/manual/core/index-unique/#create-a-unique-index · keywords: index, index build, unique index, db.collection.createIndex(), unique, true · also: https://www.mongodb.com/docs/manual/core/index-unique/create/
- [passage] To create a wildcard index, use the wildcard specifier (`$**`) as the index key: — https://www.mongodb.com/docs/manual/core/index-wildcard/#chunk-0 · keywords: index, index definition, wildcard index, $**
- [passage] To learn how to create a collection with a clustered index, see [Date Clustered Index Key Example.](https://www.mongodb.com/docs/manual/core/clustered-collections/#std-label-clustered-collections-index-example) — https://www.mongodb.com/docs/manual/core/indexes/index-types/#chunk-3 · keywords: clustered index, index, index definition
- [passage] To override the default version and specify a different version for your 2dsphere index, set the `2dsphereIndexVersion` option when you create an index: — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2dsphere/2dsphere-index-versions/#important · keywords: geospatial index, index, index build, 2dsphereIndexVersion
- [passage] You can create an index on a field containing an array value to improve performance for queries on that field. When you create an index on a field containing an array value, MongoDB stores that index as a multikey index. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-multikey/create-multikey-index-basic/#chunk-0 · keywords: index, index build, multikey index
- [passage] To create wildcard indexes, use a standard index creation command: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/index-wildcard-compound/#behavior · keywords: index, index build, wildcard index · also: https://www.mongodb.com/docs/manual/core/index-wildcard/
- [actionable] Optimize query performance and index size for wildcard indexes by using the `wildcardProjection` option to include or exclude specific fields. — https://www.mongodb.com/docs/manual/manual-1-llms.txt · keywords: index, index overhead, wildcard index, wildcardProjection
- [passage] To drop a `unique constraint`, the same syntax is used as dropping an index: `ALTER TABLE users DROP INDEX uq_email;`. There's no separate `DROP CONSTRAINT statement` for it in MySQL. — https://orm.drizzle.team/docs/mysql/indexes-constraints#unique · keywords: index, index build, unique constraint, ALTER TABLE users DROP INDEX uq_email;, DROP CONSTRAINT statement
- [passage] To avoid this, use a partial filter expression so the index only includes documents that contain both fields. For more information, see [Partial Index with Unique Constraint](https://www.mongodb.com/docs/manual/core/index-partial/#std-label-partial-index-with-unique-constraints). Recreate the index using the following options: — https://www.mongodb.com/docs/manual/tutorial/unique-indexes-schema-validation/#chunk-3 · keywords: index, partial index, unique constraint
- [passage] You can now query efficiently using operators like `@>`, and PostgreSQL will leverage this index. — https://thenile.dev/docs/postgres/datatype/json#gin-index-generalized-inverted-index · keywords: GIN index, index, @>
- [passage] Run `collMod` on the `type` field index and set `prepareUnique` to `true`: — https://www.mongodb.com/docs/manual/core/index-unique/convert-to-unique/#prepare-the-index-to-be-converted-to-a-unique-index · keywords: index, unique index, collMod, type, prepareUnique, true
- [passage] CREATE TABLE "composite_example" ( "id" int4, "name" string, CONSTRAINT "composite_example_id_name_unique" UNIQUE("id","name"), CONSTRAINT "custom_name" UNIQUE("id","name") ); ``` </Section> — https://orm.drizzle.team/docs/cockroach/indexes-constraints#unique · keywords: index, unique constraint · also: https://orm.drizzle.team/docs/mssql/indexes-constraints, https://orm.drizzle.team/docs/mysql/indexes-constraints, https://orm.drizzle.team/docs/pg/indexes-constraints, https://orm.drizzle.team/docs/singlestore/indexes-constraints
- [passage] CREATE TABLE [table] ( [id] int, CONSTRAINT [custom_name_1] UNIQUE([id]) ); — https://orm.drizzle.team/docs/mssql/indexes-constraints#unique · keywords: index, unique constraint · also: https://orm.drizzle.team/docs/cockroach/indexes-constraints, https://orm.drizzle.team/docs/mysql/indexes-constraints, https://orm.drizzle.team/docs/pg/indexes-constraints, https://orm.drizzle.team/docs/singlestore/indexes-constraints
- [idea] Define indexes including single field, compound, unique indexes, and alternate keys to optimize query performance. — https://www.mongodb.com/docs/entity-framework/current/llms.txt · keywords: index, unique index
- [passage] To use partial indexes, add the `partialIndexes` feature flag to the `generator` block of your `schema.prisma` file: — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#enabling-the-partialindexes-preview-feature-enabling-the-partialindexes-preview-feature · keywords: index, partial index, partialIndexes, generator, schema.prisma
- [passage] You can define a partial index with a raw SQL predicate string using the `raw()` function. This approach supports any valid SQL `WHERE` expression that your database accepts: — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#raw-sql-syntax-with-raw-raw-sql-syntax-with-raw · keywords: index, partial index, raw(), WHERE
- [passage] You can also define partial indexes using an object literal syntax, which provides type-safety by validating field names and value types against your Prisma schema: — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#object-literal-syntax-type-safe-alternative-object-literal-syntax-type-safe-alternative · keywords: index, partial index
- [passage] To implement a unique and case-insensitive `email` handling in PostgreSQL with Drizzle, you can create a unique index on the lowercased `email` column. This way, you can ensure that the `email` is unique regardless of the case. — https://orm.drizzle.team/docs/guides/unique-case-insensitive-email#postgresql · keywords: index, unique index, email
- [passage] To implement a unique and case-insensitive `email` handling in SQLite with Drizzle, you can create a unique index on the lowercased `email` column. This way, you can ensure that the `email` is unique regardless of the case. — https://orm.drizzle.team/docs/guides/unique-case-insensitive-email#sqlite · keywords: index, unique index, email
- [passage] To implement an upsert query in MySQL with Drizzle you can use `.onDuplicateKeyUpdate()` method. MySQL will automatically determine the conflict target based on the primary key and unique indexes, and will update the row if any unique index conflicts. — https://orm.drizzle.team/docs/guides/upsert#mysql · keywords: index, unique index, .onDuplicateKeyUpdate()
- [passage] Function form of dot notation — `struct_extract(col, 'field')` is equivalent to `col.field`. Primarily useful in expression indexes, where dot notation cannot be used: — https://docs.turso.tech/sql-reference/statements/create-type#struct-extract · keywords: expression index, index, struct_extract(col, 'field'), col.field
- [passage] To create a compound index, use the following prototype: — https://www.mongodb.com/docs/manual/core/index-compound/#chunk-0 · keywords: compound index, index
- [passage] You can [create and manage compound indexes in the UI](https://www.mongodb.com/docs/atlas/atlas-ui/indexes/) for deployments hosted in [MongoDB Atlas.](https://www.mongodb.com/docs/atlas) — https://www.mongodb.com/docs/manual/core/index-compound/#chunk-0 · keywords: compound index, index · also: https://www.mongodb.com/docs/manual/core/index-unique/
- [passage] To create a compound index, see [Create a Compound Index.](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-compound/create-compound-index/#std-label-index-create-compound) — https://www.mongodb.com/docs/manual/core/index-compound/#get-started · keywords: compound index, index
- [passage] To hide an existing index, you can use the [`collMod`](https://www.mongodb.com/docs/manual/reference/command/collMod/#mongodb-dbcommand-dbcmd.collMod) command or [`mongosh`](https://www.mongodb.com/docs/mongodb-shell/#mongodb-binary-bin.mongosh) helper `db.collection.hideIndex()`. — https://www.mongodb.com/docs/manual/core/index-hidden/#chunk-5 · keywords: hidden index, index, collMod, mongosh, db.collection.hideIndex()
- [passage] To unhide a hidden index, you can use the [`collMod`](https://www.mongodb.com/docs/manual/reference/command/collMod/#mongodb-dbcommand-dbcmd.collMod) command or [`mongosh`](https://www.mongodb.com/docs/mongodb-shell/#mongodb-binary-bin.mongosh) helper [`db.collection.unhideIndex()`](https://www.mongodb.com/docs/manual/reference/method/db.collection.unhideIndex/#mongodb-method-db.collection.unhideIndex). You can specify either: — https://www.mongodb.com/docs/manual/core/index-hidden/#unhide-an-existing-index · keywords: hidden index, index, collMod, mongosh, db.collection.unhideIndex()
- [passage] Create a unique compound [multikey](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-multikey/#std-label-index-type-multikey) index on `email` and `name`: — https://www.mongodb.com/docs/manual/core/index-unique/#chunk-1 · keywords: index, multikey index, email, name
- [passage] To convert a non-unique index to a [unique index](https://www.mongodb.com/docs/manual/core/index-unique/#std-label-index-type-unique), use the [`collMod`](https://www.mongodb.com/docs/manual/reference/command/collMod/#mongodb-dbcommand-dbcmd.collMod) command. The `collMod` command provides options to verify that your indexed field contains unique values before you complete the conversion. — https://www.mongodb.com/docs/manual/core/index-unique/convert-to-unique/#chunk-0 · keywords: index, unique index, collMod
- [passage] To create a unique index on the `user_id` field of the `members` collection, run the following command in `mongosh`: — https://www.mongodb.com/docs/manual/core/index-unique/create/#steps · keywords: index, unique index, user_id, members, mongosh
- [passage] Consider using a wildcard index in the following scenarios: — https://www.mongodb.com/docs/manual/core/index-wildcard/#use-cases · keywords: index, wildcard index
- [passage] You can perform the following tasks with wildcard indexes: — https://www.mongodb.com/docs/manual/core/index-wildcard/#get-started · keywords: index, wildcard index
- [passage] Consider the following wildcard index on the `employees` collection: — https://www.mongodb.com/docs/manual/core/index-wildcard/#chunk-3 · keywords: index, wildcard index, employees
- [passage] To learn more about wildcard indexes, see: — https://www.mongodb.com/docs/manual/core/index-wildcard/#learn-more · keywords: index, wildcard index · also: https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/create-wildcard-index-all-fields/
- [passage] You can define the range of coordinates included in a [2d index](https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2d/#std-label-2d-index). By default, 2d indexes have longitude and latitude boundaries of: — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2d/create/define-location-range/#chunk-0 · keywords: geospatial index, index
- [passage] To change the location range of a 2d index, specify the `min` and `max` options when you create the index: — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2d/create/define-location-range/#chunk-0 · keywords: geospatial index, index, min, max
- [passage] Create a 2d index on the `address` field. Specify the following location bounds: — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2d/create/define-location-range/#procedure · keywords: geospatial index, index, address
- [passage] You can use the 2d index to perform calculations on location data, such as [proximity queries.](https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2d/query/proximity-flat-surface/#std-label-2d-index-proximity-query) — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2d/create/define-location-range/#next-steps · keywords: geospatial index, index
- [passage] To index all of the coordinate pairs in the `locs` array, create a 2d index on the `locs` field: — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2d/internals/#chunk-2 · keywords: geospatial index, index, locs
- [passage] To index all of the `loc` values in the `addresses` array, create a 2d index on the `addresses.loc` field: — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2d/internals/#embedded-multi-location-documents · keywords: geospatial index, index, loc, addresses, addresses.loc
- [passage] Create a [compound multikey index](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-compound/#std-label-index-type-compound) on the `item` and `ratings` fields: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-multikey/multikey-index-bounds/#chunk-4 · keywords: index, multikey index, item, ratings
- [passage] Create a compound index on the `inventory` collection that contains the following fields: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-text/limit-number-of-items-scanned-for-text-search/#procedure · keywords: compound index, index, inventory
- [passage] You can create a wildcard index that supports queries on all possible document fields. Wildcard indexes support queries on arbitrary or unknown field names. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/create-wildcard-index-all-fields/#chunk-0 · keywords: index, wildcard index
- [passage] To learn how to create a wildcard index that projects specific fields to cover, see the following pages: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/create-wildcard-index-all-fields/#learn-more · keywords: index, wildcard index
- [passage] To include or exclude fields in a wildcard index, specify the chosen fields in the `wildcardProjection` option: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/create-wildcard-index-multiple-fields/#chunk-0 · keywords: index, wildcard index, wildcardProjection
- [passage] To learn how to use wildcard projection with a compound wildcard index to filter fields, see [Filter Fields with a `wildcardProjection`.](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/index-wildcard-compound/#std-label-wc-compound-index-wcProject) — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/create-wildcard-index-multiple-fields/#chunk-5 · keywords: index, wildcard index, wildcardProjection
- [passage] To learn more about behaviors and use cases for wildcard indexes, see: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/create-wildcard-index-multiple-fields/#chunk-5 · keywords: index, wildcard index
- [passage] Use a compound wildcard index instead. The compound wildcard index is easier to write, easier to maintain, and is unlikely to reach the 64 index collection limit. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/index-wildcard-compound/#chunk-1 · keywords: index, wildcard index
- [passage] To create a [partial](https://www.mongodb.com/docs/manual/core/index-partial/#std-label-index-type-partial) compound wildcard index, you can use the `partialFilterExpression` option to specify a filter expression so that the index only includes documents that match the filter condition. `partialFilterExpression` can cover fields included or not included in the index. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/index-wildcard-compound/#create-a-partial-compound-wildcard-index · keywords: index, wildcard index, partialFilterExpression
- [passage] Create a wildcard index that includes the `ship` field: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/reference/embedded-object-behavior/#chunk-3 · keywords: index, wildcard index, ship
- [passage] Consider the following wildcard index on a `books` collection: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/reference/wildcard-projection-signature/#example · keywords: index, wildcard index, books
- [passage] To support the query, add a [compound index](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-compound/#std-label-index-type-compound). With compound indexes, the order of the fields matter. — https://www.mongodb.com/docs/manual/tutorial/analyze-query-plan/#chunk-15 · keywords: compound index, index
- [passage] To create [unique indexes](https://www.mongodb.com/docs/manual/core/index-unique/#std-label-index-type-unique) using the following procedure, you must stop all writes to the collection during this procedure. — https://www.mongodb.com/docs/manual/tutorial/build-indexes-on-replica-sets/#unique-indexes · keywords: index, unique index
- [passage] To improve query performance, you can create a [compound index](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-compound/#std-label-index-type-compound) that narrows the documents that queries read. For example, if you want to improve performance for queries on `status` and `product_type`, you could create a compound index on those two fields. — https://www.mongodb.com/docs/manual/tutorial/create-queries-that-ensure-selectivity/#chunk-4 · keywords: compound index, index, status, product_type
- [passage] To design your database so that it confines its documents to the application’s rules, combine a unique index and schema validation on your database using the following procedure. — https://www.mongodb.com/docs/manual/tutorial/unique-indexes-schema-validation/#chunk-1 · keywords: index, unique index
- [passage] You can configure indexes, unique constraints, and primary key constraints with the following attribute arguments: — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#index-configuration-index-configuration · keywords: index, unique constraint
- [passage] You can also create a named compound ID or compound unique constraint by using the `@@id` or `@@unique` attributes' `name` field. For example: — https://www.prisma.io/docs/orm/v7/prisma-client/special-fields-and-types/working-with-composite-ids-and-constraints#working-with-compound-ids-and-unique-constraints-docs-orm-v7-prisma-client-special-fields-and-types-working-with-composite-ids-and-constraints · keywords: @@index, unique constraint, @@id, @@unique, name

## Examples and snippets

- [snippet] ```sql — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#raw-sql-syntax-with-raw-raw-sql-syntax-with-raw · keywords: index, index build, partial index, unique index
- [snippet] ```sql — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#raw-sql-syntax-with-raw-raw-sql-syntax-with-raw · keywords: index, index build, partial index, unique index
- [snippet] ```sql theme={null} — https://thenile.dev/docs/extensions/btree_gin#creating-a-gin-index-with-btree-gin · keywords: B-tree, GIN index, index, index build · also: https://thenile.dev/docs/extensions/hstore, https://thenile.dev/docs/extensions/pg_trgm, https://thenile.dev/docs/postgres/datatype/json
- [snippet] ```sql theme={null} — https://thenile.dev/docs/extensions/btree_gist#creating-a-gist-index-with-btree-gist · keywords: B-tree, GiST index, index, index build · also: https://thenile.dev/docs/extensions/cube, https://thenile.dev/docs/extensions/pg_trgm, https://thenile.dev/docs/extensions/rdkit
- [snippet] ```sql theme={null} — https://thenile.dev/docs/postgres/indexes#example-employees-table · keywords: B-tree, hashed index, index, index build
- [snippet] ```sql theme={null} — https://thenile.dev/docs/postgres/indexes#3-create-an-sp-gist-index-on-emp-location · keywords: B-tree, GiST index, index, index build
- [snippet] ```sql theme={null} — https://thenile.dev/docs/postgres/indexes#3-create-brin-index-on-hire-date · keywords: B-tree, BRIN index, index, index build
- [snippet] ```prisma title="schema.prisma" showLineNumbers — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#raw-sql-syntax-with-raw-raw-sql-syntax-with-raw · keywords: @@index, index, partial index, unique constraint
- [snippet] ```prisma title="schema.prisma" showLineNumbers — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#object-literal-syntax-type-safe-alternative-object-literal-syntax-type-safe-alternative · keywords: @@index, index, partial index, unique constraint
- [snippet] ```prisma title="schema.prisma" showLineNumbers — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#object-literal-syntax-type-safe-alternative-object-literal-syntax-type-safe-alternative · keywords: @@index, index, partial index, unique constraint
- [snippet] ```prisma title="schema.prisma" showLineNumbers — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#using-where-with-other-index-arguments-using-where-with-other-index-arguments · keywords: @@index, index, partial index, unique constraint
- [snippet] ```sql theme={null} — https://docs.turso.tech/sql-reference/statements/create-index#unique-indexes · keywords: index, index build, unique constraint, unique index
- [snippet] ``` — file:///Users/mitch/.global-ai-hub/llms-full/files/docs.convex.dev.txt#why-why-3-direct-link-to-why · keywords: index, unique constraint, unused index, withIndex, ctx.auth, user
- [snippet] ```sql theme={null} — https://thenile.dev/docs/extensions/pg_trgm#creating-a-table-with-text-search · keywords: GIN index, GiST index, index, index build
- [snippet] ```prisma — https://www.prisma.io/docs/orm/v7/reference/prisma-schema-reference#define-a-multi-column-index-relational-databases-only-define-a-multi-column-index-relational-databases-only · keywords: @@index, compound index, index · also: https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/models
- [snippet] ```prisma title="schema.prisma" showLineNumbers — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#space-partitioned-gist-sp-gist-space-partitioned-gist-sp-gist · keywords: @@index, GiST index, index
- [snippet] ```sql — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#space-partitioned-gist-sp-gist-space-partitioned-gist-sp-gist · keywords: GiST index, index, index build
- [snippet] ```sql theme={null} — https://thenile.dev/docs/extensions/btree_gin#removing-a-gin-index · keywords: GIN index, index, index build, sql theme={null}
DROP INDEX gin_idx;
 · also: https://thenile.dev/docs/extensions/bloom, https://thenile.dev/docs/extensions/btree_gist, https://thenile.dev/docs/extensions/citext, https://thenile.dev/docs/extensions/cube
- [snippet] ```sql theme={null} — https://thenile.dev/docs/extensions/pg_bigm#creating-a-gin-index · keywords: GIN index, index, index build
- [snippet] ```sql theme={null} — https://thenile.dev/docs/postgres/indexes#b-tree-index · keywords: B-tree, index, index build
- [snippet] ```sql theme={null} — https://thenile.dev/docs/postgres/indexes#5-another-example-ip-network-search · keywords: B-tree, index, index build
- [snippet] ```sql theme={null} — https://docs.turso.tech/sql-reference/statements/create-index#expression-indexes · keywords: expression index, index, index build
- [snippet] ```sql theme={null} — https://docs.turso.tech/sql-reference/statements/create-index#unique-index-to-enforce-a-business-rule · keywords: index, index build, unique index · also: https://docs.turso.tech/sql-reference/cli/shell-commands
- [snippet] ```sql theme={null} — https://docs.turso.tech/sql-reference/statements/create-index#partial-index-for-a-status-filter · keywords: index, index build, partial index
- [snippet] db.collection.createIndex( <key and index type specification>, { unique: true } ) — https://www.mongodb.com/docs/manual/core/index-unique/#create-a-unique-index · keywords: index, index build, unique index
- [snippet] db.users.createIndex( { "email": 1 }, { unique: true } ) — https://www.mongodb.com/docs/manual/core/index-unique/#unique-index-on-a-single-field · keywords: index, index build, unique index
- [snippet] db.users.createIndex( { password : 1 }, { name: "unique_index", unique: true } ) — https://www.mongodb.com/docs/manual/core/index-unique/#unique-and-sparse-index-creation · keywords: index, index build, sparse index
- [snippet] db.users.createIndex( { password : 1 }, { name: "unique_sparse_index", unique: true, sparse: true } ) — https://www.mongodb.com/docs/manual/core/index-unique/#unique-and-sparse-index-creation · keywords: index, index build, sparse index
- [snippet] db.users.createIndex( { password : 1 }, { name: "sparse_index", sparse: true } ) — https://www.mongodb.com/docs/manual/core/index-unique/#basic-and-sparse-index-creation · keywords: index, index build, sparse index
- [snippet] db.users.createIndex( { password : 1 }, { name: "basic_index" } ) — https://www.mongodb.com/docs/manual/core/index-unique/#basic-and-sparse-index-creation · keywords: index, index build, sparse index
- [snippet] db.students.createIndex( { grades: 1 } ) — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-multikey/multikey-index-bounds/#create-a-multikey-index · keywords: index, index build, multikey index · also: https://www.mongodb.com/docs/manual/core/indexes/index-types/index-multikey/create-multikey-index-basic/, https://www.mongodb.com/docs/manual/core/indexes/index-types/index-single/create-embedded-object-index/, https://www.mongodb.com/docs/manual/core/indexes/index-types/index-single/create-single-field-index/
- [snippet] db.survey3.createIndex( { "ratings.scores.q1": 1, "ratings.scores.q2": 1 } ) — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-multikey/multikey-index-bounds/#create-a-compound-multikey-index · keywords: index, index build, multikey index
- [snippet] db.products.createIndex( { "$**" : 1 }, { "wildcardProjection" : { "attributes.memory" : 0 } } ) — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/create-wildcard-index-multiple-fields/#exclude-specific-fields-from-a-wildcard-index · keywords: index, index build, wildcard index
- [snippet] db.movies.createIndex( { "awards.$**": 1 } ) — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#create-a-wildcard-index-on-a-single-field-path · keywords: index, index build, wildcard index
- [snippet] db.movies.createIndex( { "$**": 1 } ) — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#create-a-wildcard-index-on-all-field-paths · keywords: index, index build, wildcard index · also: https://www.mongodb.com/docs/manual/data-modeling/schema-design-process/create-indexes/
- [snippet] db.movies.createIndex( { "$**": 1 }, { "wildcardProjection": { "tomatoes.viewer": 1, "tomatoes.critic": 1 } } ) — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#include-specific-fields-in-wildcard-index-coverage · keywords: index, index build, wildcard index
- [snippet] db.movies.createIndex( { "$**": 1 }, { "wildcardProjection": { "tomatoes.viewer": 0, "tomatoes.critic": 0 } } ) — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#omit-specific-fields-from-wildcard-index-coverage · keywords: index, index build, wildcard index
- [snippet] ```prisma title="schema.prisma" showLineNumbers — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#configuring-the-index-sort-order-with-sort-configuring-the-index-sort-order-with-sort · keywords: @@index, index, unique constraint
- [snippet] ``` — file:///Users/mitch/.global-ai-hub/llms-full/files/docs.convex.dev.txt#indexing-multiple-fields-indexing-multiple-fields-direct-link-to-indexing-multiple-fields · keywords: index, unique constraint, withIndex
- [snippet] ``` — file:///Users/mitch/.global-ai-hub/llms-full/files/docs.convex.dev.txt#indexing-multiple-fields-indexing-multiple-fields-direct-link-to-indexing-multiple-fields · keywords: index, unique constraint, withIndex
- [snippet] ``` — file:///Users/mitch/.global-ai-hub/llms-full/files/docs.convex.dev.txt#indexing-multiple-fields-indexing-multiple-fields-direct-link-to-indexing-multiple-fields · keywords: index, unique constraint, withIndex
- [snippet] ```prisma title="schema.prisma" showLineNumbers — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#generalized-inverted-index-gin-generalized-inverted-index-gin · keywords: @@index, GIN index, index
- [snippet] ```prisma title="schema.prisma" showLineNumbers — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#generalized-search-tree-gist-generalized-search-tree-gist · keywords: @@index, GiST index, index
- [snippet] ```prisma title="schema.prisma" showLineNumbers — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#block-range-index-brin-block-range-index-brin · keywords: @@index, BRIN index, index
- [snippet] ```sql — https://www.prisma.io/docs/guides/upgrade-prisma-orm/v6#schema-change-for-implicit-m-n-relations-on-postgresql-schema-change-for-implicit-m-n-relations-on-postgresql · keywords: index, index build, unique index
- [snippet] ```sql title="migration" — https://www.prisma.io/docs/orm/v7/prisma-migrate/workflows/customizing-migrations#before · keywords: index, index build, unique index
- [snippet] ```sql title="migration" — https://www.prisma.io/docs/orm/v7/prisma-migrate/workflows/customizing-migrations#after · keywords: index, index build, unique index
- [snippet] ```sql — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#generalized-inverted-index-gin-generalized-inverted-index-gin · keywords: GIN index, index, index build
- [snippet] ```sql — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#generalized-search-tree-gist-generalized-search-tree-gist · keywords: GiST index, index, index build
- [snippet] ```sql — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#block-range-index-brin-block-range-index-brin · keywords: BRIN index, index, index build
- [snippet] ```ts — https://orm.drizzle.team/docs/pg/extensions#indexes · keywords: GiST index, index, index build
- [snippet] ```sql theme={null} — https://thenile.dev/docs/extensions/pgrouting#creating-the-network-table · keywords: GiST index, index, index build
- [snippet] ```sql theme={null} — https://thenile.dev/docs/extensions/postgis#creating-a-spatial-table · keywords: GiST index, index, index build
- [snippet] ```sql theme={null} — https://thenile.dev/docs/extensions/prefix#creating-a-table-for-phone-prefixes · keywords: GiST index, index, index build
- [snippet] ```sql theme={null} — https://thenile.dev/docs/extensions/prefix#call-router-implementation · keywords: GiST index, index, index build
- [snippet] ```sql theme={null} — https://thenile.dev/docs/extensions/seg#performance-optimization · keywords: GiST index, index, index build
- [snippet] ```prisma — https://www.prisma.io/docs/orm/reference/orm-client#mongodb · keywords: @@index, index, unique constraint · also: https://www.prisma.io/docs/orm/reference/pipeline-builder, https://www.prisma.io/docs/guides/upgrade-prisma-orm/mongodb, https://www.prisma.io/docs/orm/contract-authoring/psl-syntax
- [snippet] ```prisma title="schema.prisma" showLineNumbers — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#configuring-the-name-of-indexes-with-map-configuring-the-name-of-indexes-with-map · keywords: @@index, index, unique constraint
- [snippet] ``` — https://orm.drizzle.team/docs/cockroach/migrations#how-can-drizzle-help · keywords: index, unique constraint, unique index, 
Add column to 
- [snippet] In MySQL, a `unique constraint` and a `unique index` are essentially the same thing. — https://orm.drizzle.team/docs/mysql/indexes-constraints#unique · keywords: index, unique constraint, unique index
- [snippet] ```sql theme={null} — https://thenile.dev/docs/auth/concepts/builtintables#auth-sessions · keywords: B-tree, index, unique constraint
- [snippet] ```sql theme={null} — https://thenile.dev/docs/auth/concepts/builtintables#auth-verification-tokens · keywords: B-tree, index, unique constraint
- [snippet] ```sql theme={null} — https://thenile.dev/docs/auth/concepts/builtintables#auth-oidc-providers · keywords: B-tree, index, unique constraint
- [snippet] ```prisma title="schema.prisma" showLineNumbers — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#enabling-the-partialindexes-preview-feature-enabling-the-partialindexes-preview-feature · keywords: index, partial index
- [snippet] ```sql — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#raw-sql-syntax-with-raw-raw-sql-syntax-with-raw · keywords: index, partial index
- [snippet] ```prisma title="schema.prisma" showLineNumbers — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#introspection-introspection · keywords: index, partial index
- [snippet] db.runCommand( { collMod: "apples", index: { keyPattern: { type: 1 }, prepareUnique: true } } ) — https://www.mongodb.com/docs/manual/core/index-unique/convert-to-unique/#prepare-the-index-to-be-converted-to-a-unique-index · keywords: index, unique index
- [snippet] Docs in MCP: "How do I create a compound index in MongoDB?" — `"How do I create a compound index in MongoDB?"` — https://www.mongodb.com/docs/build-with-ai#docs-in-mcp · keywords: compound index, index, "How do I create a compound index in MongoDB?"
- [snippet] ```text — https://www.mongodb.com/docs/build-with-ai#docs-in-mcp · keywords: compound index, index, text
"How do I create a compound index in MongoDB?"

- [snippet] ```sql — https://www.prisma.io/docs/orm/v7/prisma-client/setup-and-configuration/custom-model-and-field-names#example-relational-database-example-relational-database · keywords: index, unique index
- [snippet] ```sql — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#configuring-the-index-sort-order-with-sort-configuring-the-index-sort-order-with-sort · keywords: index, unique index
- [snippet] ```sql — https://orm.drizzle.team/docs/mysql/drizzle-kit-generate#drizzle-kit-generate · keywords: index, unique index, users, id, name, email · also: https://orm.drizzle.team/docs/mysql/migrations
- [snippet] ```typescript — https://orm.drizzle.team/docs/mysql/insert#on-duplicate-key-update · keywords: index, unique index
- [snippet] ```sql — https://orm.drizzle.team/docs/mysql/migrations#how-can-drizzle-help · keywords: index, unique index, sql
CREATE TABLE ,  (
        ,  int AUTO_INCREMENT PRIMARY KEY,
        ,  varchar(255),
        
- [snippet] ```typescript — https://orm.drizzle.team/docs/singlestore/insert#on-duplicate-key-update · keywords: index, unique index
- [snippet] ```sql — https://orm.drizzle.team/docs/tutorials/drizzle-with-turso#applying-changes-to-the-database · keywords: index, unique index, sql
CREATE TABLE ,  (
	,  integer PRIMARY KEY NOT NULL,
	,  text NOT NULL,
	
- [snippet] ```sql — https://orm.drizzle.team/docs/tutorials/drizzle-with-vercel-edge-functions#applying-changes-to-the-database · keywords: index, unique index, sql
CREATE TABLE ,  (
	,  integer PRIMARY KEY NOT NULL,
	,  text NOT NULL,
	
- [snippet] ```sql theme={null} — https://thenile.dev/docs/auth/concepts/builtintables#users-users · keywords: B-tree, index · also: https://thenile.dev/docs/auth/dashboard/managing-users
- [snippet] ```sql theme={null} — https://thenile.dev/docs/auth/concepts/builtintables#users-tenant-users · keywords: B-tree, index
- [snippet] ```sql theme={null} — https://thenile.dev/docs/auth/concepts/builtintables#auth-credentials · keywords: B-tree, index
- [snippet] ```sql theme={null} — https://thenile.dev/docs/auth/concepts/builtintables#auth-invites · keywords: B-tree, index
- [snippet] ```sql theme={null} — https://thenile.dev/docs/auth/concepts/builtintables#auth-oidc-relying-parties · keywords: B-tree, index
- [snippet] ```sql theme={null} — https://thenile.dev/docs/auth/concepts/builtintables#auth-tenant-oidc-relying-parties · keywords: B-tree, index
- [snippet] ```sql theme={null} — https://docs.turso.tech/sql-reference/statements/upsert#conflict-target · keywords: index, unique index
- [snippet] ```typescript filename="drizzle/schema.ts" — https://orm.drizzle.team/docs/get-started/gel-existing#step-3-pull-gel-types-to-drizzle-schema · keywords: B-tree, unique index, uuid_generate_v4() · also: https://orm.drizzle.team/docs/get-started/gel-new, https://orm.drizzle.team/docs/guides/gel-ext-auth
- [snippet] ```typescript — file:///Users/mitch/.global-ai-hub/llms-full/files/instantdb.com.txt#common-mistakes-with-storage · keywords: index, unique constraint
- [snippet] ```ts — file:///Users/mitch/.global-ai-hub/llms-full/files/instantdb.com.txt#creating-a-to-do-list-app · keywords: index, unique constraint
- [snippet] ```ts — file:///Users/mitch/.global-ai-hub/llms-full/files/instantdb.com.txt#creating-a-to-do-list-app · keywords: index, unique constraint
- [snippet] ```ts — file:///Users/mitch/.global-ai-hub/llms-full/files/instantdb.com.txt#creating-a-to-do-list-app · keywords: index, unique constraint
- [snippet] ```ts — file:///Users/mitch/.global-ai-hub/llms-full/files/instantdb.com.txt#creating-a-to-do-list-app · keywords: index, unique constraint
- [snippet] ```typescript — file:///Users/mitch/.global-ai-hub/llms-full/files/instantdb.com.txt#typesafety · keywords: index, unique constraint
- [snippet] ```typescript — file:///Users/mitch/.global-ai-hub/llms-full/files/instantdb.com.txt#instant-schema-ts · keywords: index, unique constraint
- [snippet] ```typescript — file:///Users/mitch/.global-ai-hub/llms-full/files/instantdb.com.txt#setting-limits-via-permissions · keywords: index, unique constraint
- [snippet] ```js — file:///Users/mitch/.global-ai-hub/llms-full/files/instantdb.com.txt#composite-keys · keywords: index, unique constraint
- [snippet] ```javascript — file:///Users/mitch/.global-ai-hub/llms-full/files/instantdb.com.txt#adding-properties · keywords: index, unique constraint, nickname
- [snippet] ```typescript — file:///Users/mitch/.global-ai-hub/llms-full/files/instantdb.com.txt#attributes · keywords: index, unique constraint, nickname
- [snippet] ```javascript — file:///Users/mitch/.global-ai-hub/llms-full/files/instantdb.com.txt#storage-quick-start · keywords: index, unique constraint
- [snippet] ```javascript — file:///Users/mitch/.global-ai-hub/llms-full/files/instantdb.com.txt#view-files · keywords: index, unique constraint
- [snippet] ```javascript — file:///Users/mitch/.global-ai-hub/llms-full/files/instantdb.com.txt#update-files · keywords: index, unique constraint
- [snippet] ```ts — file:///Users/mitch/.global-ai-hub/llms-full/files/instantdb.com.txt#linking-streams-to-entities · keywords: index, unique constraint
- [snippet] ```ts — file:///Users/mitch/.global-ai-hub/llms-full/files/instantdb.com.txt#setup · keywords: index, unique constraint
- [snippet] db.<collection>.createIndex( { <field>: "2dsphere" }, { "2dsphereIndexVersion": <version> } ) — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2dsphere/2dsphere-index-versions/#important · keywords: geospatial index, index build
- [snippet] db.test.createIndex( { "address": "2dsphere" }, { "2dsphereIndexVersion": 2 } ) — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2dsphere/2dsphere-index-versions/#chunk-1 · keywords: geospatial index, index build
- [snippet] db.collection.createIndex( { "a": 1 }, { unique: true, sparse: true, expireAfterSeconds: 3600 } ) — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#options · keywords: TTL index, index build
- [snippet] db.restaurants.createIndexes([{"borough": 1}, {"location": "2dsphere"}]) — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/#chunk-28 · keywords: geospatial index, index build
- [snippet] ```prisma — https://www.prisma.io/docs/orm/v7/reference/prisma-client-reference#database-upsert-examples-database-upsert-examples · keywords: @@index, unique constraint · also: https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/models, https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/relations/one-to-one-relations
- [snippet] ```prisma no-lines — https://www.prisma.io/docs/orm/v7/reference/prisma-schema-reference#arguments-arguments-4 · keywords: @@index, unique constraint
- [snippet] ```prisma no-lines — https://www.prisma.io/docs/orm/v7/reference/prisma-schema-reference#arguments-arguments-4 · keywords: @@index, unique constraint
- [snippet] ```prisma — https://www.prisma.io/docs/orm/v7/reference/prisma-schema-reference#relational-databases · keywords: @@index, unique constraint
- [snippet] ```prisma — https://www.prisma.io/docs/orm/v7/reference/prisma-schema-reference#relational-databases · keywords: @@index, unique constraint
- [snippet] ```prisma — https://www.prisma.io/docs/orm/v7/reference/prisma-schema-reference#relational-databases · keywords: @@index, unique constraint · also: https://www.prisma.io/docs/guides/upgrade-prisma-orm/v3, https://www.prisma.io/docs/orm/v7/more/troubleshooting/many-to-many-relations, https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/models
- [snippet] ```prisma — https://www.prisma.io/docs/orm/v7/reference/prisma-schema-reference#mongodb · keywords: @@index, unique constraint
- [snippet] ```prisma — https://www.prisma.io/docs/orm/v7/prisma-client/setup-and-configuration/custom-model-and-field-names#example-relational-database-example-relational-database · keywords: @@index, unique constraint
- [snippet] ```prisma — https://www.prisma.io/docs/orm/v7/prisma-client/setup-and-configuration/custom-model-and-field-names#example-relational-database-example-relational-database · keywords: @@index, unique constraint
- [snippet] ```prisma — https://www.prisma.io/docs/orm/v7/prisma-client/special-fields-and-types/composite-types#duplicate-values-in-unique-fields-of-composite-types-duplicate-values-in-unique-fields-of-composite-types · keywords: @@index, unique constraint
- [snippet] ```prisma — https://www.prisma.io/docs/orm/v7/prisma-client/special-fields-and-types/composite-types#use-prisma-orm-relations-to-enforce-unique-values-in-a-record-use-prisma-orm-relations-to-enforce-unique-values-in-a-record · keywords: @@index, unique constraint
- [snippet] ``` — file:///Users/mitch/.global-ai-hub/llms-full/files/docs.convex.dev.txt#unique-unique-direct-link-to-unique · keywords: unique constraint, withIndex
- [snippet] ``` — file:///Users/mitch/.global-ai-hub/llms-full/files/docs.convex.dev.txt#interface-query-tableinfo · keywords: unique constraint, withIndex
- [snippet] ``` — file:///Users/mitch/.global-ai-hub/llms-full/files/docs.convex.dev.txt#example-example-direct-link-to-example · keywords: unique constraint, withIndex
- [snippet] ```ts — https://www.prisma.io/docs/orm/v7/more/comparisons/prisma-and-drizzle#data-modeling-data-modeling · keywords: unique constraint, unique index · also: https://orm.drizzle.team/docs/singlestore/relations-v2

## Problems, failure modes and limitations

- [passage] - If a hidden index is a [unique index](https://www.mongodb.com/docs/manual/core/index-unique/#std-label-index-type-unique) , the index still applies its unique constraint to the documents. - If a hidden index is a [TTL index](https://www.mongodb.com/docs/manual/core/index-ttl/#std-label-index-feature-ttl) , the index still expires documents. - Hidden indexes are included in [`listIndexes`](https://www.mongodb.com/docs/manual/reference/command/listIndexes/#mongodb-dbcommand-dbcmd.listIndexes) and[`db.collection.getIndexes()`](https://www.mongodb.com/docs/manual/reference/method/db.collection.getIndexes/#mongodb-method-db.collection.getIndexes) results. - Hidden indexes are updated upon write operations to the collection and continue to consume disk space and memory. As such, they are included in various statistics operations, such as [`db.collection.stats()`](https://www.mongodb.com/docs/manual/reference/method/db.collection.stats/#mongodb-method-db.collection.stats) and[`$indexStats`.](https://www.mongodb.com/docs/manual/reference/operator/aggregation/indexStats/#mongodb-pipeline-pipe.-indexStats) - Hiding an unhidden index or unhiding a hidden index resets its [`$indexStats`](htt — https://www.mongodb.com/docs/manual/core/index-hidden/#chunk-1 · keywords: TTL index, hidden index, index, index build, unique constraint, unique index
- [passage] If a document has a `null` or missing value for the indexed field in a unique single-field index, the index stores a `null` value for that document. Because of the unique constraint, a single-field unique index can only contain one document that contains a `null` value in its index entry. If there is more than one document with a `null` value in its index entry, the index build fails with a duplicate key error. — https://www.mongodb.com/docs/manual/core/index-unique/#missing-document-field-in-a-unique-single-field-index · keywords: index, index build, index definition, unique constraint, unique index, null
- [passage] | Parameter | Description | | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | | `UNIQUE` | Enforces a uniqueness constraint. Turso rejects any INSERT or UPDATE that would create duplicate values in the indexed columns. | | `IF NOT EXISTS` | Prevents an error if an index with the same name already exists. The statement is a no-op when the index is present. | | `schema-name` | The name of the attached database containing the table. Defaults to the main database if omitted. | | `index-name` | A unique name for the index within the database. | | `table-name` | The table to index. | | `column-or-expr` | A column name or an expression to include in the index. Multiple entries are separated by commas. | | `COLLATE collation-name` | An optional collation sequence applied to the indexed column (e.g. `NOCASE`). | | `ASC` / `DESC` | Sort direction for the indexed column. Default is `ASC`. | | `WHERE filter-expression` | An optional filter that creates a partial index. Only rows matching the expression are included in the index. | — https://docs.turso.tech/sql-reference/statements/create-index#parameters · keywords: index, index build, index definition, partial index, UNIQUE, IF NOT EXISTS
- [passage] * GIN indexes can be large, plan storage accordingly * Index creation might be slow for large tables * Index only necessary columns * Monitor index size and search performance * Adjust similarity threshold to balance precision and recall * Consider using `pg_bigm.enable_recheck` for better accuracy — https://thenile.dev/docs/extensions/pg_bigm#performance-considerations · keywords: GIN index, index, index build, index overhead, pg_bigm.enable_recheck
- [passage] * GIN indexes provide faster search but slower updates * Index size can be large for text columns with many unique values * Consider partial indexes for large tables * Monitor and adjust similarity threshold based on false positive/negative rates — https://thenile.dev/docs/extensions/pg_trgm#performance-considerations · keywords: GIN index, index, index overhead, partial index
- [passage] | Condition | Error | | --------------------------------------------------------------------------------------- | ------------------------------------------------ | | The column is a PRIMARY KEY or part of one | Cannot drop PRIMARY KEY column | | The column has a UNIQUE constraint | Cannot drop UNIQUE column | | The column is referenced by an index | Cannot drop indexed column | | The column is referenced in an expression index | Cannot drop column used in expression index | | The column is named in a partial index WHERE clause | Cannot drop column used in partial index | | The column is referenced in a CHECK constraint belonging to another column or the table | Cannot drop column used in CHECK constraint | | The column is referenced by a foreign key in another table | Cannot drop column used in foreign key | | The table has only one column | Cannot drop the last remaining column | | The column appears in a trigger or view | Cannot drop column referenced by trigger or view | — https://docs.turso.tech/sql-reference/statements/alter-table#restrictions · keywords: expression index, index, partial index, unique constraint
- [passage] After `prepareUnique` is set, you cannot insert new documents that duplicate an index key entry. For example, the following insert operation results in an error: — https://www.mongodb.com/docs/manual/core/index-unique/convert-to-unique/#prepare-the-index-to-be-converted-to-a-unique-index · keywords: index, index definition, unique index, prepareUnique
- [passage] * Faster searches * Slower updates * Larger index size * Better for static data — https://thenile.dev/docs/extensions/pg_trgm#gin-index · keywords: GIN index, index, index overhead
- [passage] * Enforced by a [compound index in MongoDB](https://www.mongodb.com/docs/manual/core/index-compound/) * A `@@unique` block cannot be used as the only unique identifier for a model - MongoDB requires an `@id` field — https://www.prisma.io/docs/orm/v7/reference/prisma-schema-reference#mongodb-mongodb-13 · keywords: @@index, compound index, index, @@unique, @id
- [passage] * Only explicitly created indexes can be dropped. Indexes automatically created for PRIMARY KEY and UNIQUE constraints cannot be dropped directly -- drop the table or use ALTER TABLE instead. * Dropping an index may degrade query performance for queries that relied on it, but does not affect correctness. — https://docs.turso.tech/sql-reference/statements/drop-index#behavior · keywords: index, index build, unique constraint
- [problem] Wildcard indexes have restrictions including compound index limitations, incompatible properties, and specific shard key constraints. — https://www.mongodb.com/docs/manual/manual-1-llms.txt · keywords: compound index, index, wildcard index
- [passage] GIN and BTree are the only index types supported by CockroachDB. The operator classes marked to work with CockroachDB are the only ones allowed on that database and supported by Prisma ORM. The operator class cannot be defined in the Prisma Schema Language: the `ops` argument is not necessary or allowed on CockroachDB. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#cockroachdb-cockroachdb · keywords: B-tree, GIN index, index, ops
- [passage] - For a to-be-sharded collection, you cannot shard the collection if the collection has multiple unique indexes unless the shard key is the prefix for all the unique indexes. - For an already-sharded collection, you cannot create unique indexes on other fields unless the shard key is included as the prefix. - A unique index stores a null value for a document missing the indexed field; that is a missing index field is treated as another instance of a `null` index key value. For more information, see[Missing Document Field in a Unique Single-Field Index.](https://www.mongodb.com#std-label-unique-index-and-missing-field) — https://www.mongodb.com/docs/manual/core/index-unique/#chunk-7 · keywords: index, index definition, unique index, null
- [passage] If the query does not join the conditions on the array field with `$elemMatch`, MongoDB cannot intersect the multikey index bounds. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-multikey/multikey-index-bounds/#query-without-elemmatch · keywords: index, index definition, multikey index, $elemMatch
- [passage] You cannot [drop](https://www.mongodb.com/docs/manual/reference/method/db.collection.dropIndex/#std-label-collection-drop-index) or [hide](https://www.mongodb.com/docs/manual/reference/method/db.collection.hideIndex/#std-label-collection-hide-index) an index if it is the only non-hidden index that supports the shard key. — https://www.mongodb.com/docs/manual/core/sharding-shard-key-indexes/#chunk-2 · keywords: hidden index, index, index build
- [passage] If you cannot stop all writes to the collection during this procedure, do not use the procedure on this page. Instead, build your unique index on the collection by issuing [`db.collection.createIndex()`](https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#mongodb-method-db.collection.createIndex) on the primary for a replica set. — https://www.mongodb.com/docs/manual/tutorial/build-indexes-on-replica-sets/#unique-indexes · keywords: index, index build, unique index, db.collection.createIndex()
- [passage] - For building unique indexes - To create [unique indexes](https://www.mongodb.com/docs/manual/core/index-unique/#std-label-index-type-unique) using the following procedure, you must stop all writes to the collection during the index build. Otherwise, you may end up with inconsistent data across the replica set members.## WarningIf you cannot stop all writes to the collection, do not use the following procedure to create unique indexes. — https://www.mongodb.com/docs/manual/tutorial/build-indexes-on-replica-sets/#prerequisites · keywords: index, index build, unique index · also: https://www.mongodb.com/docs/manual/tutorial/build-indexes-on-sharded-clusters/
- [passage] If you cannot stop all writes to the collection during this procedure, do not use the procedure on this page. Instead, build your unique index on the collection by issuing [`db.collection.createIndex()`](https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#mongodb-method-db.collection.createIndex) on the [`mongos`](https://www.mongodb.com/docs/manual/reference/program/mongos/#mongodb-binary-bin.mongos) for a sharded cluster. — https://www.mongodb.com/docs/manual/tutorial/build-indexes-on-sharded-clusters/#chunk-1 · keywords: index, index build, unique index, db.collection.createIndex(), mongos
- [passage] When you insert `user1` without the `accounts.bank` and `accounts.number` fields, MongoDB sets them to `null` and adds a unique index entry. Any later insert that also lacks either field, such as `user2`, causes a duplicate key error. — https://www.mongodb.com/docs/manual/tutorial/unique-indexes-schema-validation/#chunk-3 · keywords: index, index definition, unique index, user1, accounts.bank, accounts.number
- [passage] Note that in PostgreSQL, while you cannot specify sort order on unique constraints directly, you can create a unique index with a sort order that will enforce uniqueness: — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#configuring-the-index-sort-order-with-sort-configuring-the-index-sort-order-with-sort · keywords: index, unique constraint, unique index
- [passage] MongoDB cannot create a [unique index](https://www.mongodb.com#std-label-index-type-unique) on the specified index field(s) if the collection already contains data that would violate the unique constraint for the index. — https://www.mongodb.com/docs/manual/core/index-unique/#restrictions · keywords: index, unique constraint, unique index
- [passage] You cannot specify a unique constraint on a [hashed index.](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-hashed/#std-label-index-type-hashed) — https://www.mongodb.com/docs/manual/core/index-unique/#restrictions · keywords: hashed index, index, unique constraint
- [passage] > [!WARNING] > Indexes using a function (such as `to_tsvector`) to determine the indexed value are not yet supported by Prisma ORM. Indexes defined in this way will not be visible with `prisma db pull`. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#generalized-inverted-index-gin-generalized-inverted-index-gin · keywords: GIN index, index, to_tsvector, prisma db pull
- [passage] > [!NOTE] > The object literal syntax validates field types. For example, you cannot use a `Boolean` value for a `String` field. For fields with types that are not supported by the object syntax (such as `Unsupported` or composite types), use `raw()` instead. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#object-literal-syntax-type-safe-alternative-object-literal-syntax-type-safe-alternative · keywords: index, partial index, Boolean, String, Unsupported, raw()
- [passage] MongoServerError: E11000 duplicate key error collection: test.apples index: type_1 dup key: { type: "Delicious" } — https://www.mongodb.com/docs/manual/core/index-unique/convert-to-unique/#prepare-the-index-to-be-converted-to-a-unique-index · keywords: index, unique index
- [passage] The pattern `"$**"` includes all fields in the document. Use the `wildcardProjection` field to limit the index to fields you specify. For complete documentation on `wildcardProjection`, see [Options for `wildcard` indexes.](https://www.mongodb.com#std-label-createIndex-method-wildcard-option) — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#include-specific-fields-in-wildcard-index-coverage · keywords: index, wildcard index, "$**", wildcardProjection, wildcard
- [passage] SQL Server [allows only one `NULL` value per `UNIQUE` constraint](https://learn.microsoft.com/en-us/sql/relational-databases/tables/unique-constraints-and-check-constraints). Use filtered indexes to work around this, but note they cannot be used as foreign keys. — https://www.prisma.io/docs/orm/v7/core-concepts/supported-databases/sql-server#common-considerations-common-considerations · keywords: index, partial index, NULL, UNIQUE
- [passage] The `Hash` type will store the index data in a format that is much faster to search and insert, and that will use less disk space. However, only the `=` and `<>` comparisons can use the index, so other comparison operators such as `<` and `>` will be much slower with `Hash` than when using the default `BTree` type. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#hash-hash · keywords: B-tree, index, Hash,  and ,  will be much slower with ,  than when using the default 
- [passage] > [!WARNING] > **CockroachDB limitation**: CockroachDB supports creating partial indexes, but it cannot introspect the predicate text from existing indexes. This means that after initial creation, modifications to the `where` clause (adding, changing, or removing a predicate) will not be detected by Prisma Migrate. The differ skips predicate comparison for CockroachDB to prevent false-positive migrations. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#database-specific-behavior-database-specific-behavior · keywords: index, partial index, where
- [passage] <Callout type="warning"> Functional indexes are supported in MySQL starting from version `8.0.13`. For the correct syntax, the expression should be enclosed in parentheses, for example, `(lower(column))`. </Callout> — https://orm.drizzle.team/docs/guides/unique-case-insensitive-email#mysql · keywords: expression index, index, 8.0.13, (lower(column))
- [passage] * `CITEXT` is slightly **slower** than `TEXT` due to case normalization. * It does **not support LIKE queries** efficiently unless you create a functional index using `LOWER(column1)`. * Collation-sensitive operations may not always behave as expected. — https://thenile.dev/docs/extensions/citext#limitations · keywords: expression index, index, CITEXT, TEXT, LOWER(column1)
- [passage] MongoDB **cannot** use the compound index to support queries on these field combinations: — https://www.mongodb.com/docs/manual/core/index-compound/#chunk-3 · keywords: compound index, index
- [passage] For replica sets and sharded clusters, using a [rolling procedure](https://www.mongodb.com/docs/manual/tutorial/build-indexes-on-replica-sets/#std-label-index-build-on-replica-sets) to create a unique index requires that you stop all writes to the collection during the procedure. If you cannot stop all writes to the collection during the procedure, do not use the rolling procedure. Instead, to build your unique index on the collection you must either: — https://www.mongodb.com/docs/manual/core/index-unique/#building-unique-index-on-replica-sets-and-sharded-clusters · keywords: index, unique index
- [passage] Using a 2d index for queries on spherical data can return incorrect results or an error. For example, 2d indexes don't support spherical queries that wrap around the poles. — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2d/calculate-distances/#about-this-task · keywords: geospatial index, index
- [passage] You cannot create a 2d index if your collection contains coordinate data outside of the index's location range. — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2d/create/define-location-range/#important · keywords: geospatial index, index
- [passage] After you create a 2d index, you cannot insert a document that contains coordinate data outside of the index's location range. — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2d/create/define-location-range/#chunk-1 · keywords: geospatial index, index
- [passage] MongoDB **cannot** use the wildcard index to fulfill this query: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/reference/embedded-object-behavior/#chunk-4 · keywords: index, wildcard index
- [passage] | Parameter | Type | Description | |---|---|---| | `wildcardProjection` | document | Optional. Allows users to include or exclude specific field paths from a [wildcard index.](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/#std-label-wildcard-index-core) This option is only valid when you create a [wildcard index](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/#std-label-wildcard-index-core) on all document fields. You cannot specify the`wildcardProjection` option when you create a wildcard index on a specific field path and its subfields. `wildcardProjection` works with specifications like: However, you can't define an index that includes the same field in the wildcard fields and the regular (non-wildcard) fields. To define the index correctly, use a `wildcardProjection` to exclude duplicated fields from the wildcard pattern. `wildcardProjection` does not work with a specification like: The `wildcardProjection` option takes the following form: The `<value>` can be either of the following: `1` or`true` to include the field in the wildcard index. `0` or`false` to exclude the field from the wildcard index. | — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#chunk-18 · keywords: index, wildcard index, wildcardProjection, <value>,  or,  to include the field in the wildcard index.  · also: https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/

## Comparisons and alternatives

- [passage] 3. **Performance Considerations:** * GIN indexes are efficient for array-based queries but may have overhead during updates. * Consider the trade-off between query performance and update cost. * Regularly vacuum the GIN index to maintain performance. — https://thenile.dev/docs/postgres/indexes#gin-index · keywords: B-tree, GIN index, index
- [passage] * GIN indexes are optimized for **fast lookups** but have **slower insert/update performance** compared to B-tree indexes. * They work best when querying multiple indexed columns together. * Unlike B-tree indexes, they **do not support range queries efficiently**. — https://thenile.dev/docs/extensions/btree_gin#limitations · keywords: B-tree, GIN index, index
- [passage] * GiST indexes generally **do not provide the same performance as B-tree indexes** for single-column lookups. * They **excel at multi-column queries** and range searches but can be **slower for simple equality lookups**. * `btree_gist` is useful primarily for **exclusion constraints** rather than improving query performance. — https://thenile.dev/docs/extensions/btree_gist#limitations · keywords: B-tree, GiST index, index, btree_gist
- [passage] 1. **Structure**: * Hash indexes store only the hash value of the data being indexed. * No restrictions on the size of the indexed column. * Support only single-column indexes. * Do not allow uniqueness checking. 2. **Use Cases**: * Ideal for scenarios where exact matches are common. * Not suitable for range queries or pattern matching. 3. **Performance Considerations**: * Fast for equality lookups. * Minimal overhead during data insertion. * Not automatically maintained (unlike B-tree indexes). — https://thenile.dev/docs/postgres/indexes#hash-index · keywords: B-tree, hashed index, index
- [passage] * GiST indexes are powerful but may have higher insertion and maintenance costs compared to B-tree indexes. * Choose the appropriate operator class and indexing strategy based on your data type and query requirements. — https://thenile.dev/docs/postgres/indexes#performance-considerations · keywords: B-tree, GiST index, index
- [passage] * `@id` marks the primary key; `@@id([a, b])` declares a composite key. * `@unique` adds a unique constraint; `@@index([...])` declares a secondary index. * `@default(...)` sets a default. Database function defaults such as `@default(now())` become column defaults in the database. Generated defaults such as `@default(uuid())` are applied by Prisma 8 before each write instead, so they work the same on every database. They appear in the contract's `execution` section rather than as DDL. * `@map("column_name")` sets a field's physical name; `@@map("table_name")` sets the table or collection name when it differs from the model name. — https://www.prisma.io/docs/orm/contract-authoring/psl-syntax#models-and-fields-models-and-fields · keywords: @@index, index, unique constraint, @id, @@id([a, b]), @unique
- [passage] * Bloom filters are probabilistic and can produce **false positives**, meaning they may return more results than expected. * They are best suited for queries filtering multiple indexed columns using **equality conditions** (`=`). * Unlike B-tree indexes, they **do not support range queries** (`<`, `>`, `BETWEEN`). — https://thenile.dev/docs/extensions/bloom#limitations · keywords: B-tree, index, , 
- [passage] * Indexes can be larger compared to traditional B-tree indexes * Not suitable for exact matching (use standard indexes instead) * May require more memory during search operations * Performance depends on similarity threshold and data size — https://thenile.dev/docs/extensions/pg_bigm#limitations · keywords: B-tree, index
- [passage] If your data is stored as longitude and latitude and you often run queries on spherical surfaces, use a [2dsphere index](https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2dsphere/#std-label-2dsphere-index) instead of a 2d index. — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2d/calculate-distances/#about-this-task · keywords: geospatial index, index

## Facts and statements

- [passage] * The [`type` argument](#configuring-the-access-type-of-indexes-with-type-postgresql) allows you to support index access methods other than PostgreSQL's default `BTree` access method * Available on the `@@index` attribute * PostgreSQL only * Supported index access methods: `Hash`, `Gist`, `Gin`, `SpGist` and `Brin` — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#index-configuration-index-configuration · keywords: @@index, B-tree, BRIN index, GIN index, GiST index, index
- [passage] The `type` argument is available for configuring the index type in PostgreSQL with the `@@index` attribute. The index access methods available are `Hash`, `Gist`, `Gin`, `SpGist` and `Brin`, as well as the default `BTree` index access method. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#configuring-the-access-type-of-indexes-with-type-postgresql-configuring-the-access-type-of-indexes-with-type-postgresql · keywords: @@index, B-tree, BRIN index, GIN index, GiST index, index
- [passage] b-query-op.-or) operator [`$in`](https://www.mongodb.com/docs/manual/reference/operator/query/in/#mongodb-query-op.-in) operator [`$geoWithin`](https://www.mongodb.com/docs/manual/reference/operator/query/geoWithin/#mongodb-query-op.-geoWithin) operator [`$geoIntersects`](https://www.mongodb.com/docs/manual/reference/operator/query/geoIntersects/#mongodb-query-op.-geoIntersects) operator You can specify a `partialFilterExpression` option for all MongoDB[index types.](https://www.mongodb.com/docs/manual/core/indexes/index-types/#std-label-index-types) | | `sparse` | boolean | Optional. If `true` , the indexes only reference documents with the specified fields. These indexes use less space but behave differently in some situations (particularly sorts). The default value is`false` . See[Sparse Indexes](https://www.mongodb.com/docs/manual/core/index-sparse/#std-label-index-type-sparse) for more information. The following index types are sparse by default and ignore this option: For a compound index that includes `2dsphere` index keys and keys for other types, only the`2dsphere` index fields determine whether the index references a document. [Partial indexes](https://www.mongodb.com/do — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/#chunk-8 · keywords: compound index, geospatial index, index, index definition, partial index, sparse index · also: https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/
- [passage] ound index that includes `2dsphere` index keys and keys for other types, only the`2dsphere` index fields determine whether the index references a document. [Partial indexes](https://www.mongodb.com/docs/manual/core/index-partial/#std-label-index-type-partial) have a superset of the sparse index functionality. Unless your application has a specific requirement, use partial indexes instead of sparse indexes. | | `expireAfterSeconds` | integer | Optional. Specifies a value, in seconds, as a time to live ([TTL](https://www.mongodb.com/docs/manual/reference/glossary/#std-term-TTL) ) to control how long MongoDB retains documents in this collection. This option only applies to[TTL](https://www.mongodb.com/docs/manual/reference/glossary/#std-term-TTL) indexes. See[Expire Data from Collections by Setting TTL](https://www.mongodb.com/docs/manual/tutorial/expire-data/#std-label-ttl-collections) for more information. If you use TTL indexes created before MongoDB 5.0, or if you want to sync data created in MongDB 5.0 with a pre-5.0 installation, see [Indexes Configured Using NaN](https://www.mongodb.com/docs/manual/tutorial/expire-data/#std-label-expireData-warning) to avoid misconfiguration i — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/#chunk-9 · keywords: TTL index, geospatial index, index, index definition, partial index, sparse index
- [passage] | Index type (Algorithm) | Supported | Prisma schema | Prisma Client | Prisma Migrate | | ---------------------- | :-------: | :-----------: | :-----------: | :------------: | | B-tree | ✔️ | ✔️† | ✔️ | Not yet | | Hash | ✔️ | ✔️† | ✔️ | Not yet | | GiST | ✔️\* | ✔️† | ✔️\* | Not yet | | GIN | ✔️\* | ✔️† | ✔️\* | Not yet | | BRIN | ✔️\* | ✔️† | ✔️\* | Not yet | | SP-GiST | ✔️\* | ✔️† | ✔️\* | Not yet | — https://www.prisma.io/docs/orm/v7/reference/database-features#indexes-indexes · keywords: B-tree, BRIN index, GIN index, GiST index, index
- [passage] | Parameter | Type | Description | |---|---|---| | `unique` | boolean | Optional. Specifies that each index specified in the `keyPatterns` array is a[unique index](https://www.mongodb.com/docs/manual/core/index-unique/#std-label-index-type-unique) . Unique indexes will not accept insertion or update of documents where the index key value matches an existing value in the index. Specify `true` to create a unique index. The default value is`false` . The option is *unavailable* for[hashed indexes.](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-hashed/#std-label-index-hashed-index) | | `name` | string | Optional. The name of the index. If unspecified, MongoDB generates an index name by concatenating the names of the indexed fields and the sort order. Options specified to [`db.collection.createIndexes()`](https://www.mongodb.com#mongodb-method-db.collection.createIndexes) apply to**all** of the index specifications included in the key pattern array. Since index names must be unique, you may only specify name if you are creating a single index using`db.collection.createIndexes()` . | | `partialFilterExpression` | document | Optional. If specified, the indexes only — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/#chunk-6 · keywords: hashed index, index, index build, index definition, unique index, unique
- [passage] | Column | Type | Description | | ------- | ------- | --------------------------------------------------------------------- | | seq | INTEGER | Index sequence number | | name | TEXT | Index name | | unique | INTEGER | 1 if the index is UNIQUE | | origin | TEXT | `c` for CREATE INDEX, `u` for UNIQUE constraint, `pk` for PRIMARY KEY | | partial | INTEGER | 1 if the index is a partial index | — https://docs.turso.tech/sql-reference/pragmas#index-list · keywords: index, index build, index definition, partial index, unique constraint,  for CREATE INDEX, 
- [passage] ```sql theme={null} CREATE INDEX idx_location ON employees USING GIST (location); — https://thenile.dev/docs/postgres/indexes#example-1-geometric-shapes · keywords: B-tree, GiST index, index, index build
- [passage] * To create a GIN index on the `skills` column: ```sql theme={null} CREATE INDEX idx_gin_skills ON employees USING gin(skills); ``` — https://thenile.dev/docs/postgres/indexes#gin-index · keywords: B-tree, GIN index, index, index build, skills
- [passage] This example creates multiple indexes with the same key pattern and different `sparse` options: — https://www.mongodb.com/docs/manual/core/index-unique/#unique-and-sparse-index-creation · keywords: index, index build, index definition, sparse index, sparse
- [passage] Let's create an example `employees` table and demonstrate Hash index usage: — https://thenile.dev/docs/postgres/indexes#example-employees-table · keywords: B-tree, hashed index, index, unused index, employees
- [passage] Let's create an employee table and demonstrate BRIN index usage. — https://thenile.dev/docs/postgres/indexes#example-employee-table · keywords: B-tree, BRIN index, index, unused index
- [passage] The unique index permits the insertion of the following document into the collection if no other document in the collection has an index key value of `{ "email": "arya@winterfell.com", "name": null }`. — https://www.mongodb.com/docs/manual/core/index-unique/#unique-constraint-across-separate-documents · keywords: index, index definition, unique constraint, unique index, { "email": "arya@winterfell.com", "name": null }
- [passage] * **Length configuration** for MySQL indexes * **Sort order** configuration for indexes * **Additional index types** for PostgreSQL: * Hash * GIN * GiST * SP-GiST * BRIN * **Index clustering** for SQL Server — https://www.prisma.io/docs/guides/upgrade-prisma-orm/v4#1-improved-index-configuration-ga-1-improved-index-configuration-ga · keywords: BRIN index, GIN index, GiST index, index
- [passage] A unique index ensures that the indexed fields do not store duplicate values, and that a value appears at most once for a given field. A unique compound index ensures that any given combination of the index key values appears at most once. By default, MongoDB creates a unique index on the [_id](https://www.mongodb.com/docs/manual/core/document/#std-label-document-id-field) field during the creation of a collection. — https://www.mongodb.com/docs/manual/core/index-unique/#chunk-0 · keywords: compound index, index, index definition, unique index
- [passage] Basic and unique indexes can exist with the same [key pattern.](https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/#std-label-key_patterns) — https://www.mongodb.com/docs/manual/core/index-unique/#chunk-9 · keywords: index, index build, index definition, unique index
- [passage] - To learn how to create a multikey index on embedded document fields, see [Create an Index on an Embedded Field in an Array.](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-multikey/create-multikey-index-embedded/#std-label-index-create-multikey-embedded) - To learn about multikey index bounds, see [Multikey Index Bounds.](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-multikey/multikey-index-bounds/#std-label-indexes-multikey-bounds) — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-multikey/create-multikey-index-basic/#chunk-2 · keywords: index, index build, index definition, multikey index
- [passage] - Compound wildcard indexes are sparse indexes. - Documents are included in the index if they are missing the wildcard field but have one of the compound fields. - Index fields, including wildcard fields, can be sorted in ascending ( `1` ) or descending (`-1` ) order. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/index-wildcard-compound/#chunk-3 · keywords: index, index definition, sparse index, wildcard index,  ) or descending (
- [passage] he following form: The `<value>` can be either of the following: `1` or`true` to include the field in the wildcard index. `0` or`false` to exclude the field from the wildcard index. Wildcard indexes omit the `_id` field by default. To include the`_id` field in the wildcard index, you must explicitly include it in the`wildcardProjection` document: All of the statements in the `wildcardProjection` document must be either inclusion or exclusion statements. You can also include the`_id` field with exclusion statements. This is the only exception to the rule. Options specified to [`db.collection.createIndexes()`](https://www.mongodb.com#mongodb-method-db.collection.createIndexes) apply to**all** of the index specifications included in the key pattern array. Specify`wildcardProjection` only if you are creating a single wildcard index using`db.collection.createIndexes()` . | — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/#chunk-20 · keywords: index, index build, index definition, wildcard index, <value>,  or
- [passage] * Corresponding database construct: `UNIQUE` * A `@@unique` block is required if it represents the only unique constraint on a model without an `@id` / `@@id` * Adding a unique constraint automatically adds a corresponding *unique index* to the specified column(s) — https://www.prisma.io/docs/orm/v7/reference/prisma-schema-reference#relational-databases-relational-databases-4 · keywords: @@index, index, unique constraint, unique index, UNIQUE, @@unique
- [passage] > **Note**: The `where` argument accepts either `raw("SQL expression")` for raw SQL predicates or an object literal like `{ field: value }` for type-safe conditions. See [Configuring partial indexes](https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#configuring-partial-indexes-with-where) for details. — https://www.prisma.io/docs/orm/v7/reference/prisma-schema-reference#signature-signature-3 · keywords: index, partial index, @@index, where, raw("SQL expression"), { field: value }
- [passage] | Name | Required | Type | Description | | ----------- | -------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | | `fields` | **Yes** | `FieldReference[]` | A list of field names - for example, `["firstname", "lastname"]`. Fields must be mandatory - see remarks. | | `name` | **No** | `String` | The name of the unique combination of fields - defaults to `fieldName1_fieldName2_fieldName3` | | `map` | **No** | `String` | | | `length` | **No** | `number` | Allows you to specify a maximum length for the subpart of the value to be indexed.<br /><br />MySQL only. | | `sort` | **No** | `String` | Allows you to specify in what order the entries of the constraint are stored in the database. The available options are `Asc` and `Desc`. | | `clustered` | **No** | `Boolean` | Defines whether the constraint is clustered or non-clustered. Defaults to `false`.<br /><br />SQL Server only. | | `where` | **No** | `function` or `object` | Defines a [partial index](https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#configuring-partial-indexes-with-where) that only includes rows matching the specified condition. Accepts `raw("SQL expression")` or an object literal like `{ field: value }`.<br /><br />PostgreSQL, SQLite, SQL Server, and CockroachDB. Requires the `partialIndexes` Preview feature. | — https://www.prisma.io/docs/orm/v7/reference/prisma-schema-reference#arguments-arguments-4 · keywords: @@index, index, partial index, fields, FieldReference[], ["firstname", "lastname"]
- [passage] The `where` argument is available on the `@unique`, `@@unique` and `@@index` attributes. It requires the `partialIndexes` Preview feature. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#configuring-partial-indexes-with-where-configuring-partial-indexes-with-where · keywords: @@index, index, partial index, where, @unique, @@unique
- [passage] The default operator class (marked with ✅) can be omitted from the index definition. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#supported-operator-classes-for-gin-supported-operator-classes-for-gin · keywords: GIN index, index, index definition, GiST index, BRIN index
- [passage] Queries like `value = 2` will now use the index, which uses a fraction of the space used by the `BTree` or `Hash` indexes. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#block-range-index-brin-block-range-index-brin · keywords: B-tree, BRIN index, index, value = 2, BTree, Hash
- [passage] For example, a PostgreSQL partial unique index on a single field will be introspected as: — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#introspection-introspection · keywords: index, partial index, unique index
- [passage] A GiST index with `btree_gist` can be used for multi-column searches and exclusion constraints. Here's how to create one: — https://thenile.dev/docs/extensions/btree_gist#creating-a-gist-index-with-btree-gist · keywords: B-tree, GiST index, index, btree_gist
- [passage] * gin must be used as an index method. GiST is not available for pg\_bigm. * gin\_bigm\_ops must be used as an operator class. — https://thenile.dev/docs/extensions/pg_bigm#creating-a-gin-index · keywords: GIN index, GiST index, index
- [passage] Hash indexes use a hash function to map indexed column values to 32-bit hash codes. These indexes are optimized for simple equality comparisons (using the `=` operator). Here's how they work: — https://thenile.dev/docs/postgres/indexes#hash-index · keywords: B-tree, hashed index, index
- [passage] * **Purpose**: GiST indexes are designed to support various query types, including equality queries, range queries, and partial match queries. * **Infrastructure**: GiST provides an infrastructure within which different indexing strategies can be implemented. * [**Operator Classes**: The operators used with GiST indexes depend on the specific indexing strategy (operator class) chosen2](https://www.postgresql.org/docs/current/indexes-types.html). — https://thenile.dev/docs/postgres/indexes#gist-index · keywords: B-tree, GiST index, index
- [passage] Let's explore some examples using tables related to employees. We'll create a sample table, insert data, and demonstrate how GiST indexes work. — https://thenile.dev/docs/postgres/indexes#examples-of-gist-indexes · keywords: B-tree, GiST index, index
- [passage] Suppose we have an `employees` table with a `location` column representing the employees' office locations (stored as geometric points). We want to efficiently query employees based on their proximity to a specific location. — https://thenile.dev/docs/postgres/indexes#example-1-geometric-shapes · keywords: B-tree, GiST index, index, employees, location
- [passage] 3. **Create GiST Index**: — https://thenile.dev/docs/postgres/indexes#example-1-geometric-shapes · keywords: B-tree, GiST index, index
- [passage] Let's create an example employee table and demonstrate how to use SP-GiST indexes. — https://thenile.dev/docs/postgres/indexes#example-employee-table · keywords: B-tree, GiST index, index
- [passage] A GIN index is designed for efficiently handling composite data values, such as arrays or JSON objects. Here are the key points: — https://thenile.dev/docs/postgres/indexes#gin-index · keywords: B-tree, GIN index, index
- [passage] * Query using the GIN index: — https://thenile.dev/docs/postgres/indexes#gin-index · keywords: B-tree, GIN index, index
- [passage] A partial index includes only the rows that satisfy the `WHERE` clause. Partial indexes are smaller than full indexes and are more efficient for queries that always include the same filter condition. — https://docs.turso.tech/sql-reference/statements/create-index#partial-indexes · keywords: index, index build, partial index, WHERE
- [passage] The `WHERE` clause of a partial index can reference any column of the table and may use operators, literal values, and built-in functions. Subqueries are not allowed. — https://docs.turso.tech/sql-reference/statements/create-index#partial-indexes · keywords: index, index build, partial index, WHERE
- [passage] Each expression in the index must be a deterministic expression that references only columns of the indexed table. Aggregate functions and subqueries are not allowed. — https://docs.turso.tech/sql-reference/statements/create-index#expression-indexes · keywords: expression index, index, index build
- [passage] <Info> **Turso Extension**: Custom index methods extend indexing beyond B-trees. This feature is experimental and must be [enabled before use](/sql-reference/experimental-features). </Info> — https://docs.turso.tech/sql-reference/statements/create-index#custom-index-methods · keywords: B-tree, index, index build
- [passage] The server uses the index the same way, but you may notice a difference in the output of the [`listIndexes`](https://www.mongodb.com#mongodb-dbcommand-dbcmd.listIndexes) and [`db.collection.getIndexes()`](https://www.mongodb.com/docs/manual/reference/method/db.collection.getIndexes/#mongodb-method-db.collection.getIndexes) commands. — https://www.mongodb.com/docs/manual/reference/command/listIndexes/#wildcard-indexes · keywords: index, index build, wildcard index, listIndexes, db.collection.getIndexes() · also: https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/reference/wildcard-projection-signature/, https://www.mongodb.com/docs/manual/reference/method/db.collection.getIndexes/
- [passage] The following example creates a compound index on the `year`, `runtime`, and `title` fields: — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#create-an-index-on-multiple-fields · keywords: compound index, index, index build, year, runtime, title
- [passage] The following example creates a compound index on the `title` field (in ascending order) and the `runtime` field (hashed): — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#create-an-index-on-multiple-fields · keywords: compound index, index, index build, title, runtime
- [passage] For more information on hashed indexes, see [Hashed Indexes.](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-hashed/#std-label-index-type-hashed) — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#create-an-index-on-multiple-fields · keywords: hashed index, index, index build · also: https://www.mongodb.com/docs/manual/core/indexes/index-types/
- [passage] * Balanced performance between search and update * Smaller index size * Good for dynamic data — https://thenile.dev/docs/extensions/pg_trgm#gist-index · keywords: GiST index, index, index overhead
- [passage] <Note> The `libsql_vector_idx` marker function is **required** and used by libSQL to distinguish `ANN`-indices from ordinary B-Tree indices. </Note> </Step> — https://docs.turso.tech/features/ai-and-embeddings#index-usage · keywords: B-tree, index, unused index, libsql_vector_idx, ANN
- [passage] If you have a collection that has both a compound index and an index on its prefix (for example, `{ a: 1, b: 1 }` and `{ a: 1 }`), if neither index has a [sparse](https://www.mongodb.com/docs/manual/core/index-sparse/#std-label-index-type-sparse) or [unique](https://www.mongodb.com/docs/manual/core/index-unique/#std-label-index-type-unique) constraint, you can remove the index on the prefix (`{ a: 1 }`). MongoDB uses the compound index in all of the situations that it would have used the prefix index. — https://www.mongodb.com/docs/manual/core/index-compound/#remove-redundant-indexes · keywords: compound index, index, unused index, { a: 1, b: 1 }, { a: 1 }
- [passage] > **Note**: With the `partialIndexes` Preview feature, the `where` argument is available. Before this Preview feature, the signature was: > > ```prisma no-lines > @@index(_ fields: FieldReference[], map: String?) > ``` — https://www.prisma.io/docs/orm/v7/reference/prisma-schema-reference#signature-signature-5 · keywords: @@index, index, partial index, partialIndexes, where
- [passage] > [!NOTE] > Partial indexes are now supported in Prisma Schema Language via the `where` argument on `@@index`, `@@unique`, and `@unique`. See [Configuring partial indexes](https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#configuring-partial-indexes-with-where) for details. You no longer need to customize migrations for partial indexes. > > The Prisma schema is also able to represent [unsupported field types](https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/unsupported-database-features#unsupported-field-types) and [native database functions](https://www.prisma.io/docs/orm/v7/prisma-migrate/workflows/native-database-functions). — https://www.prisma.io/docs/orm/v7/prisma-migrate/workflows/unsupported-database-features#unsupported-database-features-prisma-migrate-docs-orm-v7-prisma-migrate-workflows-unsupported-database-features · keywords: @@index, index, partial index, where, @@unique, @unique
- [passage] The `clustered` argument is available to configure (non)clustered indexes in SQL Server. It can be used on the `@id`, `@@id`, `@unique`, `@@unique` and `@@index` attributes. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#configuring-if-indexes-are-clustered-or-non-clustered-with-clustered-sql-server-configuring-if-indexes-are-clustered-or-non-clustered-with-clustered-sql-server · keywords: @@index, clustered index, index, clustered, @id, @@id
- [passage] > [!NOTE] > Partial indexes are now supported in Prisma Schema Language via the `where` argument on `@@index`, `@@unique`, and `@unique`. See [Configuring partial indexes](https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#configuring-partial-indexes-with-where) for details. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/unsupported-database-features#unsupported-database-features-unsupported-database-features · keywords: @@index, index, partial index, where, @@unique, @unique
- [passage] | Entity | Convention | Example | | ----------------- | ------------------------------------ | ------------------------------ | | Primary Key | {tablename}_pkey | `User_pkey` | | Unique Constraint | {tablename}_{column_names}_key | `User_firstName_last_Name_key` | | Non-Unique Index | {tablename}_{column_names}_idx | `User_age_idx` | | Foreign Key | {tablename}_{column_names}_fkey | `User_childName_fkey` | — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/database-mapping#prisma-orm-s-default-naming-conventions-for-indexes-and-constraints-prisma-orms-default-naming-conventions-for-indexes-and-constraints · keywords: index, index definition, unique constraint, User_pkey, User_firstName_last_Name_key, User_age_idx
- [passage] The unique index prevents different documents in the collection from having the same value for the indexed key. — https://www.mongodb.com/docs/manual/core/index-unique/#unique-constraint-across-separate-documents · keywords: index, unique constraint, unique index
- [passage] For example, create a unique compound multikey index on `email` and `name`: — https://www.mongodb.com/docs/manual/core/index-unique/#unique-constraint-across-separate-documents · keywords: index, multikey index, unique constraint, email, name
- [fact] MongoDB\Model\IndexInfo methods allow checking if an index is a 2dSphere, sparse, text, TTL, or unique index. — https://www.mongodb.com/docs/php-library/current/llms.txt · keywords: geospatial index, index, unique index
- [passage] The GiST index type is used for implementing indexing schemes for user-defined types. By default there are not many direct uses for GiST indexes, but for example the B-Tree index type is built using a GiST index. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#generalized-search-tree-gist-generalized-search-tree-gist · keywords: B-tree, GiST index, index
- [passage] export const stores = pgTable( 'stores', { id: serial('id').primaryKey(), name: text('name').notNull(), location: geometry('location', { type: 'point', mode: 'xy', srid: 4326 }).notNull(), }, (t) => [ index('spatial_index').using('gist', t.location), ] ); ``` </CodeTab> ```sql CREATE TABLE IF NOT EXISTS "stores" ( "id" serial PRIMARY KEY NOT NULL, "name" text NOT NULL, "location" geometry(point) NOT NULL ); --> statement-breakpoint CREATE INDEX IF NOT EXISTS "spatial_index" ON "stores" USING gist ("location"); ``` </CodeTabs> — https://orm.drizzle.team/docs/guides/postgis-geometry-point · keywords: GiST index, index, index build,  </CodeTab> 
- [passage] // custom lower function export function lower(email: AnyPgColumn): SQL { return sql`lower(${email})`; } ``` </CodeTab> ```sql CREATE TABLE IF NOT EXISTS "users" ( "id" serial PRIMARY KEY NOT NULL, "name" text NOT NULL, "email" text NOT NULL ); --> statement-breakpoint CREATE UNIQUE INDEX IF NOT EXISTS "emailUniqueIndex" ON "users" USING btree (lower("email")); ``` </CodeTabs> — https://orm.drizzle.team/docs/guides/unique-case-insensitive-email#postgresql · keywords: B-tree, index, unique index, lower(${email}),  </CodeTab> 
- [passage] Enables GIN indexes to support B-tree indexable data types. — https://thenile.dev/docs/extensions/btree_gin · keywords: B-tree, GIN index, index
- [passage] The `btree_gin` extension enhances GIN indexes by allowing them to handle B-tree indexable data types efficiently. It is particularly useful for **multi-column indexing** scenarios where a mix of text, integer, and timestamp fields are queried together. — https://thenile.dev/docs/extensions/btree_gin#conclusion · keywords: B-tree, GIN index, index, btree_gin
- [passage] Allows GiST indexes to support B-tree indexable data types. — https://thenile.dev/docs/extensions/btree_gist · keywords: B-tree, GiST index, index
- [passage] The [btree\_gist](https://www.postgresql.org/docs/current/btree-gist.html) extension in PostgreSQL allows GiST indexes to support B-tree indexable data types. It is useful for indexing columns that typically use B-tree indexes but require additional GiST-specific features such as **multicolumn indexing**, **range queries**, and **support for exclusion constraints**. Your Nile database arrives with `btree_gist` extension already enabled, so there's no need to run `create extension`. — https://thenile.dev/docs/extensions/btree_gist · keywords: B-tree, GiST index, index, btree_gist, create extension
- [passage] The `btree_gist` extension enhances GiST indexes by allowing them to handle B-tree indexable data types efficiently. It is particularly useful for **multi-column indexing**, **range queries**, and **exclusion constraints**. — https://thenile.dev/docs/extensions/btree_gist#conclusion · keywords: B-tree, GiST index, index, btree_gist
- [passage] * GIN indexes are typically better for exact matches and contained-by queries * GiST indexes are better for overlap queries but may be less precise * Array operations are performed in memory, so be cautious with very large arrays * Sorting and uniqueness operations (`sort`, `uniq`) create new arrays — https://thenile.dev/docs/extensions/intarray#performance-considerations · keywords: GIN index, GiST index, index, sort, uniq
- [passage] <Card title="Btree_Gin" href="./btree_gin" icon="martini-glass-citrus"> GIN index support for B-tree indexable data types. </Card> — https://thenile.dev/docs/extensions/introduction#search-and-indexing · keywords: B-tree, GIN index, index
- [passage] <Card title="Btree_Gist" href="./btree_gist" icon="tree"> GiST index support for B-tree indexable data types. </Card> — https://thenile.dev/docs/extensions/introduction#search-and-indexing · keywords: B-tree, GiST index, index
- [passage] 1. **Index Selection**: * Use GIN for mostly-read data * Use GiST for frequently updated data * Consider creating indexes only on frequently searched columns — https://thenile.dev/docs/extensions/pg_trgm#best-practices · keywords: GIN index, GiST index, index
- [passage] 1. **Indexing**: Use GIN or BTREE indexes for frequent lookups by `tenant_id` and specific keys. 2. **Normalization**: Use `jsonb` for semi-structured data. For structured data, use regular columns and normalize the schema. 3. **Use UUID for `tenant_id`**: Ensure the `tenant_id` is a UUID to uniquely identify tenants, providing clear data isolation. — https://thenile.dev/docs/postgres/datatype/json#best-practices-for-jsonb-with-multi-tenancy · keywords: B-tree, GIN index, index, tenant_id, jsonb
- [passage] Compound indexes collect and sort data from multiple field values from each document in a collection. You can use the compound index to query the first field or any prefix fields of the index. The order of fields in a compound index is very important. The B-tree created by a compound index stores the sorted data in the order that the index specifies the fields. — https://www.mongodb.com/docs/manual/core/index-compound/#chunk-0 · keywords: B-tree, compound index, index
- [passage] - the index key specification document to the [`db.collection.hideIndex()`](https://www.mongodb.com/docs/manual/reference/method/db.collection.hideIndex/#mongodb-method-db.collection.hideIndex) method:db.restaurants.hideIndex( { borough: 1, ratings: 1 } ); // Specify the index key specification document - the index name to the [`db.collection.hideIndex()`](https://www.mongodb.com/docs/manual/reference/method/db.collection.hideIndex/#mongodb-method-db.collection.hideIndex) method:db.restaurants.hideIndex( "borough_1_ratings_1" ); // Specify the index name — https://www.mongodb.com/docs/manual/core/index-hidden/#chunk-5 · keywords: hidden index, index, index definition, db.collection.hideIndex()
- [passage] - the index key specification document to the [`db.collection.unhideIndex()`](https://www.mongodb.com/docs/manual/reference/method/db.collection.unhideIndex/#mongodb-method-db.collection.unhideIndex) method:db.restaurants.unhideIndex( { borough: 1, city: 1 } ); // Specify the index key specification document - the index name to the [`db.collection.unhideIndex()`](https://www.mongodb.com/docs/manual/reference/method/db.collection.unhideIndex/#mongodb-method-db.collection.unhideIndex) method:db.restaurants.unhideIndex( "borough_1_ratings_1" ); // Specify the index name — https://www.mongodb.com/docs/manual/core/index-hidden/#chunk-7 · keywords: hidden index, index, index definition, db.collection.unhideIndex()
- [passage] Given an indexed array field, consider a query that specifies multiple query predicates on the array and uses a [multikey index](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-multikey/#std-label-index-type-multikey) to fulfill the query. MongoDB can intersect the multikey index bounds if an [`$elemMatch`](https://www.mongodb.com/docs/manual/reference/operator/query/elemMatch/#mongodb-query-op.-elemMatch) operator joins the query predicates. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-multikey/multikey-index-bounds/#chunk-1 · keywords: index, index definition, multikey index, $elemMatch
- [passage] MongoDB supports creating wildcard indexes on a field or a set of fields. A compound index has multiple index terms. A compound wildcard index has one wildcard term and one or more additional index terms. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/index-wildcard-compound/#chunk-0 · keywords: compound index, index, wildcard index
- [passage] The index key pattern is `"$**"`. You can create another wildcard index with the same key pattern if you specify a different `wildcardProjection`. For example: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/reference/wildcard-projection-signature/#example · keywords: index, index definition, wildcard index, "$**", wildcardProjection
- [passage] Although you can have a unique [compound index](https://www.mongodb.com/docs/manual/reference/glossary/#std-term-compound-index) where the shard key is a [prefix](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-compound/#std-label-compound-index-prefix), if using `unique` parameter, the collection must have a unique index that is on the shard key. — https://www.mongodb.com/docs/manual/core/sharding-shard-key-indexes/#chunk-5 · keywords: compound index, index, unique index, unique
- [passage] If your application performs queries on both a single key and multiple keys, a [compound index](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-compound/#std-label-index-type-compound) is more efficient than a single-key index. For example, you can create an index on the `year`, `runtime`, and `title` fields: — https://www.mongodb.com/docs/manual/data-modeling/schema-design-process/create-indexes/#chunk-3 · keywords: compound index, index, index build, year, runtime, title
- [passage] - `listIndexes`- Returns information about the indexes on the specified collection, including [hidden indexes](https://www.mongodb.com/docs/manual/core/index-hidden/#std-label-index-type-hidden) and indexes that are currently being built. Returned index information includes the keys and options used to create the index. You can optionally set the batch size for the first batch of results.## TipIn [`mongosh`](https://www.mongodb.com/docs/mongodb-shell/#std-program-mongosh) , this command can also be run through the[`db.collection.getIndexes()`](https://www.mongodb.com/docs/manual/reference/method/db.collection.getIndexes/#mongodb-method-db.collection.getIndexes) helper method.Helper methods are convenient for [`mongosh`](https://www.mongodb.com/docs/mongodb-shell/#std-program-mongosh) users, but they may not return the same level of information as database commands. In cases where the convenience is not needed or the additional return fields are required, use the database command. — https://www.mongodb.com/docs/manual/reference/command/listIndexes/#definition · keywords: hidden index, index, index build, listIndexes, mongosh, db.collection.getIndexes()
- [passage] If the options specification had been split into multiple documents like this: `{ unique: true }, { sparse: true, expireAfterSeconds: 3600 }` the index creation operation would have failed. — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#options · keywords: TTL index, index, index build, { unique: true }, { sparse: true, expireAfterSeconds: 3600 }
- [passage] If the `keys` document specifies more than one field, then [`createIndex()`](https://www.mongodb.com#mongodb-method-db.collection.createIndex) creates a [compound index.](https://www.mongodb.com/docs/manual/reference/glossary/#std-term-compound-index) — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#create-an-ascending-index-on-a-single-field · keywords: compound index, index, index build, keys, createIndex()
- [passage] ust be unique, you may only specify name if you are creating a single index using`db.collection.createIndexes()` . | | `partialFilterExpression` | document | Optional. If specified, the indexes only reference documents that match the filter expression. See [Partial Indexes](https://www.mongodb.com/docs/manual/core/index-partial/#std-label-index-type-partial) for more information. A filter expression can include: equality expressions (i.e. `field: value` or using the[`$eq`](https://www.mongodb.com/docs/manual/reference/operator/query/eq/#mongodb-query-op.-eq) operator) [`$exists: true`](https://www.mongodb.com/docs/manual/reference/operator/query/exists/#mongodb-query-op.-exists) expression [`$type`](https://www.mongodb.com/docs/manual/reference/operator/query/type/#mongodb-query-op.-type) expressions [`$and`](https://www.mongodb.com/docs/manual/reference/operator/query/and/#mongodb-query-op.-and) operator [`$or`](https://www.mongodb.com/docs/manual/reference/operator/query/or/#mongodb-query-op.-or) operator [`$in`](https://www.mongodb.com/docs/manual/reference/operator/query/in/#mongodb-query-op.-in) operator [`$geoWithin`](https://www.mongodb.com/docs/manual/reference/operator/q — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/#chunk-7 · keywords: index, index build, partial index, db.collection.createIndexes(), partialFilterExpression, field: value
- [passage] The following lists examples of wildcard index creation: — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/#chunk-29 · keywords: index, index build, wildcard index
- [passage] - `db.collection.getIndexes()`- Returns an array that holds a list of documents that identify and describe the existing indexes on the collection, including [hidden indexes](https://www.mongodb.com/docs/manual/core/index-hidden/#std-label-index-type-hidden) and indexes that are currently being built.You must call [`db.collection.getIndexes()`](https://www.mongodb.com#mongodb-method-db.collection.getIndexes) on a collection. For example:db.collection.getIndexes() Change `collection` to the name of the collection for which to return index information. — https://www.mongodb.com/docs/manual/reference/method/db.collection.getIndexes/#definition · keywords: hidden index, index, index build, db.collection.getIndexes(), collection
- [passage] You therefore create a compound multikey unique index with the following specification and options: — https://www.mongodb.com/docs/manual/tutorial/unique-indexes-schema-validation/#create-a-multi-property-index · keywords: index, multikey index, unique index
- [passage] **3. Practical Examples:** Let's create an example `employees` table and demonstrate B-tree index usage: — https://thenile.dev/docs/postgres/indexes#b-tree-index · keywords: B-tree, index, unused index, employees
- [passage] ](https://www.mongodb.com/docs/manual/reference/operator/aggregation/indexStats/#mongodb-pipeline-pipe.-indexStats) - Hiding an unhidden index or unhiding a hidden index resets its [`$indexStats`](https://www.mongodb.com/docs/manual/reference/operator/aggregation/indexStats/#mongodb-pipeline-pipe.-indexStats) . Hiding an already hidden index or unhiding an already unhidden index does not reset the[`$indexStats`.](https://www.mongodb.com/docs/manual/reference/operator/aggregation/indexStats/#mongodb-pipeline-pipe.-indexStats) — https://www.mongodb.com/docs/manual/core/index-hidden/#chunk-2 · keywords: hidden index, index, unused index, $indexStats
- [passage] * Corresponding database construct: `UNIQUE` * `NULL` values are considered to be distinct (multiple rows with `NULL` values in the same column are allowed) * Adding a unique constraint automatically adds a corresponding *unique index* to the specified column(s). — https://www.prisma.io/docs/orm/v7/reference/prisma-schema-reference#relational-databases-relational-databases-3 · keywords: index, unique constraint, unique index, UNIQUE, NULL
- [passage] That is why Drizzle always treats any `.unique()` as `UNIQUE INDEX` — https://orm.drizzle.team/docs/mysql/indexes-constraints#unique · keywords: index, unique constraint, unique index, .unique(), UNIQUE INDEX
- [passage] If you use both the `partialFilterExpression` and a unique constraint, the unique constraint only applies to documents that meet the filter expression. For an example, see [Partial Index with Unique Constraint.](https://www.mongodb.com/docs/manual/core/index-partial/#std-label-partial-index-with-unique-constraints) — https://www.mongodb.com/docs/manual/core/index-unique/#unique-partial-indexes · keywords: index, partial index, unique constraint, partialFilterExpression
- [passage] An indexed field can define the operator class, which defines the operators handled by the index. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#generalized-inverted-index-gin-generalized-inverted-index-gin · keywords: GIN index, index
- [passage] Prisma ORM generally supports operator classes provided by PostgreSQL in versions 10 and later. If the operator class requires the field type to be of a type Prisma ORM does not yet support, using the `raw` function with a string input allows you to use these operator classes without validation. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#supported-operator-classes-for-gin-supported-operator-classes-for-gin · keywords: GIN index, index, GiST index, raw
- [passage] | Operator class | Allowed field type (native types) | Default | Other | | -------------- | --------------------------------- | ------- | ----------------------------- | | `ArrayOps` | Any array | ✅ | Also available in CockroachDB | | `JsonbOps` | `Json` (`@db.JsonB`) | ✅ | Also available in CockroachDB | | `JsonbPathOps` | `Json` (`@db.JsonB`) | | | | `raw("other")` | | | | — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#supported-operator-classes-for-gin-supported-operator-classes-for-gin · keywords: GIN index, index, ArrayOps, JsonbOps, Json, @db.JsonB
- [passage] Read more about built-in operator classes in the [official PostgreSQL documentation](https://www.postgresql.org/docs/14/gin-builtin-opclasses.html). — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#supported-operator-classes-for-gin-supported-operator-classes-for-gin · keywords: GIN index, index
- [passage] Queries comparing IP addresses, such as `value > '10.0.0.2'`, will use the index. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#generalized-search-tree-gist-generalized-search-tree-gist · keywords: GiST index, index, value > '10.0.0.2'
- [passage] | Operator class | Allowed field type (allowed native types) | | -------------- | ----------------------------------------- | | `InetOps` | `String` (`@db.Inet`) | | `raw("other")` | | — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#supported-operator-classes-for-gist-supported-operator-classes-for-gist · keywords: GiST index, index, InetOps, String, @db.Inet, raw("other")
- [passage] Read more about built-in operator classes in the [official PostgreSQL documentation](https://www.postgresql.org/docs/14/gist-builtin-opclasses.html). — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#supported-operator-classes-for-gist-supported-operator-classes-for-gist · keywords: GiST index, index
- [passage] As with GiST, SP-GiST is important as a building block for user-defined types, allowing implementation of custom search operators directly with the database. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#space-partitioned-gist-sp-gist-space-partitioned-gist-sp-gist · keywords: GiST index, index
- [passage] As an example, the following model adds a `SpGist` index to the `value` field with `TextOps` as the operators using the index: — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#space-partitioned-gist-sp-gist-space-partitioned-gist-sp-gist · keywords: GiST index, index, SpGist, value, TextOps
- [passage] Queries such as `value LIKE 'something%'` will be sped up by the index. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#space-partitioned-gist-sp-gist-space-partitioned-gist-sp-gist · keywords: GiST index, index, value LIKE 'something%'
- [passage] | Operator class | Allowed field type (native types) | Default | Supported PostgreSQL versions | | -------------- | ------------------------------------ | ------- | ----------------------------- | | `InetOps` | `String` (`@db.Inet`) | ✅ | 10+ | | `TextOps` | `String` (`@db.Text`, `@db.VarChar`) | ✅ | | | `raw("other")` | | | | — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#supported-operator-classes-for-sp-gist-supported-operator-classes-for-sp-gist · keywords: GiST index, index, InetOps, String, @db.Inet, TextOps
- [passage] Read more about built-in operator classes from [official PostgreSQL documentation](https://www.postgresql.org/docs/14/spgist-builtin-opclasses.html). — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#supported-operator-classes-for-sp-gist-supported-operator-classes-for-sp-gist · keywords: GiST index, index
- [passage] Prisma ORM generally supports operator classes provided by PostgreSQL in versions 10 and later, and some supported operators are only available from PostgreSQL versions 14 and later. If the operator class requires the field type to be of a type Prisma ORM does not yet support, using the `raw` function with a string input allows you to use these operator classes without validation. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#supported-operator-classes-for-brin-supported-operator-classes-for-brin · keywords: BRIN index, index, raw
- [passage] | Operator class | Allowed field type (native types) | Default | Supported PostgreSQL versions | | --------------------------- | ------------------------------------ | ------- | ----------------------------- | | `BitMinMaxOps` | `String` (`@db.Bit`) | ✅ | | | `VarBitMinMaxOps` | `String` (`@db.VarBit`) | ✅ | | | `BpcharBloomOps` | `String` (`@db.Char`) | | 14+ | | `BpcharMinMaxOps` | `String` (`@db.Char`) | ✅ | | | `ByteaBloomOps` | `Bytes` (`@db.Bytea`) | | 14+ | | `ByteaMinMaxOps` | `Bytes` (`@db.Bytea`) | ✅ | | | `DateBloomOps` | `DateTime` (`@db.Date`) | | 14+ | | `DateMinMaxOps` | `DateTime` (`@db.Date`) | ✅ | | | `DateMinMaxMultiOps` | `DateTime` (`@db.Date`) | | 14+ | | `Float4BloomOps` | `Float` (`@db.Real`) | | 14+ | | `Float4MinMaxOps` | `Float` (`@db.Real`) | ✅ | | | `Float4MinMaxMultiOps` | `Float` (`@db.Real`) | | 14+ | | `Float8BloomOps` | `Float` (`@db.DoublePrecision`) | | 14+ | | `Float8MinMaxOps` | `Float` (`@db.DoublePrecision`) | ✅ | | | `Float8MinMaxMultiOps` | `Float` (`@db.DoublePrecision`) | | 14+ | | `InetInclusionOps` | `String` (`@db.Inet`) | ✅ | 14+ | | `InetBloomOps` | `String` (`@db.Inet`) | | 14+ | | `InetMinMaxOps` | `String` (`@db.Inet`) | | | | `InetMinMaxMultiOps` | `String` (`@db.Inet`) | | 14+ | | `Int2BloomOps` | `Int` (`@db.SmallInt`) | | 14+ | | `Int2MinMaxOps` | `Int` (`@db.SmallInt`) | ✅ | | | `Int2MinMaxMultiOps` | `Int` (`@db.SmallInt`) | | 14+ | | `Int4BloomOps` | `Int` (`@db.Integer`) | | 14+ | | `Int4MinMaxOps` | `Int` (`@db.Integer`) | ✅ | | | `Int4MinMaxMultiOps` | `Int` (`@db.Integer`) | | 14+ | | `Int8BloomOps` | `BigInt` (`@db.BigInt`) | | 14+ | | `Int8MinMaxOps` | `BigInt` (`@db.BigInt`) | ✅ | | | `Int8MinMaxMultiOps` | `BigInt` (`@db.BigInt`) | | 14+ | | `NumericBloomOps` | `Decimal` (`@db.Decimal`) | | 14+ | | `NumericMinMaxOps` | `Decimal` (`@db.Decimal`) | ✅ | | | `NumericMinMaxMultiOps` | `Decimal` (`@db.Decimal`) | | 14+ | | `OidBloomOps` | `Int` (`@db.Oid`) | | 14+ | | `OidMinMaxOps` | `Int` (`@db.Oid`) | ✅ | | | `OidMinMaxMultiOps` | `Int` (`@db.Oid`) | | 14+ | | `TextBloomOps` | `String` (`@db.Text`, `@db.VarChar`) | | 14+ | | `TextMinMaxOps` | `String` (`@db.Text`, `@db.VarChar`) | ✅ | | | `TextMinMaxMultiOps` | `String` (`@db.Text`, `@db.VarChar`) | | 14+ | | `TimestampBloomOps` | `DateTime` (`@db.Timestamp`) | | 14+ | | `TimestampMinMaxOps` | `DateTime` (`@db.Timestamp`) | ✅ | | | `TimestampMinMaxMultiOps` | `DateTime` (`@db.Timestamp`) | | 14+ | | `TimestampTzBloomOps` | `DateTime` (`@db.Timestamptz`) | | 14+ | | `TimestampTzMinMaxOps` | `DateTime` (`@db.Timestamptz`) | ✅ | | | `TimestampTzMinMaxMultiOps` | `DateTime` (`@db.Timestamptz`) | | 14+ | | `TimeBloomOps` | `DateTime` (`@db.Time`) | | 14+ | | `TimeMinMaxOps` | `DateTime` (`@db.Time`) | ✅ | | | `TimeMinMaxMultiOps` | `DateTime` (`@db.Time`) | | 14+ | | `TimeTzBloomOps` | `DateTime` (`@db.Timetz`) | | 14+ | | `TimeTzMinMaxOps` | `DateTime` (`@db.Timetz`) | ✅ | | | `TimeTzMinMaxMultiOps` | `DateTime` (`@db.Timetz`) | | 14+ | | `UuidBloomOps` | `String` (`@db.Uuid`) | | 14+ | | `UuidMinMaxOps` | `String` (`@db.Uuid`) | ✅ | | | `UuidMinMaxMultiOps` | `String` (`@db.Uuid`) | | 14+ | | `raw("other")` | | | | — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#supported-operator-classes-for-brin-supported-operator-classes-for-brin · keywords: BRIN index, index, BitMinMaxOps, String, @db.Bit, VarBitMinMaxOps
- [passage] Read more about built-in operator classes in the [official PostgreSQL documentation](https://www.postgresql.org/docs/14/brin-builtin-opclasses.html). — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#supported-operator-classes-for-brin-supported-operator-classes-for-brin · keywords: BRIN index, index
- [passage] | Value type | Example | Notes | | ---------------- | ---------------------------------------- | ------------------------------------------------------ | | `Boolean` | `{ active: true }`, `{ deleted: false }` | For `Boolean` fields | | `String` | `{ status: "active" }` | For `String`, `DateTime`, and `Enum` fields | | `Number` | `{ priority: 1 }`, `{ score: 1.5 }` | For `Int`, `BigInt`, `Float`, and `Decimal` fields | | `null` | `{ deletedAt: null }` | Translates to `IS NULL`. Works with any nullable field | | `{ not: value }` | `{ deletedAt: { not: null } }` | Negation. Translates to `IS NOT NULL` or `!= value` | — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#object-literal-syntax-type-safe-alternative-object-literal-syntax-type-safe-alternative · keywords: index, partial index, Boolean, { active: true }, { deleted: false }, String
- [passage] The `where` argument can be combined with other index arguments such as `name` and `map`: — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#using-where-with-other-index-arguments-using-where-with-other-index-arguments · keywords: index, partial index, where, name, map
- [passage] > [!NOTE] > The introspected `raw()` string reflects the database's normalized form of the SQL expression, which may differ from what you originally wrote. For example, PostgreSQL adds parentheses and explicit type casts (e.g., `'active'::text`), SQL Server wraps column names in brackets and adds parentheses (e.g., `([status]='active')`), while SQLite generally preserves the original expression as-is. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#introspection-introspection · keywords: index, partial index, raw(), 'active'::text, ([status]='active')
- [passage] This index allows fast searches for keys and values within `jsonb` fields for tenants: — https://thenile.dev/docs/postgres/datatype/json#gin-index-generalized-inverted-index · keywords: GIN index, index, jsonb
- [passage] For a ranged sharded collection, only the following indexes can be [unique:](https://www.mongodb.com#std-label-index-type-unique) — https://www.mongodb.com/docs/manual/core/index-unique/#sharded-clusters-and-unique-indexes · keywords: index, unique index · also: https://www.mongodb.com/docs/manual/core/sharding-shard-key-indexes/
- [passage] If you frequently query certain document fields, you can specify those fields in a `wildcardProjection` to support those queries without adding unnecessary bloat to the index. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/create-wildcard-index-multiple-fields/#include-specific-fields-in-a-wildcard-index · keywords: index, wildcard index, wildcardProjection
- [passage] The created index can support queries on any arbitrary field within documents in the collection: — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#create-a-wildcard-index-on-all-field-paths · keywords: index, wildcard index
- [passage] The wildcard pattern `"$**"` includes all of the fields in the document. However, the `wildcardProjection` field excludes the specified fields from the index. — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#omit-specific-fields-from-wildcard-index-coverage · keywords: index, wildcard index, "$**", wildcardProjection · also: https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/create-wildcard-index-multiple-fields/, https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/
- [passage] For complete documentation on `wildcardProjection`, see [Options for `wildcard` indexes.](https://www.mongodb.com#std-label-createIndex-method-wildcard-option) — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#omit-specific-fields-from-wildcard-index-coverage · keywords: index, wildcard index, wildcardProjection, wildcard · also: https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/
- [passage] For example, in MySQL/MariaDB, the following table using a descending unique constraint: — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#configuring-the-index-sort-order-with-sort-configuring-the-index-sort-order-with-sort · keywords: index, unique constraint
- [passage] The `map` argument can also be used on unique constraints: — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#configuring-the-name-of-indexes-with-map-configuring-the-name-of-indexes-with-map · keywords: index, unique constraint, map
- [passage] export const user = cockroachTable('user', { id: int4().unique(), }); — https://orm.drizzle.team/docs/cockroach/indexes-constraints#unique · keywords: index, unique constraint
- [passage] export const table = cockroachTable('table', { id: int4().unique('custom_name'), }); — https://orm.drizzle.team/docs/cockroach/indexes-constraints#unique · keywords: index, unique constraint
- [passage] export const composite = cockroachTable('composite_example', { id: int4(), name: string(), }, (t) => [ unique().on(t.id, t.name), unique('custom_name').on(t.id, t.name) ]); — https://orm.drizzle.team/docs/cockroach/indexes-constraints#unique · keywords: index, unique constraint
- [passage] export const user = mssqlTable('user', { id: int().unique(), }); — https://orm.drizzle.team/docs/mssql/indexes-constraints#unique · keywords: index, unique constraint
- [passage] export const table = mssqlTable('table', { id: int().unique('custom_name_1'), }); — https://orm.drizzle.team/docs/mssql/indexes-constraints#unique · keywords: index, unique constraint · also: https://orm.drizzle.team/docs/mysql/indexes-constraints
- [passage] export const composite = mssqlTable('composite_example', { id: int(), name: varchar({ length: 256 }), }, (t) => [ unique().on(t.id, t.name), unique('custom_name_2').on(t.id, t.name) ]); ``` — https://orm.drizzle.team/docs/mssql/indexes-constraints#unique · keywords: index, unique constraint · also: https://orm.drizzle.team/docs/mysql/indexes-constraints
- [passage] ```sql CREATE TABLE [user] ( [id] int, CONSTRAINT [user_id_key] UNIQUE([id]) ); — https://orm.drizzle.team/docs/mssql/indexes-constraints#unique · keywords: index, unique constraint · also: https://orm.drizzle.team/docs/cockroach/indexes-constraints, https://orm.drizzle.team/docs/mysql/indexes-constraints, https://orm.drizzle.team/docs/pg/indexes-constraints, https://orm.drizzle.team/docs/singlestore/indexes-constraints
- [passage] export const user = mysqlTable('user', { id: int().unique(), }); — https://orm.drizzle.team/docs/mysql/indexes-constraints#unique · keywords: index, unique constraint
- [passage] export const user = pgTable('user', { id: integer().unique(), }); — https://orm.drizzle.team/docs/pg/indexes-constraints#unique · keywords: index, unique constraint
- [passage] export const table = pgTable('table', { id: integer().unique('custom_name'), }); — https://orm.drizzle.team/docs/pg/indexes-constraints#unique · keywords: index, unique constraint
- [passage] export const composite = pgTable('composite_example', { id: integer(), name: text(), }, (t) => [ unique().on(t.id, t.name), unique('custom_name').on(t.id, t.name) ]); — https://orm.drizzle.team/docs/pg/indexes-constraints#unique · keywords: index, unique constraint
- [passage] // In Postgres 15.0+ NULLS NOT DISTINCT is available // This example demonstrates both available usages export const userNulls = pgTable("user_nulls_example", { id: integer(), id2: integer().unique("custom_name", { nulls: "not distinct" }), }, (t) => [ unique().on(t.id).nullsNotDistinct() ]); ``` — https://orm.drizzle.team/docs/pg/indexes-constraints#unique · keywords: index, unique constraint
- [passage] export const user = singlestoreTable('user', { id: int('id').unique(), }); — https://orm.drizzle.team/docs/singlestore/indexes-constraints#unique · keywords: index, unique constraint
- [passage] export const table = singlestoreTable('table', { id: int('id').unique('custom_name'), }); — https://orm.drizzle.team/docs/singlestore/indexes-constraints#unique · keywords: index, unique constraint
- [passage] export const composite = singlestoreTable('composite_example', { id: int('id'), name: varchar('name', { length: 256 }), }, (t) => [ unique().on(t.id, t.name), unique('custom_name').on(t.id, t.name) ]); ``` — https://orm.drizzle.team/docs/singlestore/indexes-constraints#unique · keywords: index, unique constraint
- [passage] export const user = sqliteTable('user', { id: int('id').unique(), }); — https://orm.drizzle.team/docs/sqlite/indexes-constraints#unique · keywords: index, unique constraint
- [passage] export const table = sqliteTable('table', { id: int('id').unique('custom_name'), }); — https://orm.drizzle.team/docs/sqlite/indexes-constraints#unique · keywords: index, unique constraint
- [passage] export const composite = sqliteTable('composite_example', { id: int('id'), name: text('name'), }, (t) => [ unique().on(t.id, t.name), unique('custom_name').on(t.id, t.name) ]); ``` — https://orm.drizzle.team/docs/sqlite/indexes-constraints#unique · keywords: index, unique constraint
- [concept] Compound indexes allow indexing on multiple fields simultaneously. — https://www.mongodb.com/docs/ruby-driver/current/llms.txt · keywords: compound index, index · also: https://www.mongodb.com/docs/languages/scala/scala-driver/current/llms.txt
- [concept] Geospatial indexes support queries involving location-based data. — https://www.mongodb.com/docs/ruby-driver/current/llms.txt · keywords: geospatial index, index
- [concept] Multikey indexes are used for arrays of values within documents. — https://www.mongodb.com/docs/ruby-driver/current/llms.txt · keywords: index, multikey index · also: https://www.mongodb.com/docs/languages/scala/scala-driver/current/llms.txt
- [concept] Clustered collections utilize clustered indexes to define the physical order of documents within a collection. — https://www.mongodb.com/docs/manual/manual-1-llms.txt · keywords: clustered index, index
- [concept] Wildcard indexes support queries on arbitrary or unknown fields in collections with flexible schemas using the `$**` specifier. — https://www.mongodb.com/docs/manual/manual-1-llms.txt · keywords: index, wildcard index, $**
- [concept] The `create` command creates collections or views in MongoDB with options for capped collections, time series, and clustered indexes. — https://www.mongodb.com/docs/manual/manual-3-llms.txt · keywords: clustered index, index, create
- [concept] The $toHashedIndexKey operator computes hash values in an aggregation pipeline using the same hash function as MongoDB's hashed index but without collation. — https://www.mongodb.com/docs/manual/manual-7-llms.txt · keywords: hashed index, index
- [concept] The $toHashedIndexKey operator uses the same hash function as MongoDB's hashed index but operates without collation. — https://www.mongodb.com/docs/manual/manual-7-llms.txt · keywords: hashed index, index
- [passage] In Prisma v6, the `UNIQUE INDEX` is changing into a `PRIMARY KEY`: — https://www.prisma.io/docs/guides/upgrade-prisma-orm/v6#schema-change-for-implicit-m-n-relations-on-postgresql-schema-change-for-implicit-m-n-relations-on-postgresql · keywords: index, unique index, UNIQUE INDEX, PRIMARY KEY
- [passage] The output reports the planned operations: creating the `users` and `posts` collections and a unique index on `users.email`. — https://www.prisma.io/docs/prisma-orm/quickstart/mongodb#npm · keywords: index, unique index, users, posts, users.email
- [passage] | Name | Required | Type | Description | | ----------- | -------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | | `map` | **No** | `String` | | | `length` | **No** | `number` | Allows you to specify a maximum length for the subpart of the value to be indexed.<br /><br />MySQL only. | | `sort` | **No** | `String` | Allows you to specify in what order the entries of the constraint are stored in the database. The available options are `Asc` and `Desc`. | | `clustered` | **No** | `Boolean` | Defines whether the constraint is clustered or non-clustered. Defaults to `false`.<br /><br />SQL Server only. | | `where` | **No** | `function` or `object` | Defines a [partial index](https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#configuring-partial-indexes-with-where) that only includes rows matching the specified condition. Accepts `raw("SQL expression")` or an object literal like `{ field: value }`.<br /><br />PostgreSQL, SQLite, SQL Server, and CockroachDB. Requires the `partialIndexes` Preview feature. | — https://www.prisma.io/docs/orm/v7/reference/prisma-schema-reference#arguments-arguments-3 · keywords: index, partial index, map, String, length, number
- [passage] The `sort` argument can also be used on compound indexes: — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#configuring-the-index-sort-order-with-sort-configuring-the-index-sort-order-with-sort · keywords: compound index, index, sort
- [passage] As an example, the following model adds a `Gin` index to the `value` field, with `JsonbPathOps` as the class of operators allowed to use the index: — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#generalized-inverted-index-gin-generalized-inverted-index-gin · keywords: GIN index, index, Gin, value, JsonbPathOps
- [passage] As an example, the following model adds a `Gist` index to the `value` field with `InetOps` as the operators that will be using the index: — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#generalized-search-tree-gist-generalized-search-tree-gist · keywords: GiST index, index, Gist, value, InetOps
- [passage] The BRIN index type is useful if you have lots of data that does not change after it is inserted, such as date and time values. If your data is a good fit for the index, it can store large datasets in a minimal space. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#block-range-index-brin-block-range-index-brin · keywords: BRIN index, index
- [passage] As an example, the following model adds a `Brin` index to the `value` field with `Int4BloomOps` as the operators that will be using the index: — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#block-range-index-brin-block-range-index-brin · keywords: BRIN index, index, Brin, value, Int4BloomOps
- [passage] A table can have at most one clustered index. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#configuring-if-indexes-are-clustered-or-non-clustered-with-clustered-sql-server-configuring-if-indexes-are-clustered-or-non-clustered-with-clustered-sql-server · keywords: clustered index, index
- [passage] And on composite indexes and constraints: — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#configuring-the-name-of-indexes-with-map-configuring-the-name-of-indexes-with-map · keywords: compound index, index
- [passage] > [!NOTE] > Partial indexes are supported on **PostgreSQL**, **SQLite**, **SQL Server**, and **CockroachDB**. They are **not** supported on MySQL. — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#configuring-partial-indexes-with-where-configuring-partial-indexes-with-where · keywords: index, partial index
- [passage] 1. Automatically add `"partialIndexes"` to the `previewFeatures` list in your generator block 2. Represent the partial index predicate using the `raw()` syntax with the database's normalized form of the SQL expression — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#introspection-introspection · keywords: index, partial index, "partialIndexes", previewFeatures, raw()
- [passage] * Table name: `_CategoryToPost` (underscore + model names alphabetically + `To`) * Columns: `A` (FK to first model alphabetically) and `B` (FK to second) * Unique index on both columns, non-unique index on `B` — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/relations/many-to-many-relations#relation-table-conventions-relation-table-conventions · keywords: index, unique index, _CategoryToPost, To,  (FK to first model alphabetically) and 
- [passage] Upsert with composite indexes, or composite primary keys for `onConflictDoUpdate`: — https://orm.drizzle.team/docs/cockroach/insert#where-clauses · keywords: compound index, index, onConflictDoUpdate · also: https://orm.drizzle.team/docs/pg/insert, https://orm.drizzle.team/docs/sqlite/insert
- [passage] Drizzle has simple and flexible API, which lets you easily create such an index using SQL-like syntax: <CodeTabs items={["schema.ts", "migration.sql"]}> <CodeTab> ```ts copy {12,13} import { SQL, sql } from 'drizzle-orm'; import { AnyPgColumn, pgTable, serial, text, uniqueIndex } from 'drizzle-orm/pg-core'; — https://orm.drizzle.team/docs/guides/unique-case-insensitive-email#postgresql · keywords: index, unique index
- [passage] Drizzle has simple and flexible API, which lets you easily create such an index using SQL-like syntax: <CodeTabs items={["schema.ts", "migration.sql"]}> <CodeTab> ```ts copy {12,13} import { SQL, sql } from 'drizzle-orm'; import { AnyMySqlColumn, mysqlTable, serial, uniqueIndex, varchar } from 'drizzle-orm/mysql-core'; — https://orm.drizzle.team/docs/guides/unique-case-insensitive-email#mysql · keywords: index, unique index
- [passage] // custom lower function export function lower(email: AnySQLiteColumn): SQL { return sql`lower(${email})`; } ``` </CodeTab> ```sql CREATE TABLE `users` ( `id` integer PRIMARY KEY NOT NULL, `name` text NOT NULL, `email` text NOT NULL ); --> statement-breakpoint CREATE UNIQUE INDEX `emailUniqueIndex` ON `users` (lower(`email`)); ``` </CodeTabs> — https://orm.drizzle.team/docs/guides/unique-case-insensitive-email#sqlite · keywords: index, unique index, lower(${email}),  </CodeTab> , sql CREATE TABLE ,  ( 
- [passage] Drizzle ORM provides API for both `index` and `unique index` declaration: — https://orm.drizzle.team/docs/mssql/indexes-constraints#indexes · keywords: index, unique index · also: https://orm.drizzle.team/docs/mysql/indexes-constraints, https://orm.drizzle.team/docs/singlestore/indexes-constraints, https://orm.drizzle.team/docs/sqlite/indexes-constraints
- [passage] <Section> ```typescript copy {9-10} import { int, varchar, index, uniqueIndex, mssqlTable } from "drizzle-orm/mssql-core"; — https://orm.drizzle.team/docs/mssql/indexes-constraints#indexes · keywords: index, unique index · also: https://orm.drizzle.team/docs/mysql/indexes-constraints, https://orm.drizzle.team/docs/singlestore/indexes-constraints
- [passage] export const user = mssqlTable("user", { id: int().primaryKey().identity(), name: varchar({ length: 256 }), email: varchar({ length: 256 }), }, (table) => [ index("name_idx").on(table.name), uniqueIndex("email_idx").on(table.email), ]); ``` ```sql {5-6} CREATE TABLE `user` ( ... ); — https://orm.drizzle.team/docs/mssql/indexes-constraints#indexes · keywords: index, unique index, sql {5-6} CREATE TABLE  · also: https://orm.drizzle.team/docs/mysql/indexes-constraints
- [passage] ```typescript // Index declaration reference index("name") .on(table.name) .algorithm("default") // "default" | "copy" | "inplace" .using("btree") // "btree" | "hash" .lock("default") // "none" | "default" | "exclusive" | "shared" ``` — https://orm.drizzle.team/docs/mysql/indexes-constraints#indexes · keywords: B-tree, index
- [passage] <Callout> For many-to-many relationships, it is generally recommended to create the following indexes on the junction table: 1. **Index on each foreign key column individually:** This optimizes queries that filter or join based on a single side of the relationship (e.g., finding all groups for a user OR all users in a group). 2. **Composite index on both foreign key columns together:** This is crucial for efficiently resolving the many-to-many relationship itself. It speeds up queries that need to find the connections between both entities. </Callout> — https://orm.drizzle.team/docs/mysql/relations#many-to-many-relationships · keywords: compound index, index · also: https://orm.drizzle.team/docs/pg/relations, https://orm.drizzle.team/docs/singlestore/relations-v2, https://orm.drizzle.team/docs/sqlite/relations
- [passage] export const user = singlestoreTable("user", { id: int("id").primaryKey().autoincrement(), name: text("name"), email: text("email"), }, (table) => [ index("name_idx").on(table.name), uniqueIndex("email_idx").on(table.email), ]); ``` ```sql {5-6} CREATE TABLE `user` ( ... ); — https://orm.drizzle.team/docs/singlestore/indexes-constraints#indexes · keywords: index, unique index, sql {5-6} CREATE TABLE  · also: https://orm.drizzle.team/docs/sqlite/indexes-constraints
- [passage] <Section> ```typescript {8-9} import { integer, text, index, uniqueIndex, sqliteTable } from "drizzle-orm/sqlite-core"; — https://orm.drizzle.team/docs/sqlite/indexes-constraints#indexes · keywords: index, unique index
- [passage] If you need to remove a GIN index: — https://thenile.dev/docs/extensions/btree_gin#removing-a-gin-index · keywords: GIN index, index
- [passage] If you need to remove a GiST index: — https://thenile.dev/docs/extensions/btree_gist#removing-a-gist-index · keywords: GiST index, index
- [passage] A B-tree index can be created on a `CITEXT` column just like a `TEXT` column: — https://thenile.dev/docs/extensions/citext#creating-an-index-on-citext-columns · keywords: B-tree, index, CITEXT, TEXT
- [passage] * hstore is generally more efficient than JSON for simple key-value pairs * GIN indexes can significantly improve query performance on hstore columns * The storage size of hstore is typically smaller than equivalent JSON storage — https://thenile.dev/docs/extensions/hstore#performance-considerations · keywords: GIN index, index
- [passage] * GiST indexes significantly improve range query performance * IP address operations are very efficient as they use native integer comparisons * Range operations are optimized for both IPv4 and IPv6 * Indexes work well with both IP versions in the same column — https://thenile.dev/docs/extensions/ip4r#performance-considerations · keywords: GiST index, index
- [passage] * **Fast Full-Text Search**: Efficient searching using bigram matching * **Similarity Calculation**: Built-in functions to measure string similarity * **Partial Matching**: Find strings containing specific patterns * **Language Agnostic**: Works well with any language, including non-Latin scripts * **GIN Index Support**: Fast search performance using GIN indexes — https://thenile.dev/docs/extensions/pg_bigm#key-features · keywords: GIN index, index
- [passage] * `pg_bigm.similarity_threshold`: Default similarity threshold (0.0 to 1.0) * `pg_bigm.enable_recheck`: Whether to recheck similarity in search results * `pg_bigm.gin_key_limit`: Maximum number of bigrams for GIN index — https://thenile.dev/docs/extensions/pg_bigm#configuration-parameters · keywords: GIN index, index, pg_bigm.similarity_threshold, pg_bigm.enable_recheck, pg_bigm.gin_key_limit
- [passage] 1. **Indexing**: * Always create spatial indexes (GiST) on geometry columns * Use appropriate coordinate systems for your use case — https://thenile.dev/docs/extensions/postgis#best-practices · keywords: GiST index, index
- [passage] 1. Create GiST indexes on seg columns: — https://thenile.dev/docs/extensions/seg#performance-optimization · keywords: GiST index, index
- [passage] 2. Common operators that can use the GiST index: — https://thenile.dev/docs/extensions/seg#performance-optimization · keywords: GiST index, index
- [passage] B-Tree indexes play a crucial role in enhancing database performance by allowing faster retrieval of specific rows. Imagine them as the index pages in a book, providing quick references to relevant data. — https://thenile.dev/docs/postgres/indexes#b-tree-index · keywords: B-tree, index
- [passage] * B-tree indexes are organized as balanced tree structures. * Each level of the tree acts like a doubly-linked list of pages. * The index starts with a metapage at the beginning of the first segment file. * All other pages are either leaf pages (the lowest level) or internal pages. — https://thenile.dev/docs/postgres/indexes#b-tree-index · keywords: B-tree, index
- [passage] Function form of dot notation — `union_extract(col, 'variant')` is equivalent to `col.variant`. Primarily useful in expression indexes: — https://docs.turso.tech/sql-reference/statements/create-type#union-extract · keywords: expression index, index, union_extract(col, 'variant'), col.variant
- [passage] For partial unique indexes (indexes with a `WHERE` clause), the conflict target must also include a `WHERE` clause that matches the index's condition. — https://docs.turso.tech/sql-reference/statements/upsert#conflict-target · keywords: index, unique index, WHERE
- [passage] For example, the following image shows a compound index where documents are first sorted by `userid` in ascending order (alphabetically). Then, the `scores` for each `userid` are sorted in descending order: — https://www.mongodb.com/docs/manual/core/index-compound/#chunk-0 · keywords: compound index, index, userid, scores
- [passage] This section describes technical details and limitations for compound indexes. — https://www.mongodb.com/docs/manual/core/index-compound/#details · keywords: compound index, index
- [passage] Indexes store references to fields in either ascending (`1`) or descending (`-1`) sort order. For compound indexes, sort order can determine whether the index supports a sort operation. For more information, see [Compound Index Sort Order.](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-compound/sort-order/#std-label-index-compound-sort-order) — https://www.mongodb.com/docs/manual/core/index-compound/#chunk-2 · keywords: compound index, index, ) or descending (
- [passage] For example, to create a unique index on the `email` field of the `users` collection, use the following operation in `mongosh`: — https://www.mongodb.com/docs/manual/core/index-unique/#unique-index-on-a-single-field · keywords: index, unique index, email, users, mongosh
- [passage] For example, to create a unique index on `name`, `email`, and `password` fields of the `users` collection, use the following operation in `mongosh`: — https://www.mongodb.com/docs/manual/core/index-unique/#chunk-1 · keywords: index, unique index, name, email, password, users
- [passage] The unique index permits the insertion of the following documents into the collection since the index enforces uniqueness for the *combination* of `email` and `name` values: — https://www.mongodb.com/docs/manual/core/index-unique/#chunk-1 · keywords: index, unique index, email, name
- [passage] - the index on the shard key - a [compound index](https://www.mongodb.com/docs/manual/reference/glossary/#std-term-compound-index) where the shard key is a[prefix](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-compound/#std-label-compound-index-prefix) - The default `_id` index; however, the`_id` index only enforces the uniqueness constraint**per shard** if the`_id` field is not the shard key. — https://www.mongodb.com/docs/manual/core/index-unique/#chunk-6 · keywords: compound index, index, _id · also: https://www.mongodb.com/docs/manual/core/sharding-shard-key-indexes/
- [passage] The unique index constraints mean that: — https://www.mongodb.com/docs/manual/core/index-unique/#chunk-7 · keywords: index, unique index
- [passage] Unique indexes ensure that a value appears at most once for a given field. — https://www.mongodb.com/docs/manual/core/index-unique/create/#chunk-0 · keywords: index, unique index
- [passage] This example adds a unique index on the `user_id` field of a `members` collection to ensure that there are no duplicate values in the `user_id` field. — https://www.mongodb.com/docs/manual/core/index-unique/create/#about-this-task · keywords: index, unique index, user_id, members
- [passage] MongoDB supports creating indexes on a field, or set of fields, to improve performance for queries. MongoDB supports [flexible schemas](https://www.mongodb.com/docs/manual/data-modeling/#std-label-manual-data-modeling-intro), meaning document field names may differ within a collection. Use wildcard indexes to support queries against arbitrary or unknown fields. — https://www.mongodb.com/docs/manual/core/index-wildcard/#chunk-0 · keywords: index, wildcard index
- [passage] - If your application queries a collection where field names vary between documents, create a wildcard index to support queries on all possible document field names. - If your application repeatedly queries an embedded document field where the subfields are not consistent, create a wildcard index to support queries on all of the subfields. - If your application queries documents that share common characteristics. A compound wildcard index can efficiently cover many queries for documents that have common fields. To learn more, see [Compound Wildcard Indexes.](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/index-wildcard-compound/#std-label-wildcard-index-compound) — https://www.mongodb.com/docs/manual/core/index-wildcard/#chunk-1 · keywords: index, wildcard index
- [passage] Wildcard indexes behave as follows: — https://www.mongodb.com/docs/manual/core/index-wildcard/#details · keywords: index, wildcard index
- [passage] | Index | Default Name | |---|---| | `{ score : 1 }` | `score_1` | | `{ content : "text", "description.tags": "text" }` | `content_text_description.tags_text` | | `{ category : 1, locale : "2dsphere"}` | `category_1_locale_2dsphere` | | `{ "fieldA" : 1, "fieldB" : "hashed", "fieldC" : -1 }` | `fieldA_1_fieldB_hashed_fieldC_-1` | — https://www.mongodb.com/docs/manual/core/indexes/create-index/specify-index-name/#chunk-1 · keywords: geospatial index, index, { score : 1 }, score_1, { content : "text", "description.tags": "text" }, content_text_description.tags_text
- [passage] Multikey indexes collect and sort data stored in arrays. — https://www.mongodb.com/docs/manual/core/indexes/index-types/#multikey-index · keywords: index, multikey index
- [passage] This image shows a multikey index on the `addr.zip` field: — https://www.mongodb.com/docs/manual/core/indexes/index-types/#multikey-index · keywords: index, multikey index, addr.zip
- [passage] Wildcard indexes apply to collections with flexible schemas, where document field names may differ. Use wildcard indexes to support queries against arbitrary or unknown field names. — https://www.mongodb.com/docs/manual/core/indexes/index-types/#wildcard-index · keywords: index, wildcard index
- [passage] Geospatial indexes improve performance for queries on geospatial coordinate data. To learn more, see [Geospatial Indexes.](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-geospatial/#std-label-geospatial-index) — https://www.mongodb.com/docs/manual/core/indexes/index-types/#geospatial-index · keywords: geospatial index, index
- [passage] Hashed indexes support [hashed sharding](https://www.mongodb.com/docs/manual/core/hashed-sharding/#std-label-sharding-hashed-sharding). Hashed indexes index the hash of a field's value. — https://www.mongodb.com/docs/manual/core/indexes/index-types/#chunk-2 · keywords: hashed index, index
- [passage] Clustered indexes specify the order in which [clustered collections](https://www.mongodb.com/docs/manual/core/clustered-collections/#std-label-clustered-collections) store data. Collections created with a clustered index are called clustered collections. — https://www.mongodb.com/docs/manual/core/indexes/index-types/#chunk-3 · keywords: clustered index, index
- [passage] 2d indexes support certain query operators that calculate distances using spherical geometry. Spherical query operators use radians for distance. To use spherical query operators with a 2d index, you must convert distances to radians. — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2d/calculate-distances/#chunk-0 · keywords: geospatial index, index
- [passage] 2d indexes support the following spherical query operators: — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2d/calculate-distances/#chunk-0 · keywords: geospatial index, index
- [passage] The default location bounds for 2d indexes allow latitudes less than -90 and greater than 90, which are invalid values. The behavior of geospatial queries with these invalid points is not defined. — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2d/create/define-location-range/#important · keywords: geospatial index, index
- [passage] Defining a smaller location range for a 2d index reduces the amount of data stored in the index, and can improve query performance. — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2d/create/define-location-range/#important · keywords: geospatial index, index
- [passage] The index covers a smaller location range and has increased performance than a default 2d index. — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2d/create/define-location-range/#results · keywords: geospatial index, index
- [passage] While 2d indexes do not support more than one location field in a document, you can use a [multi-key index](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-multikey/#std-label-index-type-multi-key) to index multiple coordinate pairs in a single document. For example, in the following document, the `locs` field holds an array of coordinate pairs: — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2d/internals/#multi-location-documents-for-2d-indexes · keywords: geospatial index, index, locs
- [passage] - To perform proximity queries on a spherical surface, see [Query for Locations Near a Point on a Sphere.](https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2dsphere/query/proximity-to-geojson/#std-label-2dsphere-query-geojson-proximity) — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2d/query/proximity-flat-surface/#learn-more · keywords: geospatial index, index
- [passage] 2dsphere indexes are available in the following versions: — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2dsphere/2dsphere-index-versions/#chunk-0 · keywords: geospatial index, index
- [passage] | 2dsphere Index Version | Description | |---|---| | Version 4 | MongoDB 8.3 introduces version 4 of 2dsphere indexes. Version 4 is the default version for 2dsphere indexes created in MongoDB 8.3 and later. | | Version 3 | MongoDB 3.2 introduces version 3 of 2dsphere indexes. Version 3 is the default version for 2dsphere indexes created in MongoDB 3.2 and later. | | Version 2 | MongoDB 2.6 introduces version 2 of 2dsphere indexes. Version 2 is the default version for 2dsphere indexes created in MongoDB 2.6 to 3.0. | | Version 1 | MongoDB 2.4 introduces version 1 of 2dsphere indexes. MongoDB 2.4 only supports version 1. | — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2dsphere/2dsphere-index-versions/#chunk-0 · keywords: geospatial index, index
- [passage] The following command creates a version 2 2dsphere index on the `address` field: — https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2dsphere/2dsphere-index-versions/#chunk-1 · keywords: geospatial index, index, address
- [passage] The following operation creates an ascending multikey index on the `test_scores` field of the `students` collection: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-multikey/create-multikey-index-basic/#procedure · keywords: index, multikey index, test_scores, students
- [passage] For example, consider a compound index `{ temperature: 1, humidity: 1 }` with the following bounds: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-multikey/multikey-index-bounds/#chunk-3 · keywords: compound index, index, { temperature: 1, humidity: 1 }
- [passage] After you create the compound index, `$text` queries only scan documents that match a specified equality condition on the `department` field. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-text/limit-number-of-items-scanned-for-text-search/#chunk-2 · keywords: compound index, index, $text, department
- [passage] The following operation creates a wildcard index on all document fields in the `artwork` collection (excluding `_id`): — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/create-wildcard-index-all-fields/#procedure · keywords: index, wildcard index, artwork, _id
- [passage] If there are document fields that you rarely query, you can create a wildcard index that omits those fields. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/create-wildcard-index-multiple-fields/#exclude-specific-fields-from-a-wildcard-index · keywords: index, wildcard index
- [passage] The following operation creates a wildcard index on all document fields in the `products` collection, but omits the `attributes.memory` field from the index: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/create-wildcard-index-multiple-fields/#exclude-specific-fields-from-a-wildcard-index · keywords: index, wildcard index, products, attributes.memory
- [passage] Wildcard indexes do not replace workload-based index planning. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/index-wildcard-compound/#important · keywords: index, wildcard index
- [passage] This example creates a compound wildcard index on the `salesData` collection: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/index-wildcard-compound/#chunk-1 · keywords: index, wildcard index, salesData
- [passage] The wildcard index term, `"$**"`, specifies every field in the collection. The `wildcardProjection` limits the index to the specified fields, `"customFields.addr"` and `"customFields.name"`. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/index-wildcard-compound/#filter-fields-with-a-wildcardprojection · keywords: index, wildcard index, "$**", wildcardProjection, "customFields.addr", "customFields.name"
- [passage] The following example creates a partial compound wildcard index on the `tenantId` field and on all the sub-fields in the `customFields` field, only on documents with a `tenantRegion` of `1`. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/index-wildcard-compound/#create-a-partial-compound-wildcard-index · keywords: index, wildcard index, tenantId, customFields, tenantRegion
- [passage] The wildcard index continues traversing any additional embedded objects or arrays until it reaches a primitive value. It then indexes the primitive value, along with the full path to that field. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/reference/embedded-object-behavior/#chunk-0 · keywords: index, wildcard index
- [passage] A wildcard index that includes the `account` field descends into the `account` object to traverse and index its contents: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/reference/embedded-object-behavior/#chunk-1 · keywords: index, wildcard index, account
- [passage] Given the sample document, the wildcard index adds the following records to the index: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/reference/embedded-object-behavior/#chunk-1 · keywords: index, wildcard index
- [passage] A wildcard index which includes the `ship` field descends into the object to traverse and index its contents: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/reference/embedded-object-behavior/#chunk-2 · keywords: index, wildcard index, ship
- [passage] Wildcard indexes do not record the array position of any given element in an array during indexing. However, MongoDB may still use the wildcard index to fulfill a query that includes a field path with one or more explicit array indices. — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/reference/embedded-object-behavior/#chunk-3 · keywords: index, wildcard index
- [passage] MongoDB can use the wildcard index to fulfill this query: — https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/reference/embedded-object-behavior/#chunk-3 · keywords: index, wildcard index
- [passage] Sharded collections require an index that supports the [shard key](https://www.mongodb.com/docs/manual/reference/glossary/#std-term-shard-key). The index can be an index on the shard key or a [compound index](https://www.mongodb.com/docs/manual/reference/glossary/#std-term-compound-index) where the shard key is a [prefix](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-compound/#std-label-compound-index-prefix) of the index. — https://www.mongodb.com/docs/manual/core/sharding-shard-key-indexes/#chunk-0 · keywords: compound index, index
- [passage] - If the collection is empty, [`sh.shardCollection()`](https://www.mongodb.com/docs/manual/reference/method/sh.shardCollection/#mongodb-method-sh.shardCollection) creates the unique index on the shard key if such an index does not already exist. - If the collection is not empty, you must create the index first before using [`sh.shardCollection()`.](https://www.mongodb.com/docs/manual/reference/method/sh.shardCollection/#mongodb-method-sh.shardCollection) — https://www.mongodb.com/docs/manual/core/sharding-shard-key-indexes/#chunk-5 · keywords: index, unique index, sh.shardCollection()
- [passage] If after the evaluation, the user decides to drop the index, you can drop the hidden index; i.e. you do not need to unhide it first to drop it. — https://www.mongodb.com/docs/manual/reference/command/dropIndexes/#hidden-indexes · keywords: hidden index, index
- [passage] For more information on hidden indexes, see [Hidden Indexes.](https://www.mongodb.com/docs/manual/core/index-hidden/) — https://www.mongodb.com/docs/manual/reference/command/dropIndexes/#chunk-9 · keywords: hidden index, index
- [passage] The following option is available for [2dsphere](https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2dsphere/#std-label-2dsphere-index) indexes only: — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#options-for-2dsphere-indexes · keywords: geospatial index, index
- [passage] | Parameter | Type | Description | |---|---|---| | `2dsphereIndexVersion` | integer | Optional. The `2dsphere` index version number. Users can use this option to override the default version number. For the available versions, see [2dsphere Indexes.](https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2dsphere/#std-label-2dsphere-v2) | — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#options-for-2dsphere-indexes · keywords: geospatial index, index, 2dsphereIndexVersion, 2dsphere
- [passage] [Wildcard indexes](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/#std-label-wildcard-index-core) can use the `wildcardProjection` option. — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#chunk-17 · keywords: index, wildcard index, wildcardProjection
- [passage] - To change the `hidden` option for an index to`true` , use the[`db.collection.hideIndex()`](https://www.mongodb.com/docs/manual/reference/method/db.collection.hideIndex/#mongodb-method-db.collection.hideIndex) method:db.movies.hideIndex( { title: 1 } ) - To change the `hidden` option for an index to`false` , use the[`db.collection.unhideIndex()`](https://www.mongodb.com/docs/manual/reference/method/db.collection.unhideIndex/#mongodb-method-db.collection.unhideIndex) method:db.movies.unhideIndex( { title: 1 } ) — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#chunk-21 · keywords: hidden index, index, hidden, true, db.collection.hideIndex(), false
- [passage] For example, you can use the following code to create a compound index on the `movies` collection of the `sample_mflix` database specifying the numeric fields `year` and `metacritic` and the string field `title`. The index also specifies the collation locale `"fr"` for string comparisons: — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#chunk-28 · keywords: compound index, index, movies, sample_mflix, year, metacritic
- [passage] The following operation creates a wildcard index on the `awards` field: — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#create-a-wildcard-index-on-a-single-field-path · keywords: index, wildcard index, awards
- [passage] With this wildcard index, MongoDB indexes all scalar values of `awards`. If the field is a nested document or array, the wildcard index recurses into the document or array and indexes all scalar fields in the document or array. — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#create-a-wildcard-index-on-a-single-field-path · keywords: index, wildcard index, awards
- [passage] The wildcard index can support arbitrary single-field queries on `awards` or one of its nested fields: — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#create-a-wildcard-index-on-a-single-field-path · keywords: index, wildcard index, awards
- [passage] The following operation creates a wildcard index on all scalar fields (excluding the `_id` field): — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#create-a-wildcard-index-on-all-field-paths · keywords: index, wildcard index, _id
- [passage] With this wildcard index, MongoDB indexes all scalar fields for each document in the collection. If a given field is a nested document or array, the wildcard index recurses into the document or array and indexes all scalar fields in the document or array. — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#create-a-wildcard-index-on-all-field-paths · keywords: index, wildcard index · also: https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/
- [passage] The following operation creates a wildcard index and uses the `wildcardProjection` option to include only scalar values of the `tomatoes.viewer` and `tomatoes.critic` fields in the index. — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#include-specific-fields-in-wildcard-index-coverage · keywords: index, wildcard index, wildcardProjection, tomatoes.viewer, tomatoes.critic
- [passage] The wildcard index supports queries on any scalar field included in the `wildcardProjection`: — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#include-specific-fields-in-wildcard-index-coverage · keywords: index, wildcard index, wildcardProjection · also: https://www.mongodb.com/docs/manual/core/indexes/index-types/index-wildcard/create-wildcard-index-multiple-fields/, https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/
- [passage] This example uses a wildcard index and a `wildcardProjection` document to index the scalar fields for each document in the collection. — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#omit-specific-fields-from-wildcard-index-coverage · keywords: index, wildcard index, wildcardProjection
- [passage] The wildcard index excludes the `tomatoes.viewer` and `tomatoes.critic` fields: — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndex/#omit-specific-fields-from-wildcard-index-coverage · keywords: index, wildcard index, tomatoes.viewer, tomatoes.critic
- [passage] The following example creates two indexes on the `restaurants` collection: an ascending index on the `borough` field and a [2dsphere](https://www.mongodb.com/docs/manual/core/indexes/index-types/geospatial/2dsphere/#std-label-2dsphere-index) index on the `location` field. — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/#chunk-28 · keywords: geospatial index, index, restaurants, borough, location
- [passage] The following operation creates a wildcard index on the `product_attributes` field: — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/#create-a-wildcard-index-on-a-single-field-path · keywords: index, wildcard index, product_attributes
- [passage] With this wildcard index, MongoDB indexes all scalar values of `product_attributes`. If the field is a nested document or array, the wildcard index recurses into the document/array and indexes all scalar fields in the document/array. — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/#create-a-wildcard-index-on-a-single-field-path · keywords: index, wildcard index, product_attributes
- [passage] The wildcard index can support arbitrary single-field queries on `product_attributes` or one of its nested fields: — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/#chunk-30 · keywords: index, wildcard index, product_attributes
- [passage] Wildcard indexes omit the `_id` field by default. To include the `_id` field in the wildcard index, you must explicitly include it in the `wildcardProjection` document. See [parameter documentation](https://www.mongodb.com#std-label-createIndexes-method-wildcard-option) for more information. — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/#note · keywords: index, wildcard index, _id, wildcardProjection
- [passage] The following operation creates a wildcard index and uses the `wildcardProjection` option to include only scalar values of the `product_attributes.colors` and `product_attributes.material` fields in the index. — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/#chunk-32 · keywords: index, wildcard index, wildcardProjection, product_attributes.colors, product_attributes.material
- [passage] This example uses a wildcard index and a `wildcardProjection` document to index the scalar fields for each document in the collection. The wildcard index excludes the `product_attributes.colors` and `product_attributes.material` fields: — https://www.mongodb.com/docs/manual/reference/method/db.collection.createIndexes/#chunk-34 · keywords: index, wildcard index, wildcardProjection, product_attributes.colors, product_attributes.material
- [passage] For example, add the following two compound indexes. The first index orders by `quantity` field first, and then the `type` field. The second index orders by `type` first, and then the `quantity` field. — https://www.mongodb.com/docs/manual/tutorial/analyze-query-plan/#chunk-15 · keywords: compound index, index, quantity, type
- [passage] A [compound index](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-compound/#std-label-index-type-compound) references multiple fields and can dramatically improve query response times. — https://www.mongodb.com/docs/manual/tutorial/equality-sort-range-guideline/#chunk-0 · keywords: compound index, index
- [passage] - To ensure the `bank` and`number` fields do not repeat, make the index[unique.](https://www.mongodb.com/docs/manual/core/index-unique/#std-label-index-type-unique) - To allow indexing of multiple fields, make the index [compound.](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-compound/#std-label-index-type-compound) - To allow indexing of documents inside an array, make the index of the type [multikey.](https://www.mongodb.com/docs/manual/core/indexes/index-types/index-multikey/#std-label-index-type-multikey) — https://www.mongodb.com/docs/manual/tutorial/unique-indexes-schema-validation/#create-a-multi-property-index · keywords: index, multikey index, bank, number
- [passage] Defines a compound [unique constraint](https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/models#defining-a-unique-field) for the specified fields. — https://www.prisma.io/docs/orm/v7/reference/prisma-schema-reference#unique-unique-1 · keywords: @@index, unique constraint
- [passage] > **Note**: Before the `partialIndexes` Preview feature (and before version 4.0.0 / 3.5.0 with the `extendedIndexes` Preview feature), the signature was: > > ```prisma no-lines > @@unique(_ fields: FieldReference[], name: String?, map: String?) > ``` — https://www.prisma.io/docs/orm/v7/reference/prisma-schema-reference#signature-signature-4 · keywords: @@index, partial index, partialIndexes, extendedIndexes
- [passage] * models, fields, and relations, plus how they map to tables and columns * storage details: primary keys, unique constraints, indexes, and foreign keys * named types, enums, and value objects * types and capabilities contributed by extension packs, such as pgvector's `Vector` * content hashes that identify this exact version of the schema — https://www.prisma.io/docs/orm/contract-authoring/the-data-contract#what-the-contract-contains-what-the-contract-contains · keywords: index, unique constraint, Vector
- [passage] * In MySQL/MariaDB, you can specify sort order (`ASC`/`DESC`) directly in unique constraints and indexes * In PostgreSQL, sort order can only be specified on indexes, not on unique constraints * In SQL Server, sort order is supported on all constraints and indexes including `@id` and `@@id` — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/indexes#configuring-the-index-sort-order-with-sort-configuring-the-index-sort-order-with-sort · keywords: index, unique constraint, ASC, DESC, @id, @@id
- [passage] - Create namespaces and attributes - Add indexes and unique constraints - Model relationships - Lock down your schema for production — file:///Users/mitch/.global-ai-hub/llms-full/files/instantdb.com.txt#modeling-data · keywords: index, unique constraint
- [passage] @@unique([firstname, lastname, id]) } ``` — https://www.prisma.io/docs/orm/v7/reference/prisma-schema-reference#general-general-2 · keywords: @@index, unique constraint
- [passage] Composite IDs and compound unique constraints can be defined in your Prisma schema using the [`@@id`](https://www.prisma.io/docs/orm/v7/reference/prisma-schema-reference) and [`@@unique`](https://www.prisma.io/docs/orm/v7/reference/prisma-schema-reference) attributes. — https://www.prisma.io/docs/orm/v7/prisma-client/special-fields-and-types/working-with-composite-ids-and-constraints#working-with-compound-ids-and-unique-constraints-docs-orm-v7-prisma-client-special-fields-and-types-working-with-composite-ids-and-constraints · keywords: @@index, unique constraint, @@id, @@unique
- [passage] * `@id` or `@@id` for primary key * `@unique` or `@@unique` for unique constraint — https://www.prisma.io/docs/orm/v7/prisma-schema/data-model/models#limitations-limitations · keywords: @@index, unique constraint, @id, @@id, @unique, @@unique
- [passage] > **Note**: Before the `partialIndexes` Preview feature, the signature was: > > ```prisma no-lines > @unique(map: String?, length: number?, sort: String?, clustered: Boolean?) > ``` — https://www.prisma.io/docs/orm/v7/reference/prisma-schema-reference#signature-signature-3 · keywords: partial index, partialIndexes
- [passage] export const users = pgTable( 'users', { id: serial('id').primaryKey(), name: text('name').notNull(), email: text('email').notNull(), }, (table) => [ // uniqueIndex('emailUniqueIndex').on(sql`lower(${table.email})`), uniqueIndex('emailUniqueIndex').on(lower(table.email)), ], ); — https://orm.drizzle.team/docs/guides/unique-case-insensitive-email#postgresql · keywords: unique index, lower(${table.email})
- [passage] export const users = mysqlTable( 'users', { id: serial('id').primaryKey(), name: varchar('name', { length: 255 }).notNull(), email: varchar('email', { length: 255 }).notNull(), }, (table) => [ // uniqueIndex('emailUniqueIndex').on(sql`(lower(${table.email}))`), uniqueIndex('emailUniqueIndex').on(lower(table.email)), ] ); — https://orm.drizzle.team/docs/guides/unique-case-insensitive-email#mysql · keywords: unique index, (lower(${table.email}))
- [passage] export const users = sqliteTable( 'users', { id: integer('id').primaryKey(), name: text('name').notNull(), email: text('email').notNull(), }, (table) => [ // uniqueIndex('emailUniqueIndex').on(sql`lower(${table.email})`), uniqueIndex('emailUniqueIndex').on(lower(table.email)), ] ); — https://orm.drizzle.team/docs/guides/unique-case-insensitive-email#sqlite · keywords: unique index, lower(${table.email})
