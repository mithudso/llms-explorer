# llms-full mirror files that refresh_snapshot.sh must NOT publish.
# Each one contains credential-shaped strings that GitHub push protection
# rejects (the repo is public). The files stay in the hub mirror; only the
# public snapshot skips them. One rsync pattern per line.
docs.cloudbase.net.txt
prismatic.io__docs.txt
vercel.com__docs.txt
