# /lca report — indexing (9 sources + the hub's mongodb raw layer · mixed scope: hub export, raw mirror, 7 third-party llms-full mirrors, hub-index extract)

**Scope:** 10 inputs · 65,279 units scanned (49,744 distinct texts embedded) · 2 expansion rounds + 1 exclude-tightening pass ·
kept per round: r0 **1,886** (bare name + the user's 12 aliases, 9 files) → r1 **2,331** (38 terms) → r2 **2,982** (39 terms + `extra.jsonl`) → r2′ **2,947** after 16 more excludes → 2,699 after exact/near-dup folding → **2,115 kept** after classification (584 dropped: 388 by rule, 196 by model review) ·
semantic adds: **1** at z ≥ 3.5 (floor raised from 3.0 after the z ≥ 3.0 adds proved off-topic) — dropped in review; 283 of the 284 units in the z ≥ 3.5 band were already in the keyword pool, 249 near-duplicates folded into `also:`, 474 keyword hits at z < 0.5 reviewed (the polysemy bucket).

**Lexicon:** 39 terms (self 1 · hyponym 18 · part 2 · dependent 3 · instance 2 · measure 2 · whole 1 · related 3 · abbreviation 1 · contrast 1 · problem 3 · hypernym 1 · near-synonym 1) · 43 excludes · zero-hit: **none** after round 2 (before `extra.jsonl`: TTL index, sparse index, index prefix, totalKeysExamined — all MongoDB-manual terms absent from the 82-page export) · low-hit (< 10): TTL index (8), covered query (6), ESR rule (8), blocking sort (7) — each present in one source only (MongoDB).

**Pack (union, `indexing--databases.llms/`):** full ≈180,713 tok (4.3% of ≈4.2M scanned tokens) · small ≈16,238 tok (budget 16,000; 16k chosen per the multi-vendor guidance) · facets present 11/13 (definition 12 · structure 81 · mechanism 159 · parameters 21 · how-to 224 · examples 364 · measures 9 · problems 153 · comparisons 30 · history 2 · facts 1060; no `questions`/`quotes`) · conflicts: **0** (vendor-qualified differences carried as `note:` — MongoDB 64 indexes/collection vs Convex 32/table & 16 fields; Convex requires explicit `withIndex` while SQL planners choose; Atlas Search and pgvector "index" senses flagged) · rights: extractive.
Sources: www.mongodb.com: 891 · docs.convex.dev.txt: 374 · www.prisma.io: 276 · thenile.dev: 213 · docs.turso.tech: 178 · orm.drizzle.team: 141 · instantdb.com.txt: 40 · motherduck.com__docs.txt: 1 · motherduck.com: 1.

**Split (full > 60k → the skill's family rule, executed):** the union pack stays as the complete catalogue and its `llms.txt` `## Child packs` section points at five children sliced by term group (priority: planner → search/vector → types → lifecycle → ORM; 270 bare-term units stay only in the parent):
- `index-lifecycle-and-health--databases.llms/` — Index lifecycle and health: 414 units · full ≈36,065 tok · small ≈8,142 tok
- `index-types--databases.llms/` — Index types: 565 units · full ≈49,290 tok · small ≈8,206 tok
- `orm-index-definitions--databases.llms/` — ORM index definitions: 328 units · full ≈27,210 tok · small ≈8,172 tok
- `query-planner-explain-and-covered-queries--databases.llms/` — Query planner, explain plans and covered queries: 284 units · full ≈28,921 tok · small ≈8,211 tok
- `search-and-vector-indexes--databases.llms/` — Search and vector indexes: 254 units · full ≈23,478 tok · small ≈8,202 tok

**Verification:**
- V1 traceability: every unit line ends in a URL/anchor; 10-link spot-check 10/10 (text present in the named source); programmatic check of all 2,115 units: 0 misses; all 777 `extra.jsonl` units trace to URLs present in the hub's dump. Caveat: 415 Convex/InstantDB units carry `file://…#heading` anchors because those llms-full files have no per-page `Source:` lines.
- V2 precision (20 random kept units): **19/20** (one Turso "File | Purpose" table off-topic → dropped afterwards).
- V3 recall (lexicon): zero-hit none; every candidate with lift ≥ 3 and ≥ 6 pool uses judged (corpus tokens `IndexRange`, `SearchIndexes`, `VectorIndexes`, `@@index`, `withIndex`, `email_idx`, `CREATE INDEX` all covered; `KeyVault`, `LangGraph`, `mongod` rejected as co-occurrence noise).
- V4 recall (semantic): pass ran over the whole scope; all 60 listed suspects + 474 z<0.5 units judged; `query` on 3 bank questions → relevant unit in top 3 for **3/3**.
- V5 coverage: `probe --semantic` on the 10-question bank → small **10/10**, full **10/10**, semantic-covered 10/10 · fresh-context agent on `llms-small.txt`: **10/10** (agent caveats: plan-selection *mechanism* only partial; the small digest has no Drizzle unit and no per-source counts; Postgres `EXPLAIN`/`Seq Scan` terms absent from the whole scope) · on `llms-full.txt`: **10/10** (caveats: no literal "index-only scan", "index bloat", or Postgres "Index Scan"/"Seq Scan" labels anywhere in scope — answered from MongoDB/SQLite terms).
- V6 leakage (40 random `facts` units): 2/40 foreign sense before the final pass ("Reference index for the Prisma 8 ORM client", "Cursor will then index all of the Convex docs") → both dropped; now 0/40 on that sample.
- V7 conflicts: none flagged; 5 `note:` lines for vendor differences.
- V8 budget: small 16,238 ≤ 16,800 ✓ · every non-empty facet present in small ✓ · full > 60k → split executed (children 23–49k each) ✓.
- V9 summary honesty: each clause maps to kept units (Convex definition `s07u007529`, MongoDB `s01u000198`/`s10u000029`, selectivity `s10u001206`, index prefix `s10u000082`, ESR `s10u001221`).
- V10 grammar: `llms_lint.py check llms.txt` → high 0 · medium 1 (P3 D4: 3 duplicate descriptions — generator grammar, `/ldo` P3) · low 2. `llms_lint.py check llms-facts.txt` → high 1 (P7 C6: the 415 `file://` lines are not counted as source URLs), medium 3 (unit type `passage` outside the hub's UNIT_TYPES; facts/full ratio 1.12 by design; 418 units > 400 chars) — all generator/`/ldo` matters, none a fabrication.

**Related concepts worth their own pack:** unique constraint (105 units, near-synonym; PK/UNIQUE constraints across Drizzle/Prisma/Nile) · vector index (148, hyponym — now the `search-and-vector-indexes` child; a full pgvector/Atlas Vector Search pack would add the excluded Pinecone/Upstash sense) · collation and index use (40 MongoDB units kept as context) · Performance Advisor / slow-query analysis (24, related) · sharding shard-key indexes (10, context).

**Files:** `/Users/mitch/.claude/skills/llms-concept-abstractor-workspace/iteration-1/eval-2-indexing-db-discovery/with_skill/outputs/`
- `indexing--databases.llms/{llms.txt, llms-full.txt, llms-small.txt, llms-facts.txt, llms-vocabulary.txt, concept-graph.json, units.jsonl, manifest.json, pool.jsonl, harvest-report.json, semantic.jsonl, semantic-report.json, classified.rules.jsonl, lexicon.json, classified.jsonl, bank.jsonl}`
- five child packs `*--databases.llms/` (each compiled from the parent pool subset with the same lexicon and classification)
- `lexicon.json` (final, 39 terms) · `lexicon.r0.json` · `lexicon.r1.json` · `classified.jsonl` (753 decisions) · `classify_rules.py` · `classify_and_split.py` · `bank.jsonl` · `SCOPE.md` · `extra.jsonl` (1,335 units from the hub's mongodb raw layer) · `scope-files.txt` · `_review/` (the six review buckets) · `_scope-dry-*/` (round-0 dry harvests incl. the excluded vector-DB mirrors) · `_hub-mongodb-raw-dump.jsonl` (72 MB, the dump `extra.jsonl` was cut from; safe to delete)
- indexed: **no** (`--no-persist`; no `docset_indexer index`, no hub write beyond the shared embedding cache) · registered: **no** (`hub_concept_lookup("indexing")` = unknown; not queued) · `/ldo`: not run.

**Gaps:**
- Facets with < 3 units: `history` (2). `measures` has 9 (selectivity ratios, Convex limits, Performance Advisor metrics) — the sources rarely quantify.
- Under-represented in `llms-small.txt` (facet round-robin, not source-balanced): Drizzle (0 of 142 units surfaced), InstantDB, MotherDuck — readers wanting Drizzle should open the `orm-index-definitions` child.
- Sources contributing little: MotherDuck (2 units — its mirror is an 85 KB routing bundle); InstantDB (40, "indexed attributes" only).
- Postgres-side planner material is thin: no `EXPLAIN ANALYZE`, `Seq Scan`/`Index Only Scan`/bitmap-scan text, `pg_stat_user_indexes`, `REINDEX`/bloat or `n_distinct` anywhere in scope (Nile's docs cover index *types*, not the planner) — the planner/covered-query/selectivity facets are answered almost entirely by MongoDB (manual via the hub raw layer), Turso (`EXPLAIN QUERY PLAN`, `sqlite_stat1`) and Convex (`withIndex` vs `.filter`). A Postgres docs mirror would close this.
- Excluded senses: llms.txt "documentation index" · `index.*` filenames and routes (`index.ts/js/html/astro/vue/css/…`, `pages/index`, React Router `index()`) · array index / `indexOf` / `index signature` / `z-index` · LlamaIndex `VectorStoreIndex` · H3 spatial index · Prisma 8 *migration planner* (dropped per unit; shares the word "planner") · Pinecone/Upstash "index" = whole vector collection (mirrors excluded, 3,546 units).
- `file://` anchors for Convex/InstantDB (415 units) — traceable to the mirror file + heading, not to a web URL.

**Decisions made without asking (recorded):** included both MongoDB layers (facts + raw pages) because the export is 82 top-level pages; added the hub's indexed mongodb raw layer as `extra.jsonl` (Step 4's third signal; 777 kept units, the source of every covered-query/ESR/selectivity/explain line) and restricted it to canonical `/docs/manual/` + Atlas Performance Advisor indexing pages; excluded Pinecone and Upstash (foreign sense) rather than merge; raised the semantic floor to 3.5; kept the union pack oversize and executed the split as children instead of shrinking the parent; used rule-based pre-classification for mechanical drops (nav lines, MDX imports, heading-only stubs, snippets with no indexing token) and reviewed the six targeted buckets by hand; 16k small budget.
