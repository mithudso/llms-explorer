#!/bin/sh
# ask — federated semantic query + LLM answer over every hub index.
# Usage: ask "question" [--retrieve-only] [--corpora codebase,logs] [--top 8]
DIR="$(cd "$(dirname "$0")" && pwd)"
PYTHONPATH="$DIR" exec "${HUB_PY:-$DIR/../.venv/bin/python}" -m semantic_ops.ask "$@"
